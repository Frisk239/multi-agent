# Phase G 切片计划 · 2026-07-27

> **方法：** Phase F 收官后开 Phase G（健壮性 → 日用手感 → 纵深）  
> **北星：** 健壮性（不留孤儿 run）→ 日用手感（吸底 / Trap / 空错载 / 脉冲）→ 纵深能力（模板 / 成本 / 虚拟滚动）

## 推荐顺序（A）

| 切片 | 主题 | 优先 | 理由 |
|------|------|------|------|
| **75** | 进程生命周期硬化 | **P0** | 最急：SIGINT 优雅退出 + 孤儿 run 收尸 |
| **76** | Memory 断路器 + 手感包 | P0 | Memory 不拖垮 run + 交互低成本高感知 |
| **77** | 委派边界 hardening | P0 | 子代理可控、不炸 prompt |
| **78** | WS 轻量订阅 | P1 | 详情页不闪 |
| **79** | 成本纵深 + 虚拟滚动 | P1 | 成本可解释 + 列表体验质变 |

**刻意不做：** 富文本全量、Wiki 图谱、云 webhook、密钥入库、daemon 1:1

---

**Goal 提示词（直接复制给 /slice-owner 使用）：**

```
Phase F 71-73 全部收官，启动 Phase G 主路径。方向：健壮性（进程生命周期硬化 + Memory 断路器 + 委派边界）→ 日用手感（WS 轻量订阅 + 交互手感包 + 成本纵深）→ 纵深（虚拟滚动 + 模板库）。采用 Slice Owner 自动迭代：先调研 Multica 差距表和当前残留债 → 选定最优首刀 → 派子代理实现 → Playwright 关刀 → main 直推。严格遵守纯本地、无云、无密钥入库、main 直推、每刀 unit + e2e 要求。
```
