---
artifact: ui-replica-inventory
version: "1.0"
created: 2026-07-08
author: 产品·需求与PRD官
source: multica-image/ (18 张唯一截图 + 中文命名副本)
method: Read 工具逐张视觉审阅
---

# Multica UI Replica — 截图清点与 REQ 映射

> **审阅范围**：`D:\code\multi-agent\chanpin\multica-image\`  
> **唯一截图**：`ref1.png`–`ref18.png`（`img_00.png` ≡ `ref14.png` 重复，不重复计数）  
> **副本**：中文文件名（`收件箱1.png`、`新建issue1.png` 等）与 ref 系列字节一致，以 ref 编号为真源 ID。

---

## 全局信息架构（跨截图一致）

所有页面共享 **暗色主题 + 左栏固定导航 + 顶栏 Tab + 主内容区** 骨架：

```
┌─────────────┬──────────────────────────────────────────────────┐
│  Workspace  │  [Tab: 当前页]  [+]                               │
│  搜索 Ctrl+K│──────────────────────────────────────────────────│
│  新建 Issue C│  主内容（列表 / 看板 / 详情 / 表格 / 模态）        │
│─────────────│                                                  │
│  收件箱      │                                                  │
│  我的 issue  │                                                  │
│─────────────│                                                  │
│  工作区      │                                                  │
│  · Issues   │                                                  │
│  · 项目      │                                                  │
│  · 自动化    │                                                  │
│  · 智能体    │                                                  │
│  · 小队      │                                                  │
│  · 用量      │                                                  │
│─────────────│                                                  │
│  配置        │                                                  │
│  · 运行时    │                                                  │
│  · Skills   │                                                  │
│  · 设置      │                                                  │
│─────────────│                                                  │
│  [?] 帮助    │                                                  │
└─────────────┴──────────────────────────────────────────────────┘
```

| 全局元素 | 可见于 | REQ 前缀 |
|----------|--------|----------|
| 左栏完整 IA | 全部 18 张 | UI-NAV |
| 顶栏 Tab + `+` | ref1–5,7–18 | UI-NAV |
| Ctrl+K 命令面板 | ref6 | UI-CMD |
| 浮动聊天/help 气泡 | 多数页面右下角 | UI-NAV (Should) |

---

## 逐张截图清点

### ref1.png — Skills 列表页

| 字段 | 内容 |
|------|------|
| **页面名** | Skills（工作区 Skill 管理） |
| **顶栏 Tab** | `Skills` + `+` |
| **可见模块** | 页头标题「Skills 127」+ 副标题说明；`+ 新建 skill`；`筛选`；`更新时间` 排序；搜索框「搜索 skill…」；表格列：名称 / 已被使用 / 添加者 / 更新时间 |
| **表格行示例** | `frontend-design` → 产品·设计·原型官；`ui-ux-pro-max` → — 未使用；`web-design-guidelines`（hover 显示 checkbox + `...`） |
| **侧栏高亮** | 配置 → **Skills** |
| **交互点** | 点击行选中；搜索；筛选；排序；新建 skill；行 hover 菜单 |
| **REQ 映射** | UI-SKL-001~006 |

---

### ref2.png — Settings 个人资料

| 字段 | 内容 |
|------|------|
| **页面名** | Settings → 个人资料 |
| **布局** | **双栏**：左二级导航 + 右表单 |
| **二级导航（设置）** | **我的账号**：个人资料（选中）、偏好设置、通知、API Token、Daemon、更新；**工作区**：通用、代码仓库、GitHub、集成、实验室、成员 |
| **主内容** | 头像区「Yu」+「点击上传头像」；姓名输入「Yu Lei」；关于你 textarea（placeholder 描述角色/技术栈）+ `0/2000`；`更新资料` 按钮 |
| **侧栏高亮** | 配置 → **设置** |
| **交互点** | 二级 nav 切换；上传头像；编辑姓名/关于；保存 |
| **REQ 映射** | UI-SET-001~004 |

---

### ref3.png — 收件箱（Issue 详情 · 初始态）

| 字段 | 内容 |
|------|------|
| **页面名** | Inbox / 收件箱 |
| **布局** | **三栏**：左导航 \| 中列表 \| 右 Issue 详情+时间线 |
| **中栏列表** | 标题「收件箱」+ `...`；条目含图标、标题截断、摘要、相对时间（6 分钟）、状态勾 |
| **选中项** | FRI-11「毕设 multi-agent…」 |
| **右栏详情** | 顶：`FRI-11` 全标题 + 时钟/图钉/`...`/关闭；**User request** 正文（含 `D:\code\multi-agent` 路径）；反应按钮 +「+ 添加子 issue」；**动态** 区：参与者头像 +「2 条动态」；队长 comment「Step 0 完成」 |
| **侧栏高亮** | **收件箱** |
| **交互点** | 列表切换选中；滚动时间线；添加子 issue；评论输入（底部未完全可见） |
| **REQ 映射** | UI-ISS-001~006, UI-NAV-004 |

---

### ref4.png — 收件箱（时间线展开 · MoSCoW 评论）

| 字段 | 内容 |
|------|------|
| **页面名** | Inbox — 时间线深览 |
| **与 ref3 差异** | 右栏展示更多 comment：队长 MoSCoW 表（Must/Should/Won't）；用户 Yu Lei 提问；队长回复含 **代码块**（`python -m http.server 8765`）与路径 |
| **可见模块** | Markdown 表格渲染；代码块样式；多级 comment 嵌套 |
| **交互点** | 展开/滚动时间线；代码块只读复制 |
| **REQ 映射** | UI-ISS-005~006, UI-ISS-020（Markdown/代码块渲染） |

---

### ref5.png — 收件箱（资源表 + 答辩路径）

| 字段 | 内容 |
|------|------|
| **页面名** | Inbox — 派活/资源索引 comment |
| **右栏特征** | 队长 comment 含 **资源路径表**（RTM、capability、handoff、IA、research、brief）；绿色高亮「答辩高光路径」步骤列表；底部 Yu 回复框 + 附件图标；「展开更早 11 条动态」；多条 agent 完成 task 记录 |
| **交互点** | 展开更早动态；回复输入；附件按钮 |
| **REQ 映射** | UI-ISS-005~007, UI-ISS-021（展开更早动态） |

---

### ref6.png — 命令面板（Ctrl+K）

| 字段 | 内容 |
|------|------|
| **页面名** | Command Palette（覆盖层） |
| **背景** | 模糊的三栏收件箱/Inbox 界面 |
| **模态内容** | 搜索「输入命令或关键词搜索…」+ `ESC`；**命令** 区：`+ 新建 issue`；**最近** 区：FRI-11~FRI-6 列表，含状态图标 + 外链图标 |
| **交互点** | Ctrl+K 打开；ESC 关闭；选择最近 issue；执行新建命令 |
| **REQ 映射** | UI-CMD-001~005 |

---

### ref7.png — 我的 Issue 五列看板

| 字段 | 内容 |
|------|------|
| **页面名** | My Issues / 我的 issue — Kanban |
| **顶栏** | 标题「我的 issue」；筛选 Tab：**全部** / 已分配 / 我创建的 / 我的智能体和小队；右侧 **0 工作中**、`筛选`、`手动`、看板视图图标 |
| **看板列（5）** | ① 待规划 (0) ② 待办 (0) ③ 进行中 (1) ④ 审核中 (1) ⑤ 已完成 (4) |
| **列头** | 状态色点 + 列名 + 计数 + `...` + `+` |
| **卡片 FRI-10** | ID + 标题 + 描述摘要 + **产品小队** badge +「更新于 23 小时前」 |
| **卡片 FRI-11** | 在 **审核中** 列；含 User request 摘要 + 产品小队 badge +「更新于 50 分钟前」 |
| **空列** | 「无 issue」居中占位 |
| **侧栏高亮** | **我的 issue** |
| **交互点** | 筛选 Tab；拖拽卡片（ implied ）；列 `+` 新建；点击卡片进详情 |
| **REQ 映射** | UI-ISS-010~019 |

---

### ref8.png — 新建 Issue · 手动创建模态

| 字段 | 内容 |
|------|------|
| **页面名** | 手动创建 Issue 模态 |
| **背景** | 模糊五列看板 |
| **模态** | 面包屑 `workspace > 手动创建`；展开/关闭按钮；**issue 标题** 输入；**添加描述…** textarea；提示「创建后 深度研究智能体-生信 会立即开始工作」 |
| **属性 Pills** | 审核中（状态）/ 无优先级 / 指派人（智能体头像+名）/ 添加标签 / 无项目 / `...` |
| **底栏** | 附件；**切换到智能体**；**继续创建** toggle；**创建 Issue** 主按钮 |
| **交互点** | 各 pill 下拉；模式切换；toggle；提交创建 |
| **REQ 映射** | UI-ISS-022~029 |

---

### ref9.png — 小队列表页

| 字段 | 内容 |
|------|------|
| **页面名** | Squads / 小队 列表 |
| **页头** | 「小队 3」；Tab **我的 3** / 全部 3；`+ 新建小队`；`筛选`；`小队` 排序 |
| **表格列** | 小队（图标+名+分类）/ 队长 / 成员（头像堆叠 +N）/ 创建者 |
| **数据行** | 产品小队、生信研究团队、哲学与人文研究小队 |
| **侧栏高亮** | 工作区 → **小队** |
| **交互点** | Tab 切换；点击行进详情；新建；筛选排序 |
| **REQ 映射** | UI-SQD-001~005 |

---

### ref10.png — 小队详情 · Members Tab

| 字段 | 内容 |
|------|------|
| **页面名** | Squad Detail — Members |
| **布局** | 左摘要卡 + 右 Tab 内容（双栏详情） |
| **面包屑** | `小队 > 产品小队`；右上归档/删除图标 |
| **左卡** | 产品小队 / 产品；详情：队长、成员 4、创建者 Yu Lei、创建/更新 1天前 |
| **右 Tab** | **Members**（选中）/ Instructions |
| **成员列表** | 产品·策划队长（负责人 badge、空闲绿点、leader 角色）；调研/PRD/原型官（Agent badge、空闲、添加角色…、最近活动）；hover 显示弹出/收藏/删除 |
| **操作** | `+ 创建智能体`；`+ 添加成员` |
| **交互点** | Tab 切换；成员卡片 hover 操作；添加成员 |
| **REQ 映射** | UI-SQD-006~012 |

---

### ref11.png — 小队详情 · Instructions Tab

| 字段 | 内容 |
|------|------|
| **页面名** | Squad Detail — Instructions |
| **右 Tab 内容** | Instructions 选中；说明文字（注入 Leader prompt）；标题「产品小队—队长派活路由」；**小队编制表**（角色/Skills/职责）；**派活路由表**（场景→负责人）；底部 **保存** |
| **交互点** | 编辑 instructions（textarea/markdown）；保存 |
| **REQ 映射** | UI-SQD-013~015 |

---

### ref12.png — 新建 Issue · 智能体创建（空态）

| 字段 | 内容 |
|------|------|
| **页面名** | 通过智能体创建 Issue 模态 |
| **背景** | 模糊收件箱三栏 |
| **模态** | `workspace > 通过智能体创建`；**创建者** badge「产品小队」；大输入框 placeholder「告诉智能体要做什么…」；**无项目** 文件夹图标 |
| **底栏** | 附件；**切换到手动**；继续创建 toggle；**创建 Ctrl+Enter** |
| **交互点** | NL 输入；切换手动；创建 |
| **REQ 映射** | UI-ISS-030~033 |

---

### ref13.png — 新建 Issue · 智能体搜索下拉

| 字段 | 内容 |
|------|------|
| **页面名** | 通过智能体创建 — Agent 选择器 |
| **与 ref12 差异** | 输入框有文本「修一下 Web 项目里收件箱加载慢的问题」；下拉 **智能体** 列表：Multica Helper（星标）、深度研究智能体-生信、生信转录组分析官… |
| **交互点** | 搜索智能体/小队；选择 agent；创建 |
| **REQ 映射** | UI-ISS-034~036 |

---

### ref14.png / img_00.png — 运行时页

| 字段 | 内容 |
|------|------|
| **页面名** | Runtimes / 运行时 |
| **布局** | 左机器列表 + 右机器详情（双栏主区） |
| **左栏** | 「运行时 3」；搜索 machine；Tab All 1 / Online 1 / Errors 0；本机 → **Frisk239** Local badge + 3 runtimes 图标 |
| **右栏头** | Frisk239 · **在线** · 本地·这台机器；`3 runtimes · 3 online · all idle · v0.3.40 · daemon…`；`+ 添加运行时`；`+ 添加电脑`；查看日志/重启/停止 |
| **表格** | 列：运行时 / 健康度 / 智能体 / 费用·7天 / CLI；行：Claude、Cursor（$3.31, 4 agents）、Opencode（$0.00, +8） |
| **侧栏高亮** | 配置 → **运行时** |
| **交互点** | 选机器；添加 runtime；表格行查看；停止/重启（mock） |
| **REQ 映射** | UI-RT-001~008 |

---

### ref15.png — 智能体列表页

| 字段 | 内容 |
|------|------|
| **页面名** | Agents / 智能体 列表 |
| **页头** | 「智能体 15」+ 说明文案 + `+ 新建智能体` |
| **Tab** | 我的 15 / 全部 15 / 已归档 4 |
| **工具栏** | 搜索智能体…；筛选；最近活跃 排序 |
| **表格列** | 智能体（图标+名+分类）/ 状态（在线绿点）/ 所有者 / 运行时 / 最近活跃 / 运行次数 |
| **数据行** | 产品·策划队长 Cursor；Multica Helper；多领域深度研究智能体… |
| **侧栏高亮** | 工作区 → **智能体** |
| **交互点** | 点击行进详情；Tab；搜索筛选；新建 |
| **REQ 映射** | UI-AGT-001~006 |

---

### ref16.png — 智能体详情 · 动态 Tab

| 字段 | 内容 |
|------|------|
| **页面名** | Agent Detail — 动态 |
| **布局** | 左属性卡 + 右 **8 Tab** 内容 |
| **面包屑** | `智能体 > 产品·策划队长` |
| **左卡** | 头像；产品·策划队长 / 产品 / 在线；属性：Cursor(Frisk239)、模型 auto、可见性 工作区、并发 6；详情：Yu Lei、1天前/9分钟前；SKILLS 6 tags + 添加；绑定飞书 |
| **Tabs** | **动态** / Tasks / 指令 / Skills / 环境变量 / 自定义参数 / MCP / 集成 |
| **动态内容** | 当前（无任务）；近30天：44次运行、84%成功、均时2m05s、7失败 + sparkline；最近工作列表 FRI-11 等 + 查看更多 |
| **交互点** | Tab 切换；查看任务历史；添加 skill |
| **REQ 映射** | UI-AGT-007~014 |

---

### ref17.png — 智能体详情 · Skills Tab

| 字段 | 内容 |
|------|------|
| **页面名** | Agent Detail — Skills |
| **Tab 内容** | Skills 选中；说明 + `+ 添加 skill`；skill 列表项（图标+名+描述+删除） |
| **Skills 示例** | define-prioritization-framework、define-problem-statement、develop-solution-brief、product-lens、utility-pm-critic |
| **交互点** | 添加/删除 skill |
| **REQ 映射** | UI-AGT-015~017 |

---

### ref18.png — 智能体详情 · MCP Tab

| 字段 | 内容 |
|------|------|
| **页面名** | Agent Detail — MCP |
| **Tab 内容** | MCP 选中；说明文案；**清空** 按钮；JSON 代码编辑器（exa-web-search mcpServers 示例）；**保存** |
| **交互点** | 编辑 JSON；清空；保存（mock） |
| **REQ 映射** | UI-AGT-018~020 |

---

## 模块 → REQ 前缀汇总

| 前缀 | 页面/组件 | 截图来源 | Must 条数 |
|------|-----------|----------|-----------|
| **UI-NAV** | 全局侧栏 IA、顶栏 Tab、工作中计数 | 全部 | 12 |
| **UI-CMD** | Ctrl+K 命令面板 | ref6 | 5 |
| **UI-ISS** | 收件箱三栏、五列看板、新建模态、卡片 | ref3–5,7–8,12–13 | 36 |
| **UI-SQD** | 小队列表+详情 Members/Instructions | ref9–11 | 15 |
| **UI-AGT** | 智能体列表+详情 8 Tab | ref15–18 | 20 |
| **UI-SKL** | Skills 表格页 | ref1 | 6 |
| **UI-SET** | Settings 二级导航+个人资料 | ref2 | 8 |
| **UI-RT** | 运行时机器列表+表格 | ref14 | 8 |
| **UI-WIK** | Wiki 嵌入侧栏 IA（毕设增量） | V1 保留 | 4 |

**V2 Must 合计：114 条**（含 V1 行为回归 32 条，见 RTM §V2 追溯）

---

## V1 → V2 Gap（亲眼对照）

| 维度 | V1 原型 | 截图目标 |
|------|---------|----------|
| 左栏 | 4 项 | 搜索+新建+收件箱+我的 issue+工作区 6 项+配置 3 项 |
| 看板 | 3 列 | **5 列** + 筛选 Tab + 视图切换 + 0工作中 |
| 卡片 | 简标题 | ID+标题+摘要+squad badge+相对时间 |
| 收件箱 | 无 | **三栏** 列表+详情+时间线 |
| 新建 | 无 | 智能体/手动 **双模态** + pills |
| 小队 | 右栏 briefing | **列表+详情** Members/Instructions Tab |
| 智能体 | 简单 CRUD | 列表表 + 详情 **8 Tab** |
| Skills | 表单 | **表格** + 搜索筛选 |
| 设置 | 无 | **二级导航** 8+ 项 |
| 运行时 | 下拉 mock | **机器列表+Runtime 表格** |
| 全局 | 无 | **Ctrl+K**、顶栏 Tab |

---

## 答辩路径保留（FRI-11）

V2 不得破坏以下路径（与 V1 seed 兼容）：

1. **收件箱或我的 issue** → 选中 FRI-11
2. 时间线见队长 comment + @mention
3. **小队** → 产品小队 → Members 见 4 成员
4. Issue assignee = 产品小队 → Instructions/briefing 可见

---

## 已读截图清单

| # | 文件 | 页面 |
|---|------|------|
| 1 | ref1.png | Skills 表格 |
| 2 | ref2.png | Settings 个人资料 |
| 3 | ref3.png | 收件箱 三栏 |
| 4 | ref4.png | 收件箱 时间线 |
| 5 | ref5.png | 收件箱 资源表 |
| 6 | ref6.png | 命令面板 |
| 7 | ref7.png | 五列看板 |
| 8 | ref8.png | 手动创建 Issue |
| 9 | ref9.png | 小队列表 |
| 10 | ref10.png | 小队 Members |
| 11 | ref11.png | 小队 Instructions |
| 12 | ref12.png | 智能体创建 Issue |
| 13 | ref13.png | 智能体选择器 |
| 14 | ref14.png | 运行时 |
| 15 | ref15.png | 智能体列表 |
| 16 | ref16.png | 智能体 动态 Tab |
| 17 | ref17.png | 智能体 Skills Tab |
| 18 | ref18.png | 智能体 MCP Tab |
| — | img_00.png | ≡ ref14（跳过） |

---

## Revision History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2026-07-08 | 产品·需求与PRD官 | 逐张 Read 18 截图初版 |
