-- Local directory path lock state waiting_local_directory on agent_run (SQLite CHECK rebuild not required;
-- drizzle enum is documentation; existing rows stay valid; new value accepted as text).
SELECT 1;
