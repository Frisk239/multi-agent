# S01 看板 + WebSocket 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **本项目工程模式：** AGENTS.md 规定的"垂直切片 × 计划者-执行者"。计划者（主会话）把本计划按**自然依赖边界**切成若干执行者片段，每个片段由**用户新开的会话**执行（保证干净上下文）。执行者之间靠 `app/.progress/` 下的 handoff 文档串行交接。**执行者数量不固定**——下面的 `impl-1 / impl-2 / impl-3` 分组是按依赖关系（契约层 → 后端 → 前端）给出的**参考边界**，计划者可在执行中合并或拆分。每个执行者在新会话里只读自己的 Task 组 + 前序 handoff + 计划者给的注意点。
> **spec 真源：** [`docs/superpowers/specs/2026-07-08-s01-kanban-ws-design.md`](../specs/2026-07-08-s01-kanban-ws-design.md)（设计依据，动手前必读对应章节）

**Goal:** 端到端打通最薄的看板实时同步路径——monorepo 骨架 + shared 契约 + DB schema/seed + Issue CRUD API + 六列看板 + WebSocket 推送。

**Architecture:** pnpm workspace 三包（server/web/shared）；Fastify + @fastify/websocket + Drizzle + better-sqlite3 后端；Next.js + React Query + Zustand 前端；EventBus → WsBroadcaster 三跳广播。照 multica `001_init.up.sql` 的 issue 7 态 schema。

**Tech Stack:** TypeScript · pnpm workspace · Fastify · @fastify/websocket · Drizzle ORM · better-sqlite3 · drizzle-kit · Next.js (App Router) · React Query · Zustand · Zod · tsx

## Global Constraints

（来自 spec，所有 Task 隐式遵守）

- **语言：** TypeScript 全栈，`strict: true`
- **Monorepo：** pnpm workspace，三包 `@ma/shared` / `@ma/server` / `@ma/web`，包间用 `workspace:*`
- **DB：** SQLite（better-sqlite3 驱动），id 用 text 存 UUID（SQLite 无原生 uuid）
- **状态枚举：** `issue.status` 7 态 `backlog|todo|in_progress|in_review|done|blocked|cancelled`；`priority` 5 档 `urgent|high|medium|low|none`
- **assignee 多态：** DB 扁平 `assignee_type`+`assignee_id`，API 嵌套 `{type,id,label}|null`（label 服务端填充）
- **HTTP：** PUT 而非 PATCH（照 multica）
- **WS：** 单向广播（服务端→客户端），无房间协议，客户端不发消息
- **端口：** server :3001，web :3000
- **Git：** 所有工程代码在 `feat/s01-kanban-ws` 分支，**绝不直接 push main**；Conventional Commits（`feat:`/`chore:`/`test:` 等）
- **工作语言：** 中文为主（注释/seed 数据），代码标识符英文
- **不动：** `references/repos/`（只读）、`chanpin/prototype/`（零依赖原型，不引入构建步骤）

## 文件结构（分解决策锁定）

```
app/
├── package.json                  根，pnpm workspace，scripts: dev/typecheck
├── pnpm-workspace.yaml           packages: [server, web, shared]
├── tsconfig.base.json            共享 strict 配置
├── .gitignore                    node_modules/dist/db/*.db
├── packages/
│   ├── shared/                   impl-1
│   │   ├── src/
│   │   │   ├── schema.ts         Zod 全部契约（spec §4）
│   │   │   └── index.ts          re-export
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── server/                   impl-2
│   │   ├── src/
│   │   │   ├── db/
│   │   │   │   ├── schema.ts     Drizzle schema（spec §3）
│   │   │   │   ├── seed.ts       seed 数据（spec §3.4）
│   │   │   │   └── client.ts     better-sqlite3 + drizzle 实例 + label map
│   │   │   ├── orchestration/
│   │   │   │   ├── event-bus.ts  §6.3
│   │   │   │   └── ws-broadcaster.ts §6.4
│   │   │   ├── routes/
│   │   │   │   ├── issues.ts     §5
│   │   │   │   └── ws.ts         §6
│   │   │   ├── app.ts            Fastify 组装 + 路由注册
│   │   │   └── index.ts          启动 :3001
│   │   ├── drizzle.config.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── web/                      impl-3
│       ├── app/
│       │   ├── layout.tsx
│       │   ├── page.tsx          看板页（唯一视图）
│       │   └── globals.css       移植 tokens.css + 补 --status-blocked
│       ├── components/
│       │   ├── KanbanBoard.tsx
│       │   ├── KanbanColumn.tsx
│       │   ├── IssueCard.tsx
│       │   └── NewIssueForm.tsx
│       ├── lib/
│       │   ├── api.ts            React Query hooks
│       │   ├── ws.ts             WS 单例 + Zustand store
│       │   └── providers.tsx     QueryClientProvider + WS 挂载
│       ├── package.json
│       └── tsconfig.json
```

---

# 执行者片段 A（impl-1）：monorepo 骨架 + shared 契约

> **参考边界：** 这是一个建议的执行者片段（契约层，所有人依赖的地基）。计划者在 `s01-planner-1.md` 给出精确的会话范围。
> **会话边界：** 开新会话，读 AGENTS.md + spec §2/§3/§4 + 本 Task 组 + `s01-planner-1.md`。在 `feat/s01-kanban-ws` 分支工作。完成后写 `app/.progress/s01-impl-1.md`。

### Task 1.1: 建 feature 分支 + monorepo 根

**Files:**
- Create: `app/package.json`
- Create: `app/pnpm-workspace.yaml`
- Create: `app/tsconfig.base.json`
- Create: `app/.gitignore`

**Interfaces:** 产出的根配置是后续所有 Task 的容器。

- [ ] **Step 1: 建 feature 分支**

```bash
cd D:/code/multi-agent
git checkout -b feat/s01-kanban-ws
git status   # 确认在 feat/s01-kanban-ws 分支
```

- [ ] **Step 2: 写 `app/package.json`**

```json
{
  "name": "multi-agent-app",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --filter=@ma/server --filter=@ma/web run dev",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "packageManager": "pnpm@10.33.0"
}
```

- [ ] **Step 3: 写 `app/pnpm-workspace.yaml`**

```yaml
packages:
  - 'packages/*'
```

- [ ] **Step 4: 写 `app/tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

- [ ] **Step 5: 写 `app/.gitignore`**

```
node_modules/
dist/
*.local
*.db
*.db-journal
.next/
.turbo/
```

- [ ] **Step 6: 提交**

```bash
git add app/package.json app/pnpm-workspace.yaml app/tsconfig.base.json app/.gitignore
git commit -m "chore(s01): monorepo 根骨架（pnpm workspace）"
```

---

### Task 1.2: shared 包 + Zod 契约

**Files:**
- Create: `app/packages/shared/package.json`
- Create: `app/packages/shared/tsconfig.json`
- Create: `app/packages/shared/src/schema.ts`
- Create: `app/packages/shared/src/index.ts`

**Interfaces:**
- Produces: `@ma/shared` 导出 `IssueStatus, Priority, AssigneeType, CreatorType, Assignee, Issue, CreateIssueInput, UpdateIssueInput, validateUpdateIssue, IssueCreatedEvent, IssueUpdatedEvent, DomainEvent`（类型 + Zod schema 双导出）

**必读：** spec §4（全部契约）、§3.2（issue 表字段）、§4.2 的 label 说明（R2 修订）。

- [ ] **Step 1: 写 `app/packages/shared/package.json`**

```json
{
  "name": "@ma/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
```

> 说明：S01 用 TS 源码直接消费（`main` 指向 `src/index.ts`），不构建。server/web 用 tsx/next 直接读 .ts。这样最简，避免引入构建步骤。

- [ ] **Step 2: 写 `app/packages/shared/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: 写 `app/packages/shared/src/schema.ts`（核心契约，照 spec §4）**

```typescript
import { z } from 'zod';

// —— 枚举（spec §4.1）——
export const IssueStatus = z.enum([
  'backlog', 'todo', 'in_progress', 'in_review', 'done', 'blocked', 'cancelled',
]);
export type IssueStatus = z.infer<typeof IssueStatus>;

export const Priority = z.enum(['urgent', 'high', 'medium', 'low', 'none']);
export type Priority = z.infer<typeof Priority>;

export const AssigneeType = z.enum(['member', 'agent', 'squad']);
export type AssigneeType = z.infer<typeof AssigneeType>;

export const CreatorType = z.enum(['member', 'agent']);
export type CreatorType = z.infer<typeof CreatorType>;

// —— 多态指派（spec §4.2，含 R2 label 修订）——
// label 是展示用冗余字段，服务端 GET 时填充；POST/PUT 输入不接受 label
export const Assignee = z
  .object({
    type: AssigneeType,
    id: z.string().uuid(),
    label: z.string(),
  })
  .nullable();
export type Assignee = z.infer<typeof Assignee>;

// —— Issue 实体（spec §4.3）——
export const Issue = z.object({
  id: z.string().uuid(),
  workspaceId: z.string().uuid(),
  identifier: z.string(),
  title: z.string(),
  description: z.string().nullable(),
  status: IssueStatus,
  priority: Priority,
  assignee: Assignee,
  creatorType: CreatorType,
  creatorId: z.string().uuid(),
  position: z.number(),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
});
export type Issue = z.infer<typeof Issue>;

// —— API 输入（spec §4.4）——
export const CreateIssueInput = z.object({
  title: z.string().min(1),
  description: z.string().optional(),
  priority: Priority.optional().default('none'),
  assignee: z
    .object({
      type: AssigneeType,
      id: z.string().uuid(),
    })
    .nullable()
    .optional()
    .default(null),
  // 注意：CreateIssueInput 的 assignee 输入不带 label（服务端权威填充）
  // status 不接受——新建恒 backlog
  // identifier 不接受——服务端生成
});
export type CreateIssueInput = z.infer<typeof CreateIssueInput>;

export const UpdateIssueInput = z.object({
  title: z.string().min(1).optional(),
  description: z.string().nullable().optional(),
  status: IssueStatus.optional(),
  priority: Priority.optional(),
  position: z.number().optional(),
  // assignee S01 不允许改（拖拽不改指派）—— spec D 偏离，放开留给 S02+
});
export type UpdateIssueInput = z.infer<typeof UpdateIssueInput>;

// R1 修订：refine 改显式字段校验，避免 Object.keys 歧义
// 用法：UpdateIssueInput.parse(body) 后调 validateUpdateIssue，false 则 400
export function validateUpdateIssue(d: UpdateIssueInput): boolean {
  return (
    d.title !== undefined ||
    d.description !== undefined ||
    d.status !== undefined ||
    d.priority !== undefined ||
    d.position !== undefined
  );
}

// —— WS 事件（spec §4.5）——
export const IssueCreatedEvent = z.object({
  type: z.literal('issue:created'),
  issue: Issue,
});
export type IssueCreatedEvent = z.infer<typeof IssueCreatedEvent>;

export const IssueUpdatedEvent = z.object({
  type: z.literal('issue:updated'),
  issue: Issue,
  statusChanged: z.boolean(),
  prevStatus: IssueStatus.nullable(),
});
export type IssueUpdatedEvent = z.infer<typeof IssueUpdatedEvent>;

export type DomainEvent = IssueCreatedEvent | IssueUpdatedEvent;
```

- [ ] **Step 4: 写 `app/packages/shared/src/index.ts`**

```typescript
export * from './schema.js';
```

> 注意：`moduleResolution: bundler` + `module: ESNext` 下，相对 import 要带 `.js` 扩展（TS 5+ 的 ESM 规则）。

- [ ] **Step 5: 安装依赖 + typecheck**

```bash
cd app
pnpm install
pnpm --filter @ma/shared typecheck
```

预期：typecheck 通过，无错误。

- [ ] **Step 6: 提交**

```bash
cd D:/code/multi-agent
git add app/packages/shared app/pnpm-lock.yaml
git commit -m "feat(s01): shared 包 Zod 契约（Issue/Assignee/事件，含 R1/R2 修订）"
```

---

### Task 1.3: 写 handoff + impl-1 自测

**Files:**
- Create: `app/.progress/s01-impl-1.md`

- [ ] **Step 1: 跑全量 typecheck 确认**

```bash
cd app
pnpm -r typecheck
```

预期：@ma/shared 通过（server/web 还没建，pnpm -r 只会跑存在的包）。

- [ ] **Step 2: 写 handoff `app/.progress/s01-impl-1.md`**

照 `app/.progress/_TEMPLATE.md` 填：
- **上下文：** S01 第一个执行者，建了 monorepo 根 + shared 契约
- **完成了什么：** 列 Task 1.1/1.2 产出
- **自测结果：** 贴 `pnpm --filter @ma/shared typecheck` 实际输出
- **与计划的偏离：** 无 / 有则记原因
- **给 impl-2 的注意点（关键）：**
  - `@ma/shared` 的 import 路径是 `@ma/shared`（package name）
  - shared 不构建，server/web 直接读 .ts 源码（`main: ./src/index.ts`）
  - 相对 import 要带 `.js` 扩展（ESM 规则）
  - `CreateIssueInput.assignee` 输入形态是 `{type,id}|null`（无 label），输出 `Issue.assignee` 是 `{type,id,label}|null`
  - `validateUpdateIssue` 是独立函数（不在 Zod 链上），parse 后单独调
- **验收结论：** 计划者填

- [ ] **Step 3: 提交 handoff**

```bash
git add app/.progress/s01-impl-1.md
git commit -m "docs(s01): impl-1 handoff（monorepo+shared 完成）"
```

---

# 执行者片段 B（impl-2）：server 全栈

> **参考边界：** 建议的后端执行者片段。可被计划者按实际情况拆分（如把 schema/seed 与 routes 拆成两个执行者）或合并到 impl-3。
> **会话边界：** 开新会话，读 AGENTS.md + spec §3/§5/§6 + impl-1 handoff + 本 Task 组 + 计划者 handoff。在 `feat/s01-kanban-ws` 分支继续工作（不开新分支）。完成后写 `app/.progress/s01-impl-2.md`。
> **本会话产出是下一执行者的接口依赖**——API 契约和 WS 事件形态必须写清。

### Task 2.1: server 包脚手架 + Drizzle schema

**Files:**
- Create: `app/packages/server/package.json`
- Create: `app/packages/server/tsconfig.json`
- Create: `app/packages/server/drizzle.config.ts`
- Create: `app/packages/server/src/db/schema.ts`

**Interfaces:**
- Consumes: `@ma/shared` 的所有 Zod schema（impl-1 产出）
- Produces: Drizzle schema（`issues`, `workspaces`, `users`, `agents`, `squads`, `skills` 表）

**必读：** spec §3（数据模型）、§3.2（issue 表）、§3.3（多态指派）、multica `references/repos/multica/server/migrations/001_init.up.sql:52-72`（issue 表真源）。

- [ ] **Step 1: 写 `app/packages/server/package.json`**

```json
{
  "name": "@ma/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:seed": "tsx src/db/seed.ts"
  },
  "dependencies": {
    "@ma/shared": "workspace:*",
    "fastify": "^4.28.0",
    "@fastify/websocket": "^10.0.0",
    "@fastify/cors": "^9.0.0",
    "drizzle-orm": "^0.33.0",
    "better-sqlite3": "^12.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "drizzle-kit": "^0.24.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
```

> 注意：版本号是写作时合理的较新版本。`pnpm install` 后若锁到更新的 minor/patch，以实际为准，记录进 handoff。

- [ ] **Step 2: 写 `app/packages/server/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "types": ["node"]
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: 写 `app/packages/server/src/db/schema.ts`（Drizzle，照 spec §3.2）**

```typescript
import { sqliteTable, text, real, integer, index, check } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';

// —— workspace（spec §3.1，单行）——
export const workspaces = sqliteTable('workspace', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  description: text('description'),
  createdAt: integer('created_at').notNull(),
});

// —— user（spec §3.1，单行林远）——
export const users = sqliteTable('user', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  email: text('email'),
  createdAt: integer('created_at').notNull(),
});

// —— agent（spec §3.1，4 行静态，用于 assignee label）——
export const agents = sqliteTable('agent', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  category: text('category'),
  createdAt: integer('created_at').notNull(),
});

// —— squad（spec §3.1，3 行静态，用于 assignee label）——
export const squads = sqliteTable('squad', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  leaderId: text('leader_id'),
  createdAt: integer('created_at').notNull(),
});

// —— skill（spec §3.1，5 行静态，S01 不展示，纯预留）——
export const skills = sqliteTable('skill', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  url: text('url'),
  createdAt: integer('created_at').notNull(),
});

// —— issue（spec §3.2，照 multica 001_init.up.sql:52-72）——
export const issues = sqliteTable(
  'issue',
  {
    id: text('id').primaryKey(),
    workspaceId: text('workspace_id')
      .notNull()
      .references(() => workspaces.id),
    identifier: text('identifier').notNull(),
    title: text('title').notNull(),
    description: text('description'),
    status: text('status', { enum: ['backlog', 'todo', 'in_progress', 'in_review', 'done', 'blocked', 'cancelled'] })
      .notNull()
      .default('backlog'),
    priority: text('priority', { enum: ['urgent', 'high', 'medium', 'low', 'none'] })
      .notNull()
      .default('none'),
    assigneeType: text('assignee_type', { enum: ['member', 'agent', 'squad'] }),
    assigneeId: text('assignee_id'),
    creatorType: text('creator_type', { enum: ['member', 'agent'] }).notNull(),
    creatorId: text('creator_id').notNull(),
    position: real('position').notNull().default(0),
    createdAt: integer('created_at').notNull(),
    updatedAt: integer('updated_at').notNull(),
  },
  (t) => ({
    statusWorkspaceIdx: index('idx_issue_status_workspace').on(t.workspaceId, t.status),
    assigneeIdx: index('idx_issue_assignee').on(t.assigneeType, t.assigneeId),
  }),
);
```

> 说明：Drizzle 的 enum 是 TS 层约束，SQLite 层 CHECK 需要手动加。S01 先靠 Drizzle enum + 应用层 Zod 校验，CHECK 约束等 db:generate 后视 drizzle-kit 支持度补（记入 handoff，不阻塞）。

- [ ] **Step 4: 写 `app/packages/server/drizzle.config.ts`**

```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './drizzle',
  dialect: 'sqlite',
  dbCredentials: {
    url: './dev.db',
  },
});
```

- [ ] **Step 5: 安装依赖 + typecheck**

```bash
cd app
pnpm install
pnpm --filter @ma/server typecheck
```

预期：typecheck 通过。若 better-sqlite3 原生编译失败（Windows 偶发），查 handoff 排错。

- [ ] **Step 6: 提交**

```bash
git add app/packages/server app/pnpm-lock.yaml
git commit -m "feat(s01): server 脚手架 + Drizzle schema（照 multica issue 7 态）"
```

---

### Task 2.2: DB client + seed + label map

**Files:**
- Create: `app/packages/server/src/db/client.ts`
- Create: `app/packages/server/src/db/seed.ts`
- Create: `app/packages/server/src/db/migrate.ts`

**Interfaces:**
- Produces: `db`（drizzle 实例）、`resolveAssigneeLabel(type, id)`（label map 查询，供 routes 用）、seed/migrate 脚本

**必读：** spec §3.4（seed 数据 + position 策略 R3）、§4.2（assignee label 说明 R2）。

- [ ] **Step 1: 写 `app/packages/server/src/db/client.ts`**

```typescript
import Database from 'better-sqlite3';
import { drizzle } from 'drizzle-orm/better-sqlite3';
import * as schema from './schema.js';

const DB_PATH = process.env.DB_PATH ?? './dev.db';

export const sqlite = new Database(DB_PATH);
sqlite.pragma('journal_mode = WAL');

export const db = drizzle(sqlite, { schema });

// —— label map（spec §4.2 R2）：静态 seed 数据，启动时加载到内存，O(1) 查询 ——
// agent/squad 表的 id -> name 映射，用于 GET issues 时填充 assignee.label
// ★5 修订（drizzle 0.33）：findFirst() 返回 query builder，需 .sync() 触发执行
export function resolveAssigneeLabel(
  type: 'member' | 'agent' | 'squad' | null,
  id: string | null,
): string | null {
  if (!type || !id) return null;
  if (type === 'member') {
    const u = db.query.users.findFirst({ where: (t, { eq }) => eq(t.id, id) }).sync();
    return u?.name ?? '未知成员';
  }
  if (type === 'agent') {
    const a = db.query.agents.findFirst({ where: (t, { eq }) => eq(t.id, id) }).sync();
    return a?.name ?? '未知智能体';
  }
  // squad
  const s = db.query.squads.findFirst({ where: (t, { eq }) => eq(t.id, id) }).sync();
  return s?.name ?? '未知小队';
}
```

- [ ] **Step 2: 写 `app/packages/server/src/db/migrate.ts`（drizzle-kit 生成 SQL 的执行器）**

```typescript
import { migrate } from 'drizzle-orm/better-sqlite3/migrator';
import { db, sqlite } from './client.js';
import { existsSync } from 'node:fs';

// drizzle-kit generate 产出 ./drizzle/*.sql，此脚本执行它们
const migrationsFolder = new URL('../../drizzle', import.meta.url).pathname;
if (!existsSync(migrationsFolder)) {
  console.error(`迁移目录不存在: ${migrationsFolder}，请先跑 pnpm --filter @ma/server db:generate`);
  process.exit(1);
}

migrate(db, { migrationsFolder });
sqlite.close();
console.log('✓ 迁移完成');
```

- [ ] **Step 3: 写 `app/packages/server/src/db/seed.ts`（照 spec §3.4）**

```typescript
import { db } from './client.js';
import { workspaces, users, agents, squads, skills, issues } from './schema.js';

// spec §3.4 position 策略（R3）：seed 全设 position=0，依赖 created_at DESC 兜底排序
// 插入顺序 = 期望显示顺序（同一 status 的 issue 后插入排后面）

const NOW = Date.now();
const WS_ID = 'ws-local';
const USER_ID = 'user-linyuan';

// 基础数据（照 seed.js）
db.insert(workspaces)
  .values({ id: WS_ID, name: '毕设 Multi-Agent', description: '本地单用户编排控制台', createdAt: NOW })
  .run();

db.insert(users)
  .values({ id: USER_ID, name: '林远', email: 'linyuan@example.com', createdAt: NOW })
  .run();

db.insert(agents)
  .values([
    { id: 'agt-lead', name: '产品·策划队长', category: '产品', createdAt: NOW },
    { id: 'agt-research', name: '产品·调研与洞察官', category: '产品', createdAt: NOW },
    { id: 'agt-prd', name: '产品·需求与PRD官', category: '产品', createdAt: NOW },
    { id: 'agt-proto', name: '产品·设计·原型官', category: '产品', createdAt: NOW },
  ])
  .run();

db.insert(squads)
  .values([
    { id: 'sqd-product', name: '产品小队', leaderId: 'agt-lead', createdAt: NOW },
    { id: 'sqd-philosophy', name: '哲学与人文研究小队', leaderId: 'agt-lead', createdAt: NOW },
    { id: 'sqd-eco', name: '生态研究团队', leaderId: 'agt-prd', createdAt: NOW },
  ])
  .run();

db.insert(skills)
  .values([
    { id: 'skl-squads', name: 'multica-squads', url: 'https://github.com/example/multica-squads', createdAt: NOW },
    { id: 'skl-prd', name: 'to-prd', url: 'https://github.com/example/to-prd', createdAt: NOW },
    { id: 'skl-research', name: 'extract-prototype-requirements', url: 'https://github.com/example/extract-prototype-requirements', createdAt: NOW },
    { id: 'skl-frontend', name: 'frontend-design', url: 'https://github.com/example/frontend-design', createdAt: NOW },
    { id: 'skl-design', name: 'design-system', url: 'https://github.com/example/design-system', createdAt: NOW },
  ])
  .run();

// seed issues（照 spec §3.4 表，position 全 0，createdAt 递增保证列内顺序）
// 注意：seed.js 的 planning 在此映射成 backlog（spec D1）
let seq = 0;
const ts = () => NOW + seq++; // 递增 timestamp

const seedIssues = [
  { identifier: 'FRI-11', title: '毕设 multi-agent：产出 PRD 与可交互原型', status: 'in_review', priority: 'high', assigneeType: 'squad', assigneeId: 'sqd-product', description: '读取 D:\\code\\multi-agent 调研资料，在 chanpin 目录交付 PRD、RTM 与可交互原型。' },
  { identifier: 'FRI-10', title: '竞品矩阵与 Multica 功能对标', status: 'done', priority: 'medium', assigneeType: 'agent', assigneeId: 'agt-research', description: '5 家竞品 + 2×2 定位图，Must 对齐 brief。' },
  { identifier: 'FRI-09', title: 'PRD 真源与 RTM 32 条 Must REQ', status: 'in_progress', priority: 'high', assigneeType: 'agent', assigneeId: 'agt-prd', description: 'docs/prd/multi-agent-platform.md + RTM + handoff。' },
  { identifier: 'FRI-08', title: 'Wiki 架构占位与 llm-wiki-pattern 对齐', status: 'backlog', priority: 'medium', assigneeType: null, assigneeId: null, description: '5 页 mock：Home / Architecture / Synthesis / Sprint Log / Glossary。' },
  { identifier: 'FRI-07', title: 'Agent runtime 适配调研（Pi / Claude / opencode）', status: 'todo', priority: 'low', assigneeType: 'agent', assigneeId: 'agt-lead', description: 'Cursor 仅 UI mock，Phase 1 实装 TBD。' },
  { identifier: 'FRI-06', title: 'Skill URL 导入 UX 走查', status: 'done', priority: 'low', assigneeType: 'agent', assigneeId: 'agt-proto', description: 'GitHub URL 导入 + Agent 分配 checkbox。' },
  { identifier: 'FRI-05', title: '答辩 Demo Script 排练（≤10 min）', status: 'todo', priority: 'high', assigneeType: 'squad', assigneeId: 'sqd-product', description: '5 步 demo + 差异化一句话。' },
  { identifier: 'FRI-04', title: 'Memory 检索面板 mock（Should）', status: 'backlog', priority: 'low', assigneeType: null, assigneeId: null, description: 'Phase 2+ 可插拔 Memory，MVP 仅占位。' },
];

for (const iss of seedIssues) {
  db.insert(issues)
    .values({
      id: crypto.randomUUID(),
      workspaceId: WS_ID,
      identifier: iss.identifier,
      title: iss.title,
      description: iss.description,
      status: iss.status,
      priority: iss.priority,
      assigneeType: iss.assigneeType,
      assigneeId: iss.assigneeId,
      creatorType: 'member',
      creatorId: USER_ID,
      position: 0, // R3：全 0，依赖 createdAt DESC
      createdAt: ts(),
      updatedAt: ts(),
    })
    .run();
}

console.log(`✓ seed 完成：${seedIssues.length} 条 issue`);
```

> 注意：seed 的 id 用 `crypto.randomUUID()`（Node 24 全局有）。identifier 是固定的 FRI-xx，但 PK id 是随机 UUID。

- [ ] **Step 4: 跑 generate + migrate + seed 验证**

```bash
cd app
pnpm --filter @ma/server db:generate    # 生成 drizzle/*.sql
pnpm --filter @ma/server db:migrate      # 建表
pnpm --filter @ma/server db:seed         # 插 seed
```

预期：三个都成功，最后输出"✓ seed 完成：8 条 issue"。

- [ ] **Step 5: 验证 DB 内容（sqlite 查询）**

```bash
cd app/packages/server
node -e "const Database = require('better-sqlite3'); const db = new Database('./dev.db'); console.log(db.prepare('SELECT identifier, status, assignee_type FROM issue ORDER BY created_at').all());"
```

预期：8 条记录，FRI-11 是 in_review。

- [ ] **Step 6: 提交**

```bash
git add app/packages/server/src/db app/packages/server/drizzle
git commit -m "feat(s01): DB client + seed（8 条 FRI issue，含 label map）"
```

---

### Task 2.3: EventBus + WsBroadcaster

**Files:**
- Create: `app/packages/server/src/orchestration/event-bus.ts`
- Create: `app/packages/server/src/orchestration/ws-broadcaster.ts`

**Interfaces:**
- Consumes: `@ma/shared` 的 `DomainEvent`
- Produces: `EventBus`（`on`/`publish`）、`WsBroadcaster`（`add`/`remove`/`broadcast`）

**必读：** spec §6.3、§6.4。

- [ ] **Step 1: 写 `app/packages/server/src/orchestration/event-bus.ts`（spec §6.3）**

```typescript
import type { DomainEvent } from '@ma/shared';

type Listener = (e: DomainEvent) => void;

// 同步 in-process 事件总线（学 multica events.Bus）
// 同步派发保证事件顺序；错误隔离不中断后续监听器
export class EventBus {
  private listeners = new Set<Listener>();

  on(fn: Listener): void {
    this.listeners.add(fn);
  }

  off(fn: Listener): void {
    this.listeners.delete(fn);
  }

  publish(e: DomainEvent): void {
    for (const fn of this.listeners) {
      try {
        fn(e);
      } catch (err) {
        console.error('[event-bus] 监听器异常:', err);
      }
    }
  }
}

export const eventBus = new EventBus();
```

- [ ] **Step 2: 写 `app/packages/server/src/orchestration/ws-broadcaster.ts`（spec §6.4，R 修 OPEN 常量）**

```typescript
import type { WebSocket } from 'ws';
import type { DomainEvent } from '@ma/shared';

// spec §6.4：单 workspace，一个 set 简化
// spec §6.6：广播给所有人（含发起者）—— 发起窗口靠 setQueryData 幂等处理（spec §7.5 R4）
const OPEN = 1; // ws.readyState 的 OPEN 常量值

export class WsBroadcaster {
  private conns = new Set<WebSocket>();

  add(ws: WebSocket): void {
    this.conns.add(ws);
  }

  remove(ws: WebSocket): void {
    this.conns.delete(ws);
  }

  broadcast(e: DomainEvent): void {
    const msg = JSON.stringify(e);
    for (const ws of this.conns) {
      if (ws.readyState === OPEN) {
        ws.send(msg);
      }
    }
  }
}

export const wsBroadcaster = new WsBroadcaster();
```

- [ ] **Step 3: typecheck + 提交**

```bash
cd app
pnpm --filter @ma/server typecheck
cd D:/code/multi-agent
git add app/packages/server/src/orchestration
git commit -m "feat(s01): EventBus + WsBroadcaster（同步总线，无 Redis）"
```

---

### Task 2.4: issue reshape 工具 + routes/issues

**Files:**
- Create: `app/packages/server/src/routes/issues.ts`
- Create: `app/packages/server/src/db/reshape.ts`

**Interfaces:**
- Consumes: `db`, `resolveAssigneeLabel`, `eventBus`, `@ma/shared` schema
- Produces: Fastify 插件 `issueRoutes`，注册 `GET/POST /api/issues`、`PUT /api/issues/:id`

**必读：** spec §5（全部 API）、§4.3/§4.4（Issue 与 Input 形态）、§5.2（identifier 生成）、§5.3（PUT 不加守卫，D2）。

- [ ] **Step 1: 写 `app/packages/server/src/db/reshape.ts`（DB 行 → API Issue，含 label 填充）**

```typescript
import type { Issue, Assignee } from '@ma/shared';
import type { issues } from '../db/schema.js';
import { resolveAssigneeLabel } from './client.js';

type IssueRow = typeof issues.$inferSelect;

// DB 扁平行 → API 嵌套 Issue（spec §3.3 + §4.2 R2 label）
export function toIssue(row: IssueRow): Issue {
  let assignee: Assignee = null;
  if (row.assigneeType && row.assigneeId) {
    const label = resolveAssigneeLabel(row.assigneeType, row.assigneeId);
    assignee = { type: row.assigneeType, id: row.assigneeId, label: label ?? '未知' };
  }
  return {
    id: row.id,
    workspaceId: row.workspaceId,
    identifier: row.identifier,
    title: row.title,
    description: row.description,
    status: row.status,
    priority: row.priority,
    assignee,
    creatorType: row.creatorType,
    creatorId: row.creatorId,
    position: row.position,
    createdAt: new Date(row.createdAt).toISOString(),
    updatedAt: new Date(row.updatedAt).toISOString(),
  };
}
```

- [ ] **Step 2: 写 `app/packages/server/src/routes/issues.ts`（spec §5）**

```typescript
import type { FastifyInstance } from 'fastify';
import { eq, sql, and } from 'drizzle-orm';
import { CreateIssueInput, UpdateIssueInput, validateUpdateIssue } from '@ma/shared';
import { db } from '../db/client.js';
import { issues } from '../db/schema.js';
import { toIssue } from '../db/reshape.js';
import { eventBus } from '../orchestration/event-bus.js';

const WS_ID = 'ws-local';
const USER_ID = 'user-linyuan';

export async function issueRoutes(app: FastifyInstance): Promise<void> {
  // GET /api/issues —— spec §5.1，扁平数组，按 position ASC, created_at DESC
  app.get('/api/issues', async () => {
    const rows = db
      .select()
      .from(issues)
      .where(eq(issues.workspaceId, WS_ID))
      .orderBy(issues.position, sql`created_at DESC`)
      .all();
    return rows.map(toIssue);
  });

  // POST /api/issues —— spec §5.2
  app.post('/api/issues', async (req, reply) => {
    const parsed = CreateIssueInput.safeParse(req.body);
    if (!parsed.success) {
      return reply.status(400).send({ error: parsed.error.flatten() });
    }
    const input = parsed.data;
    const now = Date.now();

    // identifier 生成：MAX(SUBSTR(identifier,5))+1
    // ★9 修订（off-by-one）：SQL SUBSTR 是 1-based，FRI-11 的数字从第 5 字符开始（F=1,R=2,I=3,-=4,1=5）
    const maxRow = db
      .select({ maxNum: sql<number>`MAX(CAST(SUBSTR(${issues.identifier}, 5) AS INTEGER))` })
      .from(issues)
      .where(eq(issues.workspaceId, WS_ID))
      .get();
    const nextNum = (maxRow?.maxNum ?? 0) + 1;
    const identifier = `FRI-${nextNum}`;

    // position 浮顶：MIN(position)-1（spec §5.2 + R3）
    const minRow = db
      .select({ minPos: sql<number>`COALESCE(MIN(${issues.position}), 0) - 1` })
      .from(issues)
      .where(and(eq(issues.workspaceId, WS_ID), eq(issues.status, 'backlog')))
      .get();
    const position = minRow?.minPos ?? -1;

    const id = crypto.randomUUID();
    db.insert(issues)
      .values({
        id,
        workspaceId: WS_ID,
        identifier,
        title: input.title,
        description: input.description ?? null,
        status: 'backlog',
        priority: input.priority,
        assigneeType: input.assignee?.type ?? null,
        assigneeId: input.assignee?.id ?? null,
        creatorType: 'member',
        creatorId: USER_ID,
        position,
        createdAt: now,
        updatedAt: now,
      })
      .run();

    const row = db.select().from(issues).where(eq(issues.id, id)).get();
    const issue = toIssue(row!);
    eventBus.publish({ type: 'issue:created', issue });
    return reply.status(201).send(issue);
  });

  // PUT /api/issues/:id —— spec §5.3，不加条件守卫（D2）
  app.put('/api/issues/:id', async (req, reply) => {
    const { id } = req.params as { id: string };
    const parsed = UpdateIssueInput.safeParse(req.body);
    if (!parsed.success) {
      return reply.status(400).send({ error: parsed.error.flatten() });
    }
    const input = parsed.data;
    if (!validateUpdateIssue(input)) {
      return reply.status(400).send({ error: '至少传一个字段' });
    }

    const prev = db.select().from(issues).where(eq(issues.id, id)).get();
    if (!prev) {
      return reply.status(404).send({ error: 'issue 不存在' });
    }

    // 动态构造 SET（只更新传入的字段）
    const updates: Partial<typeof issues.$inferInsert> = { updatedAt: Date.now() };
    if (input.title !== undefined) updates.title = input.title;
    if (input.description !== undefined) updates.description = input.description;
    if (input.status !== undefined) updates.status = input.status;
    if (input.priority !== undefined) updates.priority = input.priority;
    if (input.position !== undefined) updates.position = input.position;

    db.update(issues).set(updates).where(eq(issues.id, id)).run();

    const row = db.select().from(issues).where(eq(issues.id, id)).get();
    const issue = toIssue(row!);
    const statusChanged = input.status !== undefined && input.status !== prev.status;
    eventBus.publish({
      type: 'issue:updated',
      issue,
      statusChanged,
      prevStatus: statusChanged ? prev.status : null,
    });
    return reply.send(issue);
  });
}
```

- [ ] **Step 3: typecheck + 提交**

```bash
cd app
pnpm --filter @ma/server typecheck
cd D:/code/multi-agent
git add app/packages/server/src/routes app/packages/server/src/db/reshape.ts
git commit -m "feat(s01): issue routes（GET/POST/PUT + identifier 生成 + label 填充）"
```

---

### Task 2.5: WS route + app 组装 + 启动

**Files:**
- Create: `app/packages/server/src/routes/ws.ts`
- Create: `app/packages/server/src/app.ts`
- Create: `app/packages/server/src/index.ts`

**Interfaces:**
- Produces: 运行在 :3001 的 server，含 REST + WS

**必读：** spec §6.1、§6.5（接线）。

- [ ] **Step 1: 写 `app/packages/server/src/routes/ws.ts`（spec §6）**

```typescript
import type { FastifyInstance } from 'fastify';
import { wsBroadcaster } from '../orchestration/ws-broadcaster.js';

export async function wsRoutes(app: FastifyInstance): Promise<void> {
  // spec §6.1：无鉴权、无房间协议，连上即收全量 issue 事件
  app.get('/ws', { websocket: true }, (socket) => {
    wsBroadcaster.add(socket);
    socket.on('close', () => {
      wsBroadcaster.remove(socket);
    });
  });
}
```

- [ ] **Step 2: 写 `app/packages/server/src/app.ts`（Fastify 组装 + 接线）**

```typescript
import Fastify from 'fastify';
import cors from '@fastify/cors';
import websocket from '@fastify/websocket';
import { issueRoutes } from './routes/issues.js';
import { wsRoutes } from './routes/ws.js';
import { eventBus } from './orchestration/event-bus.js';
import { wsBroadcaster } from './orchestration/ws-broadcaster.js';

export async function buildApp() {
  const app = Fastify({ logger: true });

  await app.register(cors, {
    origin: ['http://localhost:3000'],
  });
  await app.register(websocket);

  // 接线（spec §6.5）：eventBus → wsBroadcaster
  eventBus.on((e) => wsBroadcaster.broadcast(e));

  await app.register(issueRoutes);
  await app.register(wsRoutes);

  return app;
}
```

- [ ] **Step 3: 写 `app/packages/server/src/index.ts`**

```typescript
import { buildApp } from './app.js';

const PORT = Number(process.env.PORT ?? 3001);

async function main() {
  const app = await buildApp();
  try {
    await app.listen({ port: PORT, host: '0.0.0.0' });
    console.log(`✓ server 起在 http://localhost:${PORT} (ws: /ws)`);
  } catch (err) {
    app.log.error(err);
    process.exit(1);
  }
}

main();
```

- [ ] **Step 4: 端到端自测（启动 + curl + WS）**

```bash
# 终端 1：起 server
cd app
pnpm --filter @ma/server dev

# 终端 2：测 GET
curl http://localhost:3001/api/issues
# 预期：8 条 issue 的 JSON 数组，FRI-11 的 assignee 含 label "产品小队"

# 测 POST
curl -X POST http://localhost:3001/api/issues -H "Content-Type: application/json" -d '{"title":"测试新建"}'
# 预期：返回 identifier=FRI-12，status=backlog，position=-1

# 测 PUT（改 status）
curl -X PUT http://localhost:3001/api/issues/<上面的id> -H "Content-Type: application/json" -d '{"status":"todo"}'
# 预期：返回更新后的 issue，status=todo

# 测 WS（用 node 脚本或 wscat）
node -e "const WebSocket=require('ws'); const ws=new WebSocket('ws://localhost:3001/ws'); ws.on('message',d=>console.log('WS收到:',d.toString())); setTimeout(()=>{fetch('http://localhost:3001/api/issues',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:'WS测试'})});},500);"
# 预期：WS 连上后 POST 触发，收到 issue:created 事件
```

- [ ] **Step 5: 提交**

```bash
git add app/packages/server/src/routes/ws.ts app/packages/server/src/app.ts app/packages/server/src/index.ts
git commit -m "feat(s01): WS route + Fastify 组装 + :3001 启动"
```

---

### Task 2.6: 写 handoff

**Files:**
- Create: `app/.progress/s01-impl-2.md`

- [ ] **Step 1: 跑全量 typecheck**

```bash
cd app
pnpm -r typecheck
```

- [ ] **Step 2: 写 handoff `app/.progress/s01-impl-2.md`**

照模板填，**给 impl-3 的注意点必须写清 API + WS 契约**：
- **API：**
  - `GET /api/issues` → `Issue[]`（扁平，前端按 status 分组）
  - `POST /api/issues` body `{title, description?, priority?, assignee?:{type,id}|null}` → `Issue`（201）
  - `PUT /api/issues/:id` body `{title?, description?, status?, priority?, position?}` → `Issue`（200）。至少一个字段，否则 400
  - issue 形态：`{id, workspaceId, identifier, title, description, status, priority, assignee:{type,id,label}|null, creatorType, creatorId, position, createdAt, updatedAt}`
- **WS：** `ws://localhost:3001/ws`，收到 `issue:created` / `issue:updated`（含 `statusChanged`, `prevStatus`）。**广播含发起者**，前端处理要幂等（spec §7.5 R4）
- **端口：** server 3001，web 3000
- **CORS：** 已配允许 localhost:3000
- **自测结果：** 贴 curl 输出 + WS 测试输出
- **偏离：** 记录 drizzle-kit 版本、better-sqlite3 原生编译等实际问题

- [ ] **Step 3: 提交**

```bash
git add app/.progress/s01-impl-2.md
git commit -m "docs(s01): impl-2 handoff（server 全栈完成）"
```

---

# 执行者片段 C（impl-3）：web 全栈 + 验收

> **参考边界：** 建议的前端执行者片段，负责收尾验收。实际范围由计划者在前序 handoff 后确定。
> **会话边界：** 开新会话，读 AGENTS.md + spec §7/§8 + impl-2 handoff（API/WS 契约）+ impl-1 handoff + 本 Task 组 + 计划者 handoff。在 `feat/s01-kanban-ws` 分支继续。完成后写 `app/.progress/s01-impl-3.md` 并**跑通双窗口验收**。

### Task 3.1: web 包脚手架 + 设计 token

**Files:**
- Create: `app/packages/web/package.json`
- Create: `app/packages/web/tsconfig.json`
- Create: `app/packages/web/next.config.mjs`
- Create: `app/packages/web/app/globals.css`
- Create: `app/packages/web/app/layout.tsx`

**Interfaces:**
- Consumes: `@ma/shared` 的 `Issue`, `IssueStatus`, `CreateIssueInput`, `UpdateIssueInput`, `DomainEvent`

**必读：** spec §7（前端）、§8.1（复刻清单）、prototype `chanpin/prototype/assets/css/tokens.css`（全量移植）。

- [ ] **Step 1: 写 `app/packages/web/package.json`**

```json
{
  "name": "@ma/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ma/shared": "workspace:*",
    "@tanstack/react-query": "^5.50.0",
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.0"
  }
}
```

- [ ] **Step 2: 写 `app/packages/web/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "noEmit": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]
}
```

- [ ] **Step 3: 写 `app/packages/web/next.config.mjs`**

```javascript
/** @type {import('next').NextConfig} */
export default {
  transpilePackages: ['@ma/shared'],
};
```

- [ ] **Step 4: 写 `app/packages/web/app/globals.css`（移植 tokens.css + 补 --status-blocked）**

从 `chanpin/prototype/assets/css/tokens.css` 全量复制到 `:root`，并补：

```css
:root {
  /* ... tokens.css 全部内容 ... */
  /* spec §8.1：prototype 缺 blocked 状态色，补 */
  --status-blocked: var(--color-orange);  /* #f97316 */
  --status-cancelled: var(--color-text-dim); /* 不渲染列，仅 token 预留 */
}

body {
  margin: 0;
  background: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-ui);
  font-size: var(--text-base);
}
```

> 执行者注意：完整 token 值见 prototype `tokens.css`，逐字复制。不要"简化"或"近似"。

- [ ] **Step 5: 写 `app/packages/web/app/layout.tsx`**

```tsx
import './globals.css';
import type { Metadata } from 'next';
import { Providers } from '@/lib/providers';

export const metadata: Metadata = {
  title: '毕设 Multi-Agent',
  description: '本地多智能体编排控制台',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN">
      <body>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
```

- [ ] **Step 6: 安装 + typecheck + 提交**

```bash
cd app
pnpm install
pnpm --filter @ma/web typecheck
cd D:/code/multi-agent
git add app/packages/web app/pnpm-lock.yaml
git commit -m "feat(s01): web 脚手架 + 设计 token 移植（含 --status-blocked）"
```

---

### Task 3.2: API hooks + WS store

**Files:**
- Create: `app/packages/web/lib/api.ts`
- Create: `app/packages/web/lib/ws.ts`
- Create: `app/packages/web/lib/providers.tsx`

**Interfaces:**
- Produces: `useIssues()`, `useCreateIssue()`, `useUpdateIssue()`（React Query hooks）、WS 单例 + store

**必读：** spec §7.4（状态分工）、§7.5（WS 回声幂等 R4）、impl-2 handoff（API/WS 契约）。

- [ ] **Step 1: 写 `app/packages/web/lib/api.ts`（React Query hooks）**

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import type { Issue, CreateIssueInput, UpdateIssueInput } from '@ma/shared';

const API = 'http://localhost:3001/api/issues';

export function useIssues() {
  return useQuery<Issue[]>({
    queryKey: ['issues'],
    queryFn: async () => {
      const res = await fetch(API);
      if (!res.ok) throw new Error('加载失败');
      return res.json();
    },
  });
}

export function useCreateIssue() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async (input: CreateIssueInput) => {
      const res = await fetch(API, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(input),
      });
      if (!res.ok) throw new Error('创建失败');
      return res.json() as Promise<Issue>;
    },
    // 不做乐观更新——POST 响应 + WS issue:created 会处理（避免重复）
    onSuccess: () => qc.invalidateQueries({ queryKey: ['issues'] }),
  });
}

export function useUpdateIssue() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async ({ id, input }: { id: string; input: UpdateIssueInput }) => {
      const res = await fetch(`${API}/${id}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(input),
      });
      if (!res.ok) throw new Error('更新失败');
      return res.json() as Promise<Issue>;
    },
    // 拖拽乐观更新在组件层用 onMutate 处理（见 KanbanBoard）
    onSuccess: () => qc.invalidateQueries({ queryKey: ['issues'] }),
  });
}
```

- [ ] **Step 2: 写 `app/packages/web/lib/ws.ts`（WS 单例 + 事件分发）**

```typescript
import { useEffect } from 'react';
import { useQueryClient } from '@tanstack/react-query';
import { create } from 'zustand';
import type { Issue, DomainEvent } from '@ma/shared';

// spec §7.4：Zustand 管 WS 连接状态
interface WsState {
  status: 'connecting' | 'open' | 'closed';
  setStatus: (s: WsState['status']) => void;
}

export const useWsStore = create<WsState>((set) => ({
  status: 'connecting',
  setStatus: (s) => set({ status: s }),
}));

// spec §6.6 + §7.5 R4：WS 广播含发起者，事件处理必须幂等
// setQueryData 天然幂等（写相同值不触发重渲染）
export function useWsEvents() {
  const qc = useQueryClient();
  const setStatus = useWsStore((s) => s.setStatus);

  useEffect(() => {
    const ws = new WebSocket('ws://localhost:3001/ws');
    let mounted = true;

    ws.onopen = () => mounted && setStatus('open');
    ws.onclose = () => mounted && setStatus('closed');

    ws.onmessage = (ev) => {
      const event = JSON.parse(ev.data) as DomainEvent;
      // 幂等：用 issue id 更新 cache，相同状态不触发重渲染
      qc.setQueryData<Issue[]>(['issues'], (old) => {
        if (!old) return old;
        if (event.type === 'issue:created') {
          // 避免重复添加（幂等）
          if (old.some((i) => i.id === event.issue.id)) return old;
          return [...old, event.issue];
        }
        if (event.type === 'issue:updated') {
          return old.map((i) => (i.id === event.issue.id ? event.issue : i));
        }
        return old;
      });
    };

    return () => {
      mounted = false;
      ws.close();
    };
  }, [qc, setStatus]);
}
```

- [ ] **Step 3: 写 `app/packages/web/lib/providers.tsx`**

```tsx
'use client';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { useState, type ReactNode } from 'react';
import { useWsEvents } from './ws';

function WsMount() {
  useWsEvents(); // 挂载时建立 WS 连接 + 监听
  return null;
}

export function Providers({ children }: { children: ReactNode }) {
  const [qc] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: { refetchOnWindowFocus: false, staleTime: Infinity },
        },
      }),
  );
  return (
    <QueryClientProvider client={qc}>
      <WsMount />
      {children}
    </QueryClientProvider>
  );
}
```

- [ ] **Step 4: typecheck + 提交**

```bash
cd app
pnpm --filter @ma/web typecheck
cd D:/code/multi-agent
git add app/packages/web/lib app/packages/web/app/layout.tsx
git commit -m "feat(s01): React Query hooks + WS 单例（幂等事件处理）"
```

---

### Task 3.3: 六列看板组件 + 拖拽

**Files:**
- Create: `app/packages/web/components/KanbanBoard.tsx`
- Create: `app/packages/web/components/KanbanColumn.tsx`
- Create: `app/packages/web/components/IssueCard.tsx`
- Create: `app/packages/web/components/NewIssueForm.tsx`
- Create: `app/packages/web/app/page.tsx`

**Interfaces:**
- Consumes: `useIssues`, `useUpdateIssue`, `useCreateIssue`, `@ma/shared` 类型

**必读：** spec §7.2（六列结构）、§7.3（拖拽 + 新建）、§7.5（cancelled 过滤 R5）、§8.1（prototype 复刻 `bindKanbanDrag`）、prototype `app.js:945-981`（拖拽实现）。

- [ ] **Step 1: 写 `app/packages/web/components/IssueCard.tsx`**

```tsx
import type { Issue, IssueStatus } from '@ma/shared';

const STATUS_COLORS: Record<IssueStatus, string> = {
  backlog: 'var(--status-backlog)',
  todo: 'var(--status-todo)',
  in_progress: 'var(--status-in-progress)',
  in_review: 'var(--status-in-review)',
  done: 'var(--status-done)',
  blocked: 'var(--status-blocked)',
  cancelled: 'var(--status-cancelled)',
};

const PRIORITY_LABEL: Record<string, string> = {
  urgent: '🔴 紧急',
  high: '🟠 高',
  medium: '🟡 中',
  low: '🔵 低',
  none: '',
};

interface Props {
  issue: Issue;
  onDragStart: (id: string) => void;
}

export function IssueCard({ issue, onDragStart }: Props) {
  return (
    <article
      draggable
      onDragStart={() => onDragStart(issue.id)}
      style={{
        background: 'var(--bg-elevated)',
        border: '1px solid var(--border)',
        borderRadius: 'var(--radius-md)',
        padding: 'var(--space-3)',
        cursor: 'grab',
      }}
    >
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 'var(--space-1)' }}>
        <span style={{ color: STATUS_COLORS[issue.status], fontFamily: 'var(--font-mono)', fontSize: 'var(--text-xs)' }}>
          {issue.identifier}
        </span>
        {issue.priority !== 'none' && (
          <span style={{ fontSize: 'var(--text-xs)' }}>{PRIORITY_LABEL[issue.priority]}</span>
        )}
      </div>
      <div style={{ fontSize: 'var(--text-sm)', marginBottom: 'var(--space-2)' }}>{issue.title}</div>
      <div style={{ color: 'var(--text-muted)', fontSize: 'var(--text-xs)' }}>
        {issue.assignee ? `▸${issue.assignee.label}` : '▸未指派'}
      </div>
    </article>
  );
}
```

- [ ] **Step 2: 写 `app/packages/web/components/KanbanColumn.tsx`**

```tsx
import type { Issue, IssueStatus } from '@ma/shared';
import { IssueCard } from './IssueCard';

interface Props {
  title: string;
  status: IssueStatus;
  color: string;
  issues: Issue[];
  onDragStart: (id: string) => void;
  onDrop: (status: IssueStatus) => void;
}

export function KanbanColumn({ title, color, issues, onDragStart, onDrop, status }: Props) {
  return (
    <section
      onDragOver={(e) => e.preventDefault()}
      onDrop={(e) => {
        e.preventDefault();
        onDrop(status);
      }}
      style={{
        background: 'var(--bg-secondary)',
        border: '1px solid var(--border)',
        borderRadius: 'var(--radius-md)',
        width: '260px',
        minHeight: '400px',
        padding: 'var(--space-3)',
        display: 'flex',
        flexDirection: 'column',
        gap: 'var(--space-2)',
      }}
    >
      <header style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-2)', marginBottom: 'var(--space-2)' }}>
        <span style={{ width: 8, height: 8, borderRadius: '50%', background: color }} />
        <strong>{title}</strong>
        <span style={{ color: 'var(--text-muted)', fontSize: 'var(--text-xs)' }}>{issues.length}</span>
      </header>
      {issues.map((iss) => (
        <IssueCard key={iss.id} issue={iss} onDragStart={onDragStart} />
      ))}
    </section>
  );
}
```

- [ ] **Step 3: 写 `app/packages/web/components/NewIssueForm.tsx`**

```tsx
'use client';
import { useState } from 'react';
import type { Priority, AssigneeType } from '@ma/shared';
import { useCreateIssue } from '@/lib/api';

// spec D7：内联表单（非模态）
export function NewIssueForm() {
  const [open, setOpen] = useState(false);
  const [title, setTitle] = useState('');
  const [priority, setPriority] = useState<Priority>('none');
  const create = useCreateIssue();

  function submit(e: React.FormEvent) {
    e.preventDefault();
    if (!title.trim()) return;
    create.mutate({ title, priority, assignee: null });
    setTitle('');
    setPriority('none');
    setOpen(false);
  }

  if (!open) {
    return (
      <button onClick={() => setOpen(true)} style={{ padding: 'var(--space-2) var(--space-4)', cursor: 'pointer' }}>
        + 新建 Issue
      </button>
    );
  }

  return (
    <form onSubmit={submit} style={{ display: 'flex', gap: 'var(--space-2)', alignItems: 'center' }}>
      <input
        value={title}
        onChange={(e) => setTitle(e.target.value)}
        placeholder="标题"
        autoFocus
        style={{ padding: 'var(--space-2)', background: 'var(--bg-elevated)', border: '1px solid var(--border)', color: 'var(--text-primary)' }}
      />
      <select value={priority} onChange={(e) => setPriority(e.target.value as Priority)}>
        <option value="none">无</option>
        <option value="low">低</option>
        <option value="medium">中</option>
        <option value="high">高</option>
        <option value="urgent">紧急</option>
      </select>
      <button type="submit" disabled={create.isPending}>提交</button>
      <button type="button" onClick={() => setOpen(false)}>取消</button>
    </form>
  );
}
```

- [ ] **Step 4: 写 `app/packages/web/components/KanbanBoard.tsx`（拖拽 + cancelled 过滤）**

```tsx
'use client';
import { useState } from 'react';
import type { Issue, IssueStatus } from '@ma/shared';
import { useIssues, useUpdateIssue } from '@/lib/api';
import { KanbanColumn } from './KanbanColumn';
import { NewIssueForm } from './NewIssueForm';

// spec §7.2：6 列，cancelled 不建列
const COLUMNS: { title: string; status: IssueStatus; color: string }[] = [
  { title: 'Backlog', status: 'backlog', color: 'var(--status-backlog)' },
  { title: 'Todo', status: 'todo', color: 'var(--status-todo)' },
  { title: 'In Progress', status: 'in_progress', color: 'var(--status-in-progress)' },
  { title: 'In Review', status: 'in_review', color: 'var(--status-in-review)' },
  { title: 'Done', status: 'done', color: 'var(--status-done)' },
  { title: 'Blocked', status: 'blocked', color: 'var(--status-blocked)' },
];

export function KanbanBoard() {
  const { data: issues, isLoading } = useIssues();
  const update = useUpdateIssue();
  const [dragId, setDragId] = useState<string | null>(null);

  if (isLoading) return <div>加载中…</div>;

  // spec §7.5 R5：cancelled 的 issue 不渲染到任何列
  const visible = (issues ?? []).filter((i) => i.status !== 'cancelled');

  function handleDrop(targetStatus: IssueStatus) {
    if (!dragId) return;
    const dragged = visible.find((i) => i.id === dragId);
    if (!dragged || dragged.status === targetStatus) {
      setDragId(null);
      return;
    }
    // spec §7.3 ①：乐观更新 + PUT（只传 status，不传 position，D4）
    update.mutate({ id: dragId, input: { status: targetStatus } });
    setDragId(null);
  }

  return (
    <div style={{ padding: 'var(--space-4)' }}>
      <div style={{ marginBottom: 'var(--space-4)' }}>
        <NewIssueForm />
      </div>
      <div style={{ display: 'flex', gap: 'var(--space-3)', overflowX: 'auto' }}>
        {COLUMNS.map((col) => (
          <KanbanColumn
            key={col.status}
            title={col.title}
            status={col.status}
            color={col.color}
            issues={visible.filter((i) => i.status === col.status)}
            onDragStart={setDragId}
            onDrop={handleDrop}
          />
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 5: 写 `app/packages/web/app/page.tsx`**

```tsx
import { KanbanBoard } from '@/components/KanbanBoard';

export default function Home() {
  return <KanbanBoard />;
}
```

- [ ] **Step 6: typecheck + 启动验证**

```bash
cd app
pnpm --filter @ma/web typecheck
pnpm dev   # 并起 server + web
```

浏览器开 `http://localhost:3000`，预期：六列看板，FRI-11 在 In Review，Blocked 列为空但列头橙色。

- [ ] **Step 7: 提交**

```bash
cd D:/code/multi-agent
git add app/packages/web/components app/packages/web/app/page.tsx
git commit -m "feat(s01): 六列看板 + 拖拽 + 新建（cancelled 过滤）"
```

---

### Task 3.4: 跑通 S01 验收

**必读：** spec §9（验收标准全部）。

- [ ] **Step 1: 工程门槛**

```bash
cd app
pnpm install
pnpm -r typecheck   # 预期全绿
pnpm dev             # 预期 server:3001 + web:3000 都起
```

- [ ] **Step 2: 功能验收（单窗口）**

打开 `http://localhost:3000`：
- [ ] 六列看板，数据来自 SQLite（非 mock）
- [ ] FRI-04~FRI-11 全部显示，FRI-11 在 In Review
- [ ] Backlog 有 FRI-08 + FRI-04；Done 有 FRI-06 + FRI-10
- [ ] Blocked 列存在 + 列头橙色点
- [ ] 点"+ 新建"→ 填"测试"→ 提交 → **FRI-12** 出现在 Backlog 浮顶

- [ ] **Step 3: 实时同步验收（双窗口）**

开**两个浏览器窗口**（或一个窗口 + 一个无痕窗口），都开 `http://localhost:3000`：
- [ ] 窗口 A 拖 FRI-07 从 Todo 到 In Progress → 窗口 B 实时看到移动
- [ ] 窗口 A 新建 issue → 窗口 B 实时看到新卡
- [ ] 拖拽后重启 server，状态以 DB 为准（刷新后仍是新状态）

- [ ] **Step 4: 写 handoff `app/.progress/s01-impl-3.md`**

照模板填，**自测结果必须贴实际截图描述或操作记录**：
- 完成了什么（web 全栈 + 验收）
- 自测：每条验收标准 ✅/❌ + 证据（如"双窗口拖 FRI-07，窗口 B 实时移动，截图/描述"）
- 偏离：prototype 复刻时的取舍
- 遗留：给后续切片的点

- [ ] **Step 5: 提交**

```bash
git add app/.progress/s01-impl-3.md
git commit -m "docs(s01): impl-3 handoff（web 完成 + 验收通过）"
```

---

## 计划者收尾（所有执行者完成后）

1. 读 `s01-impl-3.md`，确认验收清单全 ✅
2. 开 PR（新会话审查 diff，照 AGENTS.md）
3. 审查通过 → 人在 main 合并
4. 计划者写 `app/.progress/s01-planner-2.md`（切片总结 + 点亮 FRI-11 看板部分 + 给 S02 的注意点）

---

## 自检

**spec 覆盖：**
- §1 架构边界 → Task 1.1（monorepo）+ 2.5（server 组装）+ 3.1（web 脚手架）✓
- §2 技术选型 → 全 Task 的 package.json ✓
- §3 数据模型 → Task 2.1（schema）+ 2.2（seed）✓
- §4 shared 契约 → Task 1.2 ✓
- §5 API → Task 2.4 ✓
- §6 WebSocket → Task 2.3（bus/broadcaster）+ 2.5（ws route）✓
- §7 前端 → Task 3.2（hooks/ws）+ 3.3（组件）✓
- §8 复刻清单 → Task 3.1（token）+ 3.3（拖拽复刻）✓
- §9 验收 → Task 3.4 ✓
- R1-R5 修订 → Task 1.2（R1/R2）+ 2.2（R3）+ 3.2（R4）+ 3.3（R5）✓

**占位扫描：** 无 TBD/TODO，所有 step 有完整代码 ✓

**类型一致性：** `toIssue`, `resolveAssigneeLabel`, `validateUpdateIssue`, `useIssues/useCreateIssue/useUpdateIssue`, `useWsEvents` 跨 Task 名称一致 ✓

**Drizzle version note：** 版本号是写作时合理值，`pnpm install` 锁定后若有偏差，执行者在 handoff 记录实际版本。
