# S06 Wiki 存储 + 浏览器 + ingest 管线 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **本项目工程模式：** AGENTS.md 规定的"垂直切片 × 计划者-执行者"。计划者把 Task 按自然依赖边界切成执行者片段，每个片段由用户新开的会话执行，靠 `app/.progress/` handoff 串行交接。
> **spec 真源：** [`docs/superpowers/specs/2026-07-15-s06-wiki-design.md`](../specs/2026-07-15-s06-wiki-design.md)（自审 F1-F5 修正）
> **S05 真实代码（S06 在此之上改）：** 所有 file:line 已核对到 S04/S05 合并后版本

**Goal:** 端到端打通最薄 Wiki ingest 路径——Issue 完成（status→done）时自动读 Issue 内容 → LLM 生成 wiki 页 → 写 `wiki/` 目录 → 更新 index/log → WS 推前端 → `/wiki` 浏览器实时展示。

**Architecture:** Wiki 存储用文件系统 markdown（`wiki/` 目录，照 concepts + openwiki + llm-wiki-agent）；ingest 用 LangChain.js 直调 LLM API（双 provider：OpenAI 兼容 + Anthropic，不走执行层 CLI）；触发点在 Issue PUT handler（status→done 时 fire-and-forget 异步）；前端 `/wiki` 左列表右渲染，复用 S02 MarkdownBody。

**Tech Stack:** 同 S01-S05（TypeScript / Fastify / Drizzle / better-sqlite3 / Next.js / Zod）+ 新增 LangChain.js（`@langchain/openai` + `@langchain/anthropic` + `@langchain/core`）

## Global Constraints

（来自 spec，所有 Task 隐式遵守）

- **分支：** `feat/s06-wiki`（从 main 切出）
- **Wiki 存储位置：** `wiki/` 目录（项目根，照 spec W5）。定位用 `getWikiDir()` = `path.resolve(MA_WORKSPACE_CWD ?? process.cwd(), 'wiki')`（照 S05 scanner.ts 模式，spec §3.6）
- **slug 不含 `.md` 扩展名**：API 层和前端只打交道不含扩展名的 slug；扩展名仅在 `store.ts` 内部拼接（spec §3.7）
- **LLM 双 provider**：`WIKI_LLM_PROVIDER`（`openai` | `anthropic`，默认 `openai`）；`WIKI_LLM_API_KEY`（必需）；`WIKI_LLM_BASE_URL`（openai 格式用）；`WIKI_LLM_MODEL`（默认 `gpt-4o`）（spec §4.3）
- **ingest 异步 fire-and-forget**：不阻塞 PUT 响应；失败只 console.error + log 记录，不重试（DLQ 留 S08）（spec W7/W8）
- **触发条件**：PUT issue `status` 变成 `done` 时触发，且 `statusChanged === true`（唯一触发点，spec §4.1）
- **Wiki 页只由 ingest pipeline 写**：无 POST/PUT API；人只读不编辑（spec §6）
- **Git：** feature 分支，Conventional Commits，绝不 push main
- **不引入测试框架**：本项目 S01-S05 均无测试框架（无 vitest/jest），用 `pnpm typecheck` + 手动 `pnpm dev` 验证（与既有切片一致）

## 文件结构（S06 改动决策）

```
app/packages/
├── shared/src/schema.ts              [改] 加 WikiPage / WikiPageSummary / WikiPageCreatedEvent + DomainEvent 扩展
├── server/
│   ├── package.json                  [改] 加 @langchain/openai + @langchain/anthropic + @langchain/core
│   └── src/
│       ├── wiki/
│       │   ├── store.ts              [新] 文件系统读写：getWikiDir + list/read/write page + appendIndex/Log + saveRaw + ensureWikiDir
│       │   ├── llm.ts                [新] LangChain 双 provider 工厂：createLlm + buildIngestPrompt
│       │   └── ingest.ts             [新] ingest 管线：ingestIssue（读 issue → saveRaw → LLM → writePage → update index/log → WS）
│       ├── routes/
│       │   └── wiki.ts               [新] GET /api/wiki/pages + GET /api/wiki/pages/:slug
│       ├── routes/issues.ts          [改] PUT handler 加 status→done 触发 ingest（fire-and-forget）
│       └── app.ts                    [改] 注册 wikiRoutes + 启动时 ensureWikiDir
└── web/
    ├── app/wiki/page.tsx             [新] /wiki 路由（壳页面）
    ├── components/WikiPage.tsx       [新] 左列表 + 右渲染（复用 MarkdownBody）
    └── lib/api.ts                    [改] 加 useWikiPages + useWikiPage hooks
    └── lib/ws.ts                     [改] 加 wiki:page-created handler（invalidate wiki cache）
    └── components/Sidebar.tsx        [改] wiki 导航项加 href='/wiki'
wiki/                                 [新] 运行时生成（index.md + log.md + raw/ + *.md）
```

---

# 执行者片段 A（impl-1）：shared 契约 + Wiki 存储层 + LLM 工厂

> **参考边界：** 纯数据/工具层，无 Fastify 路由、无前端。是后续所有工作的依赖。
> **会话边界：** 开新会话，读 AGENTS.md + spec §3/§4.3/§5 + 本 Task 组。同一分支。完成后写 `app/.progress/s06-impl-1.md`。

### Task 1.1: shared 契约扩展

**Files:** Modify `app/packages/shared/src/schema.ts`

**必读：** spec §5（WikiPage / WikiPageSummary / WikiPageCreatedEvent + DomainEvent 扩展）

**Interfaces:**
- Produces: `WikiPage`（`{slug, title, content}`）、`WikiPageSummary`（`{slug, title}`）、`WikiPageCreatedEvent`（`{type: 'wiki:page-created', slug, title}`）；`DomainEvent` 联合加 `WikiPageCreatedEvent`

- [ ] **Step 1: 在 schema.ts 末尾（DomainEvent 定义之前）加三个 Wiki 契约**

在 `app/packages/shared/src/schema.ts` 中，找到 `CommentCreatedEvent` 定义块（约 line 246-250）之后，插入：

```typescript
// —— S06：Wiki 契约 ——
export const WikiPage = z.object({
  slug: z.string(),
  title: z.string(),
  content: z.string(),
});
export type WikiPage = z.infer<typeof WikiPage>;

export const WikiPageSummary = z.object({
  slug: z.string(),
  title: z.string(),
});
export type WikiPageSummary = z.infer<typeof WikiPageSummary>;
```

- [ ] **Step 2: 加 WikiPageCreatedEvent**

在上面的 WikiPageSummary 之后加：

```typescript
export const WikiPageCreatedEvent = z.object({
  type: z.literal('wiki:page-created'),
  slug: z.string(),
  title: z.string(),
});
export type WikiPageCreatedEvent = z.infer<typeof WikiPageCreatedEvent>;
```

- [ ] **Step 3: DomainEvent 联合加 WikiPageCreatedEvent**

找到 `export type DomainEvent =` 定义块（约 line 280-286），在 `| RunMessageEvent` 之后加一行：

```typescript
export type DomainEvent =
  | IssueCreatedEvent
  | IssueUpdatedEvent
  | CommentCreatedEvent
  | RunLifecycleEvent
  | RunProgressEvent
  | RunMessageEvent
  | WikiPageCreatedEvent;
```

- [ ] **Step 4: typecheck 验证**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿（shared 类型导出，无破坏）

- [ ] **Step 5: Commit**

```bash
git add app/packages/shared/src/schema.ts
git commit -m "feat(s06): shared Wiki 契约（WikiPage/WikiPageSummary/WikiPageCreatedEvent + DomainEvent 扩展）"
```

---

### Task 1.2: 安装 LangChain 依赖 + 版本 spike

**Files:** Modify `app/packages/server/package.json`

**必读：** spec §4.6（依赖）+ §10 风险表（版本号 impl-1 确认）

**Interfaces:** 无（环境准备）

- [ ] **Step 1: pnpm add LangChain 包**

Run:
```bash
cd app/packages/server
pnpm add @langchain/openai @langchain/anthropic @langchain/core
```

记下实际安装的版本号（写入 handoff）。

- [ ] **Step 2: 验证能 import**

创建临时文件 `app/packages/server/src/wiki/_spike.ts`：

```typescript
import { ChatOpenAI } from '@langchain/openai';
import { ChatAnthropic } from '@langchain/anthropic';
// 仅验证 import 不报错，不实际调用
console.log(typeof ChatOpenAI, typeof ChatAnthropic);
```

Run: `cd app/packages/server && npx tsx src/wiki/_spike.ts`
Expected: 打印 `function function`（两个都是类构造器）

- [ ] **Step 3: 删除 spike 文件**

```bash
rm app/packages/server/src/wiki/_spike.ts
```

- [ ] **Step 4: Commit**

```bash
git add app/packages/server/package.json pnpm-lock.yaml
git commit -m "chore(s06): 安装 LangChain.js 依赖（@langchain/openai + anthropic + core）"
```

---

### Task 1.3: Wiki 存储层（store.ts）

**Files:** Create `app/packages/server/src/wiki/store.ts`

**必读：** spec §3.1（目录布局）+ §3.6（getWikiDir 定位）+ §3.7（slug 不含 .md 约定）

**Interfaces:**
- Produces:
  - `getWikiDir(): string` — 返回 wiki 目录绝对路径
  - `ensureWikiDir(): void` — 启动时调，确保 wiki/ + raw/ 存在 + 初始 index.md/log.md
  - `listWikiPages(): { slug: string; title: string }[]` — 扫 wiki/*.md（排除 index.md/log.md），title 从首行 `# ` 提取
  - `readWikiPage(slug: string): { slug: string; title: string; content: string } | null` — 读 wiki/<slug>.md
  - `writeWikiPage(slug: string, content: string): void` — 写 wiki/<slug>.md
  - `appendIndex(entry: { slug: string; title: string; identifier: string }): void` — 读 index.md → 追加条目 → 重写
  - `appendLog(entry: { type: string; identifier: string; issueId: string; slug?: string; error?: string }): void` — append log.md
  - `saveRaw(issueId: string, content: string): void` — 写 wiki/raw/issue-<id>-<timestamp>.md

- [ ] **Step 1: 创建 store.ts**

Create `app/packages/server/src/wiki/store.ts`:

```typescript
// S06 Wiki 存储层（spec §3）
// 文件系统 markdown 存储：wiki/ 目录（index.md + log.md + raw/ + *.md）
// 照 concepts llm-wiki-pattern.md 的 raw/wiki 三层 + index/log 导航
import { readFileSync, writeFileSync, readdirSync, existsSync, mkdirSync, appendFileSync } from 'node:fs';
import { join, resolve } from 'node:path';

// 目录定位（spec §3.6）：照 S05 scanner.ts 的 MA_WORKSPACE_CWD 模式
export function getWikiDir(): string {
  const cwd = process.env.MA_WORKSPACE_CWD;
  return resolve(cwd && cwd.length > 0 ? cwd : process.cwd(), 'wiki');
}

// 启动时确保目录 + 初始文件存在（spec §3.7 ensureWikiDir）
export function ensureWikiDir(): void {
  const wikiDir = getWikiDir();
  if (!existsSync(wikiDir)) {
    mkdirSync(wikiDir, { recursive: true });
  }
  const rawDir = join(wikiDir, 'raw');
  if (!existsSync(rawDir)) {
    mkdirSync(rawDir, { recursive: true });
  }
  // 初始 index.md（spec §3.2）
  const indexPath = join(wikiDir, 'index.md');
  if (!existsSync(indexPath)) {
    writeFileSync(indexPath, '# Wiki Index\n\n## Pages\n', 'utf-8');
  }
  // 初始 log.md（spec §3.3）
  const logPath = join(wikiDir, 'log.md');
  if (!existsSync(logPath)) {
    writeFileSync(logPath, '# Wiki Log\n', 'utf-8');
  }
}

// 列表：扫 wiki/*.md（排除 index.md/log.md）→ [{slug, title}]（spec §3.7）
// slug 不含 .md；title 从首行 `# ` 提取，无标题行用 slug 兜底
export function listWikiPages(): { slug: string; title: string }[] {
  const wikiDir = getWikiDir();
  if (!existsSync(wikiDir)) return [];
  const entries = readdirSync(wikiDir);
  const result: { slug: string; title: string }[] = [];
  for (const entry of entries) {
    if (!entry.endsWith('.md')) continue;
    if (entry === 'index.md' || entry === 'log.md') continue;
    const slug = entry.slice(0, -3); // 去掉 .md
    const filePath = join(wikiDir, entry);
    const content = readFileSync(filePath, 'utf-8');
    const titleMatch = content.match(/^#\s+(.+)$/m);
    const title = titleMatch ? titleMatch[1].trim() : slug;
    result.push({ slug, title });
  }
  return result;
}

// 读单页（spec §3.7）：slug 不含 .md，内部拼扩展名
export function readWikiPage(slug: string): { slug: string; title: string; content: string } | null {
  const filePath = join(getWikiDir(), `${slug}.md`);
  if (!existsSync(filePath)) return null;
  const content = readFileSync(filePath, 'utf-8');
  const titleMatch = content.match(/^#\s+(.+)$/m);
  const title = titleMatch ? titleMatch[1].trim() : slug;
  return { slug, title, content };
}

// 写 wiki 页（spec §3.7）
export function writeWikiPage(slug: string, content: string): void {
  const filePath = join(getWikiDir(), `${slug}.md`);
  writeFileSync(filePath, content, 'utf-8');
}

// 追加 index 条目（spec §3.2）：读现有 index → 追加 → 重写
export function appendIndex(entry: { slug: string; title: string; identifier: string }): void {
  const indexPath = join(getWikiDir(), 'index.md');
  const date = new Date().toISOString().slice(0, 10);
  // index.md 条目格式：- [标题](slug.md) — 摘要（日期）
  // slug 在 index 里带 .md 后缀（markdown 链接标准格式）
  const line = `- [${entry.title}](${entry.slug}.md) — ${entry.identifier}（${date}）\n`;
  if (!existsSync(indexPath)) {
    writeFileSync(indexPath, '# Wiki Index\n\n## Pages\n', 'utf-8');
  }
  appendFileSync(indexPath, line, 'utf-8');
}

// 追加 log（spec §3.3）：append-only，前缀可 grep
export function appendLog(entry: {
  type: string; // 'ingest' | 'ingest-failed'
  identifier: string;
  issueId: string;
  slug?: string;
  error?: string;
}): void {
  const logPath = join(getWikiDir(), 'log.md');
  const date = new Date().toISOString().slice(0, 10);
  let block: string;
  if (entry.type === 'ingest') {
    block = `## [${date}] ingest | ${entry.identifier}\n- Source: issue/${entry.issueId}\n- Page: ${entry.slug}.md\n\n`;
  } else {
    // ingest-failed
    block = `## [${date}] ingest-failed | ${entry.identifier}\n- Source: issue/${entry.issueId}\n- Error: ${entry.error ?? 'unknown'}\n\n`;
  }
  if (!existsSync(logPath)) {
    writeFileSync(logPath, '# Wiki Log\n', 'utf-8');
  }
  appendFileSync(logPath, block, 'utf-8');
}

// 存 raw 快照（spec §3.4）：不可变，文件名含 timestamp 防覆盖
export function saveRaw(issueId: string, content: string): void {
  const rawDir = join(getWikiDir(), 'raw');
  if (!existsSync(rawDir)) {
    mkdirSync(rawDir, { recursive: true });
  }
  const ts = new Date().toISOString().replace(/[:.]/g, '-');
  const filePath = join(rawDir, `issue-${issueId}-${ts}.md`);
  writeFileSync(filePath, content, 'utf-8');
}
```

- [ ] **Step 2: typecheck 验证**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 3: 手动 spike 验证 store 函数可运行**

创建临时文件 `app/packages/server/src/wiki/_spike-store.ts`：

```typescript
import { ensureWikiDir, writeWikiPage, listWikiPages, readWikiPage, appendIndex, appendLog, saveRaw } from './store.js';

ensureWikiDir();
writeWikiPage('test-page', '# Test Page\n\nThis is a test.');
console.log('list:', listWikiPages());
console.log('read:', readWikiPage('test-page'));
appendIndex({ slug: 'test-page', title: 'Test Page', identifier: 'FRI-99' });
appendLog({ type: 'ingest', identifier: 'FRI-99', issueId: 'test-id', slug: 'test-page' });
saveRaw('test-id', '# Raw snapshot');
console.log('done');
```

Run: `cd app/packages/server && npx tsx src/wiki/_spike-store.ts`
Expected: 打印 list 含 `{slug: 'test-page', title: 'Test Page'}`，read 返回 content，wiki/ 目录出现 test-page.md + index.md 追加 + log.md 追加 + raw/ 快照

- [ ] **Step 4: 清理 spike + 验证产物**

```bash
rm app/packages/server/src/wiki/_spike-store.ts
```

检查 wiki/ 目录结构是否符合预期（index.md + log.md + test-page.md + raw/）。然后清理测试产物：
```bash
rm -rf wiki/
```

- [ ] **Step 5: Commit**

```bash
git add app/packages/server/src/wiki/store.ts
git commit -m "feat(s06): Wiki 存储层 store.ts（getWikiDir + list/read/write page + appendIndex/Log + saveRaw + ensureWikiDir）"
```

---

### Task 1.4: LLM 工厂 + prompt 构建（llm.ts）

**Files:** Create `app/packages/server/src/wiki/llm.ts`

**必读：** spec §4.3（双 provider 工厂）+ §4.4（LLM prompt）

**Interfaces:**
- Consumes: `@langchain/openai`（ChatOpenAI）、`@langchain/anthropic`（ChatAnthropic）、`@langchain/core`（BaseChatModel）
- Produces:
  - `createLlm(): BaseChatModel` — 按 `WIKI_LLM_PROVIDER` 环境变量返回 LLM 实例
  - `buildIngestPrompt(issue: Issue, sourceText: string): string` — 构建 ingest prompt
  - `generateWikiPage(llm, prompt): Promise<string>` — 调 LLM 返回 markdown 文本

- [ ] **Step 1: 创建 llm.ts**

Create `app/packages/server/src/wiki/llm.ts`:

```typescript
// S06 LLM 工厂 + prompt（spec §4.3-4.4）
// 双 provider 支持（OpenAI 兼容 + Anthropic），用 LangChain.js 的 BaseChatModel 统一接口
// 切换 provider 只改环境变量：WIKI_LLM_PROVIDER / WIKI_LLM_API_KEY / WIKI_LLM_BASE_URL / WIKI_LLM_MODEL
import { ChatOpenAI } from '@langchain/openai';
import { ChatAnthropic } from '@langchain/anthropic';
import type { BaseChatModel } from '@langchain/core/language_models/chat_models';
import type { Issue } from '@ma/shared';

// 双 provider 工厂（spec §4.3）
// WIKI_LLM_PROVIDER: 'openai' | 'anthropic'，默认 'openai'
// OpenAI 兼容格式覆盖 OpenAI/智谱/通义/Ollama/vLLM 等（通过 WIKI_LLM_BASE_URL 切端点）
export function createLlm(): BaseChatModel {
  const provider = process.env.WIKI_LLM_PROVIDER ?? 'openai';
  const apiKey = process.env.WIKI_LLM_API_KEY;
  const model = process.env.WIKI_LLM_MODEL ?? 'gpt-4o';
  if (!apiKey) throw new Error('WIKI_LLM_API_KEY 未配置');

  if (provider === 'anthropic') {
    return new ChatAnthropic({ model, apiKey });
  }
  // openai 兼容（默认）
  return new ChatOpenAI({
    model,
    apiKey,
    baseURL: process.env.WIKI_LLM_BASE_URL,
  });
}

// ingest prompt（spec §4.4）
export function buildIngestPrompt(issue: Issue, sourceText: string): string {
  return `你是一个项目 Wiki 维护者。以下是一个已完成的 Issue 的完整内容。请生成一个 Wiki 页（markdown），总结这个 Issue 的关键信息：做了什么、关键决策、产出。

Issue ${issue.identifier}: ${issue.title}

${sourceText}

请输出一个 markdown Wiki 页（以 # 标题开头），不要输出其他内容。`;
}

// 调 LLM 生成 wiki 页内容
// BaseChatModel.invoke 接收 string（或消息数组），返回 AIMessage，content 是字符串或复杂块
export async function generateWikiPage(
  llm: BaseChatModel,
  prompt: string,
): Promise<string> {
  const result = await llm.invoke(prompt);
  // AIMessage.content 可能是 string 或复杂块数组，统一 toString
  const content = typeof result.content === 'string'
    ? result.content
    : JSON.stringify(result.content);
  return content;
}
```

- [ ] **Step 2: typecheck 验证**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

> 注意：如果 `@langchain/core` 的 `BaseChatModel` import 路径不对，执行者需根据实际安装的版本调整。LangChain.js 不同版本的 export 路径可能变化。`BaseChatModel` 可能在 `@langchain/core/dist/language_models/chat_models` 或直接从包根。

- [ ] **Step 3: Commit**

```bash
git add app/packages/server/src/wiki/llm.ts
git commit -m "feat(s06): LLM 双 provider 工厂 + ingest prompt 构建（llm.ts）"
```

---

### Task 1.5: impl-1 自测 + handoff

**必读：** AGENTS.md §工程模式（handoff 规则）

- [ ] **Step 1: 完整 typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 2: 写 handoff**

写 `app/.progress/s06-impl-1.md`（照 `_TEMPLATE.md`），必须包含：
- 完成了什么（shared 契约 + store.ts + llm.ts + LangChain 依赖）
- LangChain 实际安装版本号
- store.ts spike 验证结果（list/read/write/appendIndex/appendLog/saveRaw 都跑通）
- 任何与 spec 的偏离及原因（如 BaseChatModel import 路径调整）
- 给 impl-2 的注意点（store.ts 导出的函数签名、llm.ts 的导出签名）

- [ ] **Step 3: Commit handoff**

```bash
git add app/.progress/s06-impl-1.md
git commit -m "docs(s06): impl-1 handoff（shared 契约 + store + llm 工厂）"
git push origin feat/s06-wiki
```

---

# 执行者片段 B（impl-2）：ingest 管线 + API 路由 + 触发接线

> **参考边界：** 服务端管线层。把 store.ts + llm.ts 接成完整 ingest，加 API 路由，接 Issue PUT 触发点。
> **会话边界：** 开新会话，读 AGENTS.md + spec §4/§6 + impl-1 handoff + 本 Task 组。同一分支。完成后写 `app/.progress/s06-impl-2.md`。
> **依赖：** impl-1 的 store.ts（所有导出函数）+ llm.ts（createLlm + buildIngestPrompt + generateWikiPage）+ shared 的 WikiPage/WikiPageSummary/WikiPageCreatedEvent

### Task 2.1: slug 生成工具

**Files:** Create `app/packages/server/src/wiki/slug.ts`

**必读：** spec §3.5（文件名 = `<identifier>-<slug>.md`，ASCII 安全但保留中文）

**Interfaces:**
- Produces: `generateSlug(identifier: string, title: string): string` — 返回 slug（不含 .md）

- [ ] **Step 1: 创建 slug.ts**

Create `app/packages/server/src/wiki/slug.ts`:

```typescript
// S06 slug 生成（spec §3.5）
// 文件名 = `<identifier>-<slug>.md`。slug 从标题生成，ASCII 安全但保留中文。
// 规则：取标题 → 空格转连字符 → 去除文件系统危险字符（/\:*?"<>|）→ 转小写 ASCII 部分 → 截断
export function generateSlug(identifier: string, title: string): string {
  const slug = title
    .trim()
    .replace(/\s+/g, '-')           // 空格 → 连字符
    .replace(/[/\\:*?"<>|]/g, '')   // 去文件系统危险字符
    .replace(/[\u0000-\u001f]/g, '') // 去控制字符
    .slice(0, 60);                   // 截断防文件名过长
  return `${identifier}-${slug}`;
}
```

- [ ] **Step 2: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 3: Commit**

```bash
git add app/packages/server/src/wiki/slug.ts
git commit -m "feat(s06): slug 生成工具（identifier + title → 文件名安全 slug）"
```

---

### Task 2.2: ingest 管线（ingest.ts）

**Files:** Create `app/packages/server/src/wiki/ingest.ts`

**必读：** spec §4.1-4.2（ingest 函数）+ §4.5（异步失败处理）+ impl-1 handoff（store/llm 导出签名）

**Interfaces:**
- Consumes: `store.ts`（saveRaw, writeWikiPage, appendIndex, appendLog）、`llm.ts`（createLlm, buildIngestPrompt, generateWikiPage）、`slug.ts`（generateSlug）、`db/client.ts`（db）、`db/schema.ts`（issues, comments）、`db/reshape.ts`（toIssue, toComment）、`event-bus.ts`（eventBus）
- Produces: `ingestIssue(issueId: string): Promise<void>` — 完整 ingest 管线（给 issues.ts PUT handler 调用）

- [ ] **Step 1: 创建 ingest.ts**

Create `app/packages/server/src/wiki/ingest.ts`:

```typescript
// S06 ingest 管线（spec §4.1-4.5）
// Issue status→done 时触发：读 Issue 内容 → 存 raw → LLM 生成 wiki 页 → 写文件 → 更新 index/log → WS 通知
// 异步 fire-and-forget，调用方负责 .catch()（spec W7/W8）
import { eq, desc } from 'drizzle-orm';
import { db } from '../db/client.js';
import { issues, comments } from '../db/schema.js';
import { toIssue, toComment } from '../db/reshape.js';
import { eventBus } from '../orchestration/event-bus.js';
import { saveRaw, writeWikiPage, appendIndex, appendLog } from './store.js';
import { createLlm, buildIngestPrompt, generateWikiPage } from './llm.js';
import { generateSlug } from './slug.js';

const K = 20; // 最近 K 条 comments（spec §4.4）

// 格式化 Issue + comments 为 sourceText（给 LLM 的输入）
function formatSource(
  issue: { description: string | null },
  commentRows: { type: string; authorLabel: string; body: string; createdAt: number }[],
): string {
  const parts: string[] = [];
  parts.push(`Description: ${issue.description ?? '(无)'}`);
  if (commentRows.length > 0) {
    parts.push(`\nComments（最近 ${commentRows.length} 条）:`);
    for (const c of commentRows) {
      parts.push(`\n[${c.type}] ${c.authorLabel}: ${c.body}`);
    }
  }
  return parts.join('\n');
}

// 完整 ingest 管线（spec §4.2）
// 调用方（issues.ts PUT handler）负责 void ingestIssue(id).catch(...)
export async function ingestIssue(issueId: string): Promise<void> {
  // 1. 读 Issue 内容
  const issueRow = db.select().from(issues).where(eq(issues.id, issueId)).get();
  if (!issueRow) throw new Error(`issue ${issueId} 不存在`);
  const issue = toIssue(issueRow);

  // 读最近 K 条 comments（按 createdAt DESC，取前 K）
  const commentRows = db
    .select()
    .from(comments)
    .where(eq(comments.issueId, issueId))
    .orderBy(desc(comments.createdAt))
    .limit(K)
    .all()
    .reverse(); // 反转成时间正序（取的是最近 K 条，展示时按正序）
  const commentApis = commentRows.map((r) => {
    const c = toComment(r);
    return { type: c.type, authorLabel: c.authorLabel, body: c.body, createdAt: new Date(c.createdAt).getTime() };
  });
  const sourceText = formatSource(issue, commentApis);

  // 2. 存 raw 快照
  saveRaw(issueId, `# ${issue.identifier}: ${issue.title}\n\n${sourceText}`);

  // 3. LLM 生成 wiki 页
  const llm = createLlm();
  const prompt = buildIngestPrompt(issue, sourceText);
  const wikiContent = await generateWikiPage(llm, prompt);

  // 4. 写 wiki 页
  const slug = generateSlug(issue.identifier, issue.title);
  writeWikiPage(slug, wikiContent);

  // 5. 更新 index + log
  appendIndex({ slug, title: issue.title, identifier: issue.identifier });
  appendLog({ type: 'ingest', identifier: issue.identifier, issueId, slug });

  // 6. WS 通知（spec §4.2 step 6）
  eventBus.publish({
    type: 'wiki:page-created',
    slug,
    title: issue.title,
  });
}
```

- [ ] **Step 2: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 3: Commit**

```bash
git add app/packages/server/src/wiki/ingest.ts
git commit -m "feat(s06): ingest 管线 ingestIssue（读 issue → raw → LLM → writePage → index/log → WS）"
```

---

### Task 2.3: Wiki API 路由（wiki.ts）

**Files:** Create `app/packages/server/src/routes/wiki.ts`

**必读：** spec §6（API：GET pages 列表 + GET pages/:slug）+ impl-1 handoff（store.ts 的 listWikiPages/readWikiPage 签名）

**Interfaces:**
- Consumes: `store.ts`（listWikiPages, readWikiPage）
- Produces: `wikiRoutes(app: FastifyInstance): Promise<void>` — 注册 GET /api/wiki/pages + GET /api/wiki/pages/:slug

- [ ] **Step 1: 创建 wiki.ts**

Create `app/packages/server/src/routes/wiki.ts`:

```typescript
// S06 Wiki API 路由（spec §6）
// GET /api/wiki/pages        列表：扫 wiki/*.md → WikiPageSummary[]
// GET /api/wiki/pages/:slug  单页：读 wiki/<slug>.md → WikiPage
// 无 POST/PUT（Wiki 页由 ingest pipeline 写，人不编辑）
import type { FastifyInstance } from 'fastify';
import { listWikiPages, readWikiPage } from '../wiki/store.js';

export async function wikiRoutes(app: FastifyInstance): Promise<void> {
  // GET /api/wiki/pages — 列表（spec §6）
  app.get('/api/wiki/pages', async () => {
    return listWikiPages();
  });

  // GET /api/wiki/pages/:slug — 单页（spec §6）
  app.get('/api/wiki/pages/:slug', async (req, reply) => {
    const { slug } = req.params as { slug: string };
    const page = readWikiPage(slug);
    if (!page) return reply.status(404).send({ error: 'wiki 页不存在' });
    return page;
  });
}
```

- [ ] **Step 2: Commit**

```bash
git add app/packages/server/src/routes/wiki.ts
git commit -m "feat(s06): Wiki API 路由（GET /api/wiki/pages + GET /api/wiki/pages/:slug）"
```

---

### Task 2.4: 注册路由 + 启动 ensureWikiDir

**Files:** Modify `app/packages/server/src/app.ts` + `app/packages/server/src/index.ts`

**必读：** spec §3.7 ensureWikiDir（启动时调）

- [ ] **Step 1: app.ts 注册 wikiRoutes**

Modify `app/packages/server/src/app.ts`，在 import 区加：

```typescript
import { wikiRoutes } from './routes/wiki.js';
```

在 `await app.register(runtimeRoutes);` 之后、`await app.register(wsRoutes);` 之前，加：

```typescript
  await app.register(wikiRoutes);
```

- [ ] **Step 2: index.ts 启动时 ensureWikiDir**

Modify `app/packages/server/src/index.ts`，在 import 区加：

```typescript
import { ensureWikiDir } from './wiki/store.js';
```

在 `scanSkills();` 之后加：

```typescript
  // S06：确保 wiki/ 目录 + 初始 index.md/log.md 存在（spec §3.7）
  ensureWikiDir();
```

- [ ] **Step 3: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 4: 启动验证**

Run: `cd app && pnpm dev`
Expected: server 启动无错，wiki/ 目录被创建（含初始 index.md + log.md + raw/）

验证：`ls wiki/` 应看到 `index.md` `log.md` `raw/`

- [ ] **Step 5: Commit**

```bash
git add app/packages/server/src/app.ts app/packages/server/src/index.ts
git commit -m "feat(s06): 注册 wikiRoutes + 启动时 ensureWikiDir"
```

---

### Task 2.5: Issue PUT 触发 ingest

**Files:** Modify `app/packages/server/src/routes/issues.ts`

**必读：** spec §4.1（触发条件 status→done）+ §4.5（异步 fire-and-forget + 失败处理）+ issues.ts 现有 PUT handler 结构

**Interfaces:**
- Consumes: `ingest.ts`（ingestIssue）、`store.ts`（appendLog）

- [ ] **Step 1: issues.ts 加 import**

在 `app/packages/server/src/routes/issues.ts` 顶部 import 区加：

```typescript
import { ingestIssue } from '../wiki/ingest.js';
import { appendLog } from '../wiki/store.js';
```

- [ ] **Step 2: 在 PUT handler 末尾加 ingest 触发**

找到 PUT handler 中 `return reply.send(issue);` 之前（约 line 176），在 assignee 副作用块之后、`return` 之前，加：

```typescript
    // S06：Issue 完成（status→done）时触发 Wiki ingest（spec §4.1/§4.5）
    // fire-and-forget 异步，不阻塞 PUT 响应；失败只 log，不影响 Issue 完成
    if (statusChanged && input.status === 'done') {
      void ingestIssue(id).catch((err) => {
        console.error('[wiki] ingest 失败:', err);
        // spec §4.5：失败只记 log，不重试（DLQ 留 S08）
        // identifier 从 prev 行读（此时 prev 还在作用域）
        appendLog({
          type: 'ingest-failed',
          identifier: prev.identifier,
          issueId: id,
          error: String(err),
        });
      });
    }
```

> 注意：`prev` 是 PUT handler 开头 `db.select()` 拿到的原始 issue 行（line 99），`prev.identifier` 在此作用域可见。`statusChanged` 和 `input.status` 也在作用域内。

- [ ] **Step 3: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 4: Commit**

```bash
git add app/packages/server/src/routes/issues.ts
git commit -m "feat(s06): Issue status→done 时触发 Wiki ingest（fire-and-forget + 失败只 log）"
```

---

### Task 2.6: impl-2 自测 + handoff

**必读：** spec §8.2-8.3（Wiki 存储 + ingest pipeline 验收）

- [ ] **Step 1: 完整 typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 2: 端到端验证（需配 WIKI_LLM_* 环境变量）**

准备环境变量（在 `app/` 目录下创建 `.env` 或终端 export）：
```bash
export WIKI_LLM_PROVIDER=openai    # 或 anthropic
export WIKI_LLM_API_KEY=你的key
export WIKI_LLM_MODEL=gpt-4o       # 或实际模型名
# export WIKI_LLM_BASE_URL=https://...  # 如用国产/本地端点
```

Run: `cd app && pnpm dev`

手动测试：
1. 浏览器开 `http://localhost:3000`，把一个 Issue 拖到 done 列（或 PUT status=done）
2. 等 5-15 秒（LLM 调用耗时）
3. 检查 `wiki/` 目录：应出现 `raw/issue-*.md` 快照 + 一个新 `*.md` wiki 页 + `index.md` 追加 + `log.md` 追加
4. `curl http://localhost:3001/api/wiki/pages` 应返回含新页的列表

> 如果没有 API key 或不想花 token，跳过实际 LLM 调用，验证 API 路由本身（`curl /api/wiki/pages` 返回 `[]` 或已有页）+ 触发逻辑（在 issues.ts console.log 确认 ingest 被 fire）。

- [ ] **Step 3: 写 handoff**

写 `app/.progress/s06-impl-2.md`，必须包含：
- 完成了什么（ingest.ts + slug.ts + wiki.ts 路由 + issues.ts 触发 + app.ts/index.ts 接线）
- 端到端验证结果（是否实际调了 LLM？ingest 产出了什么？）
- 给 impl-3 的注意点（API 端点路径、WikiPageCreatedEvent 结构、WS 已通过 eventBus→wsBroadcaster 自动到前端）

- [ ] **Step 4: Commit + push**

```bash
git add app/.progress/s06-impl-2.md
git commit -m "docs(s06): impl-2 handoff（ingest 管线 + API + 触发接线）"
git push origin feat/s06-wiki
```

---

# 执行者片段 C（impl-3）：前端 Wiki 浏览器 + 侧栏激活 + 端到端验收

> **参考边界：** 纯前端。Wiki 浏览器页 + WS 实时 + 侧栏导航激活。
> **会话边界：** 开新会话，读 AGENTS.md + spec §7 + impl-2 handoff + 本 Task 组。同一分支。完成后写 `app/.progress/s06-impl-3.md`。
> **依赖：** impl-2 的 API（GET /api/wiki/pages + GET /api/wiki/pages/:slug）+ WikiPageCreatedEvent WS 事件

### Task 3.1: api.ts 加 Wiki hooks

**Files:** Modify `app/packages/web/lib/api.ts`

**必读：** spec §6（API 端点）+ impl-2 handoff（API 路径确认）

**Interfaces:**
- Consumes: `WikiPage` / `WikiPageSummary` from `@ma/shared`
- Produces: `useWikiPages()` hook（返回 `WikiPageSummary[]`）、`useWikiPage(slug)` hook（返回 `WikiPage`）

- [ ] **Step 1: api.ts 加 import + hooks**

在 `app/packages/web/lib/api.ts` 的 import 区（line 3-16 的 type import 块）加 `WikiPage, WikiPageSummary`：

```typescript
import type {
  Issue,
  Comment,
  CreateIssueInput,
  UpdateIssueInput,
  CreateCommentInput,
  AgentSummary,
  AgentDetail,
  SkillInfo,
  SquadSummary,
  AgentRun,
  RunMessage,
  RuntimesResponse,
  WikiPage,
  WikiPageSummary,
} from '@ma/shared';
```

在文件末尾加：

```typescript
// —— S06 Wiki hooks ——

// GET /api/wiki/pages —— wiki 页列表（spec §6）
export function useWikiPages() {
  return useQuery<WikiPageSummary[]>({
    queryKey: ['wiki-pages'],
    queryFn: async () => {
      const res = await fetch(`${API}/wiki/pages`);
      if (!res.ok) throw new Error('加载 wiki 失败');
      return res.json();
    },
  });
}

// GET /api/wiki/pages/:slug —— 单页内容（spec §6）
export function useWikiPage(slug: string | null) {
  return useQuery<WikiPage>({
    queryKey: ['wiki-page', slug],
    queryFn: async () => {
      const res = await fetch(`${API}/wiki/pages/${slug}`);
      if (!res.ok) throw new Error('加载 wiki 页失败');
      return res.json();
    },
    enabled: !!slug,
  });
}
```

- [ ] **Step 2: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 3: Commit**

```bash
git add app/packages/web/lib/api.ts
git commit -m "feat(s06): useWikiPages + useWikiPage hooks"
```

---

### Task 3.2: ws.ts 加 wiki:page-created handler

**Files:** Modify `app/packages/web/lib/ws.ts`

**必读：** spec §7.2（WS 收 wiki:page-created → 列表追加）+ impl-2 handoff（WikiPageCreatedEvent 结构：`{type, slug, title}`）

**Interfaces:**
- Consumes: `WikiPageCreatedEvent` from `@ma/shared`（通过 DomainEvent 联合）

- [ ] **Step 1: ws.ts 加 handler**

在 `app/packages/web/lib/ws.ts` 的 `ws.onmessage` 回调中，找到最后的注释行 `// S03 run:progress：fire-and-forget...`（约 line 84），在其**之后**加：

```typescript
      // S06 wiki:page-created：invalidate wiki 列表 cache（spec §7.2）
      // WS 事件由 server 的 ingest pipeline → eventBus → wsBroadcaster 自动广播到前端
      if (event.type === 'wiki:page-created') {
        qc.invalidateQueries({ queryKey: ['wiki-pages'] });
      }
```

> 用 `invalidateQueries` 而非直接 setQueryData：因为 wiki 页列表来自 GET /api/wiki/pages（扫文件系统），invalidate 让 React Query 重新 fetch 是最可靠的方式。新页 content 要从文件系统读，前端无法凭 WS 事件里的 slug+title 构造完整页。

- [ ] **Step 2: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 3: Commit**

```bash
git add app/packages/web/lib/ws.ts
git commit -m "feat(s06): WS wiki:page-created handler（invalidate wiki 列表）"
```

---

### Task 3.3: WikiPage 组件

**Files:** Create `app/packages/web/components/WikiPage.tsx`

**必读：** spec §7.1（左侧页面列表 + 右侧 markdown 渲染，复用 MarkdownBody）+ SkillsPage.tsx（既有页头模式参考）

**Interfaces:**
- Consumes: `useWikiPages` + `useWikiPage` from `@/lib/api`、`MarkdownBody` from `@/components/MarkdownBody`

- [ ] **Step 1: 创建 WikiPage.tsx**

Create `app/packages/web/components/WikiPage.tsx`:

```tsx
'use client';
import { useState } from 'react';
import { useWikiPages, useWikiPage } from '@/lib/api';
import { MarkdownBody } from './MarkdownBody';

// S06 Wiki 浏览器（spec §7.1）
// 左侧页面列表 + 右侧 markdown 渲染（复用 S02 MarkdownBody）
// 照 concepts llm-wiki-pattern.md：Agent 写，人只读
export function WikiPage() {
  const { data: pages, isFetching } = useWikiPages();
  const [selectedSlug, setSelectedSlug] = useState<string | null>(null);
  const { data: currentPage } = useWikiPage(selectedSlug);

  return (
    <div className="page-container">
      <div className="page-header">
        <div>
          <div className="page-title">
            Wiki <span className="count">{pages?.length ?? 0}</span>
          </div>
          <div className="page-desc">Issue 完成时自动生成的知识页（LLM 编译式 Wiki）。</div>
        </div>
      </div>

      <div className="wiki-layout">
        {/* 左侧列表 */}
        <div className="wiki-sidebar">
          {isFetching && !pages && <div className="text-dim">加载中…</div>}
          {pages && pages.length === 0 && (
            <div className="text-dim">还没有 Wiki 页。完成一个 Issue（拖到 Done）试试。</div>
          )}
          {pages?.map((p) => (
            <button
              key={p.slug}
              type="button"
              className={`wiki-list-item${selectedSlug === p.slug ? ' active' : ''}`}
              onClick={() => setSelectedSlug(p.slug)}
            >
              {p.title}
            </button>
          ))}
        </div>

        {/* 右侧渲染 */}
        <div className="wiki-content">
          {!selectedSlug && (
            <div className="text-dim">← 从左侧选择一个页面</div>
          )}
          {selectedSlug && !currentPage && (
            <div className="text-dim">加载中…</div>
          )}
          {currentPage && (
            <MarkdownBody source={currentPage.content} />
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add app/packages/web/components/WikiPage.tsx
git commit -m "feat(s06): WikiPage 组件（左列表 + 右渲染，复用 MarkdownBody）"
```

---

### Task 3.4: /wiki 路由页 + 侧栏激活

**Files:** Create `app/packages/web/app/wiki/page.tsx` + Modify `app/packages/web/components/Sidebar.tsx`

**必读：** spec §7.3（侧栏 NAV_ITEMS 的 Wiki 入口激活 → /wiki）

- [ ] **Step 1: 创建路由页**

Create `app/packages/web/app/wiki/page.tsx`:

```tsx
import { WikiPage } from '@/components/WikiPage';

export default function Page() {
  return <WikiPage />;
}
```

- [ ] **Step 2: Sidebar 激活 Wiki 入口**

Modify `app/packages/web/components/Sidebar.tsx`，找到 NAV_ITEMS 中的 wiki 项（约 line 19）：

```typescript
  { id: 'wiki', label: 'Wiki', icon: 'wiki', section: 'workspace' },
```

改为（加 `href: '/wiki'`）：

```typescript
  { id: 'wiki', label: 'Wiki', icon: 'wiki', section: 'workspace', href: '/wiki' },
```

- [ ] **Step 3: typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 4: Commit**

```bash
git add app/packages/web/app/wiki/page.tsx app/packages/web/components/Sidebar.tsx
git commit -m "feat(s06): /wiki 路由页 + 侧栏 Wiki 入口激活"
```

---

### Task 3.5: Wiki 布局样式

**Files:** Modify `app/packages/web/app/globals.css`

**必读：** WikiPage.tsx 用到的 className（`wiki-layout` `wiki-sidebar` `wiki-list-item` `wiki-content`）

- [ ] **Step 1: globals.css 加 wiki 样式**

在 `app/packages/web/app/globals.css` 末尾加：

```css
/* —— S06 Wiki 浏览器 —— */
.wiki-layout {
  display: flex;
  gap: 16px;
  min-height: 400px;
}

.wiki-sidebar {
  width: 260px;
  flex-shrink: 0;
  border-right: 1px solid var(--border, #e0e0e0);
  padding-right: 16px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.wiki-list-item {
  text-align: left;
  padding: 8px 12px;
  border: none;
  background: none;
  cursor: pointer;
  border-radius: 6px;
  font-size: 14px;
  color: var(--text-secondary, #555);
  transition: background 0.15s;
}

.wiki-list-item:hover {
  background: var(--bg-hover, #f0f0f0);
}

.wiki-list-item.active {
  background: var(--bg-active, #e8f0fe);
  color: var(--text-primary, #1a1a1a);
  font-weight: 500;
}

.wiki-content {
  flex: 1;
  min-width: 0;
  padding: 8px 0;
}
```

> 注意：CSS 变量名（`--border` / `--text-secondary` 等）需对齐 globals.css 已有的变量。执行者应先看 globals.css 顶部的 `:root` 变量定义，用已有的；如果没有对应变量，直接用注释里的回退值。

- [ ] **Step 2: Commit**

```bash
git add app/packages/web/app/globals.css
git commit -m "feat(s06): Wiki 浏览器布局样式（左列表右渲染）"
```

---

### Task 3.6: 端到端验收 + handoff

**必读：** spec §8（全部验收标准）

- [ ] **Step 1: 完整 typecheck**

Run: `cd app && pnpm -r typecheck`
Expected: 全绿

- [ ] **Step 2: 端到端手动验收**

Run: `cd app && pnpm dev`

打开两个浏览器窗口（双窗口验证 WS 同步）：
- 窗口 A：`http://localhost:3000`（看板）
- 窗口 B：`http://localhost:3000/wiki`（Wiki 浏览器）

逐项验证 spec §8：

§8.1 工程：
- [ ] `pnpm -r typecheck` 全绿
- [ ] `pnpm dev` 起 server + web
- [ ] （配了 key 的话）ingest 能调 LLM

§8.2 Wiki 存储：
- [ ] `wiki/` 目录存在（index.md + log.md 初始版）
- [ ] GET /api/wiki/pages 返回列表（curl 验证或浏览器看 /wiki 列表）
- [ ] GET /api/wiki/pages/:slug 返回内容（点左侧页面项，右侧渲染）

§8.3 ingest pipeline（核心，需配 key）：
- [ ] PUT issue status=done → ingest 自动触发
- [ ] `wiki/raw/` 出现 Issue 快照
- [ ] `wiki/*.md` 出现 LLM 生成的 wiki 页
- [ ] `wiki/index.md` 追加条目
- [ ] `wiki/log.md` 追加日志
- [ ] WS wiki:page-created 推前端（窗口 B /wiki 列表自动出现新页）

§8.4 Wiki 浏览器：
- [ ] `/wiki` 左侧列表 + 右侧渲染
- [ ] ingest 后实时出现新页

§8.5 回归：
- [ ] 看板拖拽正常（S01）
- [ ] Issue 详情 + 时间线正常（S02）
- [ ] Agent 执行/取消正常（S03）
- [ ] Squad 委派正常（S04）
- [ ] Skills 页 + agent 详情正常（S05）

- [ ] **Step 3: 写 handoff**

写 `app/.progress/s06-impl-3.md`，必须包含：
- 完成了什么（前端 WikiPage + 路由 + 侧栏 + WS handler + 样式）
- 端到端验收结果（逐项勾 spec §8）
- 与 spec 的偏离及原因（如 CSS 变量名对齐）
- wiki/ 目录是否进 git（spec §10 风险表：开发期进 git）

- [ ] **Step 4: Commit + push**

```bash
git add app/.progress/s06-impl-3.md
git commit -m "docs(s06): impl-3 handoff（前端 Wiki 浏览器 + 端到端验收）"
git push origin feat/s06-wiki
```

---

## 验收总览（计划者对照）

spec §8 全部验收项 → Task 映射：

| spec §8 验收项 | 对应 Task |
|---|---|
| §8.1 typecheck / pnpm dev / LLM 配置 | 全部 Task 的 typecheck step + Task 2.4 启动验证 + Task 3.6 端到端 |
| §8.2 wiki/ 目录 / GET pages / GET pages/:slug | Task 1.3 ensureWikiDir + Task 2.3 wiki.ts 路由 + Task 3.6 |
| §8.3 ingest 触发 / raw / wiki 页 / index / log / WS | Task 2.2 ingest.ts + Task 2.5 issues.ts 触发 + Task 3.6 |
| §8.4 /wiki 左右栏 + 实时 | Task 3.3 WikiPage + Task 3.2 ws handler + Task 3.6 |
| §8.5 回归 S01-S05 | Task 3.6 端到端验收 |

spec 决策 W1-W8 → Task 映射：

| 决策 | 对应 Task |
|---|---|
| W1 三切片拆分 | 本计划只覆盖 S06 |
| W2 文件系统 markdown | Task 1.3 store.ts |
| W3 Issue done 触发 | Task 2.5 issues.ts |
| W4 LangChain 直调 | Task 1.4 llm.ts + Task 2.2 ingest.ts |
| W5 项目根 wiki/ | Task 1.3 getWikiDir |
| W6 双 provider | Task 1.4 createLlm |
| W7 异步 fire-and-forget | Task 2.5 void + .catch |
| W8 失败只 log | Task 2.5 .catch 内 appendLog + Task 1.3 appendLog |
