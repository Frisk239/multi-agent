# S05 Skill（本地目录）+ MCP 配置 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **本项目工程模式：** AGENTS.md 规定的"垂直切片 × 计划者-执行者"。计划者把 Task 按自然依赖边界切成执行者片段，每个片段由用户新开的会话执行，靠 `app/.progress/` handoff 串行交接。
> **spec 真源：** [`docs/superpowers/specs/2026-07-15-s05-skill-mcp-design.md`](../specs/2026-07-15-s05-skill-mcp-design.md)（自审 R1-R6）
> **S04 真实代码（S05 在此之上改）：** 所有 file:line 已核对到 S04 合并后版本

**Goal:** Skill 本地目录扫描 + agent 分配 + prompt 注入；MCP JSON 配置 + CLI 注入；前置修 claude stdin。点亮 FRI-11 答辩路径最后一段。

**Architecture:** skill 文件系统真源 + 内存索引（不进 DB，照 hermes 零足迹）；分配关系进 DB（agent_skill）；skill 拼进 user prompt；MCP JSON 存 agent 表 + `--mcp-config` 注入 CLI。

**Tech Stack:** 同 S01-S04（TypeScript / Fastify / Drizzle / better-sqlite3 / Next.js / Zod）

## Global Constraints

（来自 spec，所有 Task 隐式遵守）

- **分支：** `feat/s05-skill-mcp`（已从 main 切出）
- **skill 不进 DB**：文件系统真源 + 内存索引 Map（照 hermes `scan_skill_commands`）
- **skill 目录**：项目级 `{MA_WORKSPACE_CWD}/.skills/` + 用户级 `~/.multi-agent/skills/`，合并去重（项目级优先）
- **SKILL.md 格式**：YAML frontmatter（name + description）+ markdown body；无 frontmatter 降级用文件名
- **分配关系进 DB**：agent_skill(agent_id, skill_id) 复合 PK
- **MCP**：agent 表加 mcp_servers text nullable（JSON 字符串）；claude-code `--mcp-config` 传临时文件
- **skill 注入**：拼进 user prompt（前置于 briefing + issue body），照 hermes 保 cache
- **前置修复**：claude stdin（spawn-line 支持 stdin 写入）
- **Git：** feature 分支，Conventional Commits，绝不 push main

## 文件结构（S05 改动决策）

```
app/packages/
├── shared/src/schema.ts          [改] 加 SkillInfo 契约
├── server/src/
│   ├── db/
│   │   ├── schema.ts             [改] 删 skills 表；agent 加 mcpServers 列；新增 agentSkills 表
│   │   └── seed.ts               [改] 删 skill insert；加 agent_skill 分配 insert
│   ├── skill/
│   │   └── scanner.ts            [新] 目录扫描 + frontmatter 解析 + 内存索引
│   ├── runtime/
│   │   ├── types.ts              [改] ExecutionInput 加 mcpServers
│   │   ├── claude-code.ts        [改] stdin 修复 + --mcp-config
│   │   ├── opencode.ts           [改] 传 mcpServers（若 CLI 支持）
│   │   ├── cursor.ts             [改] 传 mcpServers（若 CLI 支持）
│   │   ├── spawn-line.ts         [改 ★结构扩展] 加 stdinInput 支持
│   │   └── prompt.ts             [改] buildPrompt 加 agentId 查 skill + 拼接
│   ├── orchestration/
│   │   └── run-worker.ts         [改] executeRun 传 agentId + mcpServers
│   └── routes/
│       ├── skills.ts             [新] GET /api/skills + POST /api/skills/refresh + agent skill 分配
│       └── roster.ts             [改] agent 详情 skill/mcp 端点（或合入 skills.ts）
├── server/src/app.ts             [改] 注册 skillRoutes
├── server/src/index.ts           [改] 启动时 scanSkills()
└── web/                          [改] Skills 页 + agent 详情 Skills/MCP Tab + 侧栏激活
.skills/                          [新] 项目级 skill 目录（5 个示例 SKILL.md）
```

---

# 执行者片段 A（impl-1）：DB + scanner + seed + shared 契约

> **参考边界：** 数据层 + skill 扫描器。S05 所有后续工作依赖此。
> **会话边界：** 开新会话，读 AGENTS.md + spec §3/§4/§5 + 本 Task 组。同一分支。完成后写 `app/.progress/s05-impl-1.md`。

### Task 1.1: DB schema 改动 + migration

**Files:** Modify `app/packages/server/src/db/schema.ts`

**必读：** spec §3.1-3.2b（R6 删死表 + agent_skill + agent.mcpServers）

- [ ] **Step 1: 删除 skills 表定义**

从 schema.ts 删除 `export const skills = sqliteTable('skill', {...})` 整块。

- [ ] **Step 2: agent 表加 mcpServers 列**

```typescript
export const agents = sqliteTable('agent', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  category: text('category'),
  runtime: text('runtime', { enum: ['claude-code', 'opencode', 'cursor'] })
    .notNull()
    .default('claude-code'),
  concurrency: integer('concurrency').notNull().default(1),
  mcpServers: text('mcp_servers'),  // S05：MCP 配置 JSON 字符串
  createdAt: integer('created_at').notNull(),
});
```

- [ ] **Step 3: 新增 agentSkills 表**

```typescript
// —— agent_skill（S05：skill 分配关系，skill_id 是 skill name 非 FK）——
export const agentSkills = sqliteTable(
  'agent_skill',
  {
    agentId: text('agent_id')
      .notNull()
      .references(() => agents.id, { onDelete: 'cascade' }),
    skillId: text('skill_id').notNull(),  // skill 的 name（文件系统真源）
  },
  (t) => ({
    pk: primaryKey({ columns: [t.agentId, t.skillId] }),
    agentIdx: index('idx_agent_skill_agent').on(t.agentId),
  }),
);
```

- [ ] **Step 4: db:generate + migrate**

```bash
cd app
pnpm --filter @ma/server db:generate
# 确认 drizzle/ 出现 0004_*.sql（DROP TABLE skill + ALTER agent ADD mcp_servers + CREATE TABLE agent_skill）
rm -f packages/server/dev.db packages/server/dev.db-shm packages/server/dev.db-wal
pnpm --filter @ma/server db:migrate
```

- [ ] **Step 5: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/db/schema.ts app/packages/server/drizzle
git commit -m "feat(s05): 删 skill 死表 + agent 加 mcpServers + agent_skill 分配表"
```

---

### Task 1.2: seed 更新

**Files:** Modify `app/packages/server/src/db/seed.ts`

**必读：** spec §3.4（seed skill 目录 + agent_skill 分配）

- [ ] **Step 1: 删除 skill insert + 其 import**

seed.ts 删除 `import { ..., skills, ... }` 里的 skills，删除 `db.insert(skills).values([...]).run()` 整块。

- [ ] **Step 2: 加 agent_skill 分配 insert**

```typescript
import { agentSkills } from './schema.js';

// S05：skill 分配关系（照 seed.js agent.skillIds，skill name 对应 .skills/ 目录名）
db.insert(agentSkills)
  .values([
    { agentId: 'agt-lead', skillId: 'multica-squads' },
    { agentId: 'agt-research', skillId: 'extract-prototype-requirements' },
    { agentId: 'agt-prd', skillId: 'prd-writer' },
    { agentId: 'agt-prd', skillId: 'extract-prototype-requirements' },
    { agentId: 'agt-proto', skillId: 'frontend-design' },
    { agentId: 'agt-proto', skillId: 'design-system' },
  ])
  .run();
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/db/seed.ts
git commit -m "feat(s05): seed 删 skill insert + 加 agent_skill 分配"
```

---

### Task 1.3: skill scanner

**Files:** Create `app/packages/server/src/skill/scanner.ts`

**必读：** spec §5（扫描器 + frontmatter 解析 + R4 降级 + R5 绝对路径）

- [ ] **Step 1: 写 scanner.ts**

```typescript
// app/packages/server/src/skill/scanner.ts
import { readFileSync, readdirSync, existsSync } from 'node:fs';
import { join, resolve, basename } from 'node:path';
import { homedir } from 'node:os';

export interface SkillInfo {
  name: string;
  description: string;
  body: string;
  path: string;
  source: 'project' | 'user';
}

let skillIndex = new Map<string, SkillInfo>();

// 扫描两个目录（项目级优先覆盖用户级），建内存索引（照 hermes scan_skill_commands）
export function scanSkills(): void {
  const next = new Map<string, SkillInfo>();
  // 用户级先扫（低优先级）
  scanDir(join(homedir(), '.multi-agent', 'skills'), 'user', next);
  // 项目级后扫（覆盖同名）。R5：用 resolve 确保绝对路径
  const cwd = process.env.MA_WORKSPACE_CWD;
  if (cwd && cwd.length > 0) scanDir(resolve(cwd, '.skills'), 'project', next);
  skillIndex = next;
}

function scanDir(dir: string, source: 'project' | 'user', out: Map<string, SkillInfo>): void {
  if (!existsSync(dir)) return;
  let entries;
  try {
    entries = readdirSync(dir, { withFileTypes: true });
  } catch {
    return;
  }
  for (const entry of entries) {
    if (entry.isDirectory()) {
      // 目录形态：<name>/SKILL.md
      const skillFile = join(dir, entry.name, 'SKILL.md');
      if (existsSync(skillFile)) parseAndStore(skillFile, source, out);
    } else if (entry.isFile() && entry.name.endsWith('.md') && entry.name !== 'SKILL.md') {
      // 扁平形态：<name>.md
      parseAndStore(join(dir, entry.name), source, out);
    }
  }
}

function parseAndStore(path: string, source: 'project' | 'user', out: Map<string, SkillInfo>): void {
  let raw: string;
  try {
    raw = readFileSync(path, 'utf8');
  } catch {
    return;
  }
  const { frontmatter, body } = parseFrontmatter(raw);
  // R4 降级：无 frontmatter 或缺 name → 用文件名作 name
  const name = frontmatter.name ?? basename(path, '.md');
  out.set(name, {
    name,
    description: frontmatter.description ?? '',
    body,
    path,
    source,
  });
}

// 简单 frontmatter 解析（不引 yaml 依赖，只解析 name/description 两字段）
function parseFrontmatter(raw: string): { frontmatter: { name?: string; description?: string }; body: string } {
  const fmMatch = raw.match(/^---\n([\s\S]*?)\n---\n?([\s\S]*)$/);
  if (!fmMatch) {
    return { frontmatter: {}, body: raw };
  }
  const fmBlock = fmMatch[1];
  const body = fmMatch[2];
  const frontmatter: { name?: string; description?: string } = {};
  for (const line of fmBlock.split('\n')) {
    const m = line.match(/^(\w+):\s*(.+)$/);
    if (m && (m[1] === 'name' || m[1] === 'description')) {
      frontmatter[m[1] as 'name' | 'description'] = m[2].trim().replace(/^["']|["']$/g, '');
    }
  }
  return { frontmatter, body };
}

export function getSkillIndex(): Map<string, SkillInfo> {
  return skillIndex;
}

export function getSkillsForAgent(agentId: string): SkillInfo[] {
  // 查 agent_skill 需 DB import——此函数在 prompt.ts 调，那里有 db
  // 这里只暴露 index，agent_skill 查询由调用方做
  return []; // 占位，实际查询在 prompt.ts 或 run-worker 内
}
```

> 注意：`getSkillsForAgent` 需要查 DB（agent_skill 表）+ 内存索引。由于 scanner.ts 不 import db（避免循环），实际的 agent→skill 查询逻辑放在调用方（prompt.ts 或一个 skill-service.ts）。impl 者可酌情把 getSkillsForAgent 移到一个有 db import 的模块。

- [ ] **Step 2: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/skill/scanner.ts
git commit -m "feat(s05): skill 目录扫描器 + frontmatter 解析 + 内存索引"
```

---

### Task 1.4: .skills/ 示例目录 + shared 契约 + 启动集成

**Files:** Create `.skills/*/SKILL.md`; Modify `shared/schema.ts`, `index.ts`

- [ ] **Step 1: 建 5 个示例 skill（项目根 .skills/）**

```bash
mkdir -p .skills/prd-writer .skills/extract-prototype-requirements .skills/frontend-design .skills/design-system .skills/multica-squads
```

每个 `SKILL.md`（示例 prd-writer）：

```markdown
---
name: prd-writer
description: 将需求转化为结构化 PRD 文档
---

# PRD Writer

当被要求产出 PRD 时，按以下结构组织文档：

1. 问题定义
2. 目标用户与场景
3. 功能需求（MoSCoW 优先级）
4. 非功能需求
5. 验收标准（RTM 矩阵）
```

其余 4 个照此格式（description 各异，body 简短即可）。

- [ ] **Step 2: shared 加 SkillInfo 契约**

```typescript
// shared/src/schema.ts 末尾加
export const SkillInfo = z.object({
  name: z.string(),
  description: z.string(),
  source: z.enum(['project', 'user']),
  usedBy: z.array(AgentSummary),
});
export type SkillInfo = z.infer<typeof SkillInfo>;
```

- [ ] **Step 3: index.ts 启动时 scanSkills**

```typescript
import { scanSkills } from './skill/scanner.js';

async function main() {
  scanSkills();  // S05：启动时扫 skill 目录
  const app = await buildApp();
  startRunWorker();
  // ...
}
```

- [ ] **Step 4: typecheck + 验证扫描 + commit**

```bash
pnpm -r typecheck
# 临时验证：node 脚本调 scanSkills() 后 console.log(getSkillIndex()) 确认 5 个 skill
```

```bash
git add .skills app/packages/shared/src/schema.ts app/packages/server/src/index.ts
git commit -m "feat(s05): 5 个示例 skill + SkillInfo 契约 + 启动扫描"
```

- [ ] **Step 5: handoff + commit**

写 `app/.progress/s05-impl-1.md`。给 impl-2 注意点：scanner 就绪、agent_skill 表就绪、agent.mcpServers 列就绪、skill name 对应 .skills/ 目录名。

```bash
git add app/.progress/s05-impl-1.md
git commit -m "docs(s05): impl-1 handoff"
```

---

# 执行者片段 B（impl-2）：stdin 修复 + skill/MCP 注入 + API

> **参考边界：** 执行层注入 + API。最厚片段（含 spawn-line 结构扩展）。
> **会话边界：** 读 spec §6/§7/§8 + s05-impl-1 handoff + 本 Task 组。同一分支。产出 `s05-impl-2.md`。

### Task 2.1: claude stdin 修复 + spawn-line stdin 支持（★结构扩展）

**Files:** Modify `spawn-line.ts`, `claude-code.ts`

**必读：** spec §8（R2 结构性改动）

> **开工第一步：spike 确认 claude-code stdin 模式 argv**。跑 `echo "say hi" | claude -p --output-format stream-json --verbose` 确认能从 stdin 读 prompt。

- [ ] **Step 1: spawn-line 加 stdinInput 支持**

spawnLineProcess 签名加可选 `stdinInput?: string`。传了时 spawn 后 write stdin：

```typescript
export function spawnLineProcess(
  bin: string,
  args: string[],
  cwd: string,
  signal: AbortSignal,
  onEvent: (e: AgentEvent) => void,
  onLine: LineHandler | null,
  stdinInput?: string,  // S05：stdin pipe 传 prompt（claude stdin 修复）
): Promise<ExecutionResult> {
  return new Promise((resolve) => {
    // ...（现有 spawn 逻辑）
    const child = spawn(bin, args, { cwd, shell: isCmdShim, windowsHide: true, env: process.env });

    // S05：stdin pipe 传 prompt
    if (stdinInput !== undefined) {
      child.stdin?.setEncoding('utf8');
      child.stdin?.write(stdinInput);
      child.stdin?.end();
      child.stdin?.on('error', (err) => {
        // stdin 写失败不致命（prompt 可能已传）
      });
    }

    // ...（其余 stdout/stderr/close 逻辑不变）
  });
}
```

- [ ] **Step 2: claude-code.ts 改用 stdin 传 prompt**

```typescript
async execute(input, onEvent, signal): Promise<ExecutionResult> {
  const det = await this.detect();
  if (!det.path) return { finalText: '', exitReason: 'failed', error: 'claude CLI 未安装' };
  // S05 stdin 修复：prompt 从 stdin 传（argv 的 -p 无参数 = 从 stdin 读）
  // spike 确认 argv：可能是 ['-p', '--output-format', 'stream-json', '--verbose']
  return spawnLineProcess(
    det.path,
    ['-p', '--output-format', 'stream-json', '--verbose'],  // spike 后调整
    input.cwd,
    signal,
    onEvent,
    parseClaudeLine,
    input.prompt,  // stdinInput
  );
}
```

> **spike 要点**：确认 `claude -p` 不带 prompt 参数时是否从 stdin 读。若不是，尝试 `claude --print -` 或 `claude` 管道模式。记进 handoff。

- [ ] **Step 3: typecheck + spike 验证 + commit**

```bash
# spike：MA_WORKSPACE_CWD=. pnpm --filter @ma/server dev
# PUT issue assignee=agent → 确认 claude run 不再报 no stdin data，能跑完
pnpm --filter @ma/server typecheck
git add app/packages/server/src/runtime/spawn-line.ts app/packages/server/src/runtime/claude-code.ts
git commit -m "fix(s05): claude stdin 修复 + spawn-line 支持 stdin pipe（R2 结构扩展）"
```

---

### Task 2.2: skill prompt 注入

**Files:** Modify `runtime/prompt.ts`, `orchestration/run-worker.ts`

**必读：** spec §6（buildPrompt 扩展 + skill 前置）

- [ ] **Step 1: buildPrompt 加 agentId + skill 拼接**

prompt.ts 的 PromptRunContext 加 agentId。buildPrompt 内查 agent_skill → 内存索引 → 拼 skill 块：

```typescript
import { eq } from 'drizzle-orm';
import { db } from '../db/client.js';
import { issues, comments, agentSkills } from '../db/schema.js';
import { loadSquadDetail } from '../db/squad-loader.js';
import { getSkillIndex } from '../skill/scanner.js';

interface PromptRunContext {
  isLeader: boolean;
  squadId: string | null;
  agentId: string;  // S05 新增
}

export function buildPrompt(issueId: string, run?: PromptRunContext): string | null {
  // ...（现有 issue body + briefing 逻辑不变）

  // S05：skill 注入（照 hermes，进 user prompt 不进 system prompt）
  let skillBlock: string | null = null;
  if (run?.agentId) {
    const assigned = db.select().from(agentSkills).where(eq(agentSkills.agentId, run.agentId)).all();
    const index = getSkillIndex();
    const skills = assigned
      .map((a) => index.get(a.skillId))
      .filter((s): s is NonNullable<typeof s> => s !== undefined);
    if (skills.length > 0) {
      skillBlock = skills.map((s) => `## Skill: ${s.name}\n${s.body}`).join('\n\n');
    }
  }

  // 拼接顺序：skillBlock + briefing(if leader) + issueBody
  const parts = [skillBlock];
  if (run?.isLeader && run?.squadId) {
    // ...briefing 逻辑（现有，产出 briefing 字符串）
    parts.push(briefing);
  }
  parts.push(body);
  return parts.filter(Boolean).join('\n\n---\n\n');
}
```

- [ ] **Step 2: run-worker.ts 传 agentId**

executeRun 调 buildPrompt 时加 agentId：

```typescript
const prompt = buildPrompt(runRow.issueId, {
  isLeader: runRow.isLeader === 1,
  squadId: runRow.squadId,
  agentId: runRow.agentId,  // S05 新增
});
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/runtime/prompt.ts app/packages/server/src/orchestration/run-worker.ts
git commit -m "feat(s05): buildPrompt skill 注入（agent_skill 查询 + 内存索引拼接）"
```

---

### Task 2.3: MCP 注入

**Files:** Modify `runtime/types.ts`, `claude-code.ts`, `opencode.ts`, `cursor.ts`, `run-worker.ts`

**必读：** spec §7（R3 临时文件 try/finally）

- [ ] **Step 1: ExecutionInput 加 mcpServers**

```typescript
// types.ts
export interface ExecutionInput {
  prompt: string;
  cwd: string;
  issueId: string;
  agentId: string;
  runId: string;
  mcpServers?: string | null;  // S05：MCP 配置 JSON 字符串
}
```

- [ ] **Step 2: claude-code.ts 加 --mcp-config + 临时文件 try/finally（R3）**

```typescript
import { writeFileSync, unlinkSync } from 'node:fs';
import { join } from 'node:path';
import { tmpdir } from 'node:os';

async execute(input, onEvent, signal): Promise<ExecutionResult> {
  const det = await this.detect();
  if (!det.path) return { finalText: '', exitReason: 'failed', error: 'claude CLI 未安装' };

  const args = ['-p', '--output-format', 'stream-json', '--verbose'];
  let mcpTmpPath: string | null = null;

  // S05 MCP 注入（R3 try/finally 清理临时文件）
  if (input.mcpServers) {
    try {
      const config = JSON.stringify({ mcpServers: JSON.parse(input.mcpServers) });
      mcpTmpPath = join(tmpdir(), `mcp-${input.runId}.json`);
      writeFileSync(mcpTmpPath, config);
      args.push('--mcp-config', mcpTmpPath);
    } catch {
      // JSON 解析失败，忽略 MCP（降级不报错）
    }
  }

  try {
    return await spawnLineProcess(
      det.path, args, input.cwd, signal, onEvent, parseClaudeLine, input.prompt,
    );
  } finally {
    if (mcpTmpPath) { try { unlinkSync(mcpTmpPath); } catch { /* ignore */ } }
  }
}
```

- [ ] **Step 3: opencode/cursor 传 mcpServers（若 CLI 支持，否则忽略）**

opencode.ts / cursor.ts 的 execute 加 `input.mcpServers` 传递（若 CLI 有 MCP flag 则加 argv，否则忽略）。

- [ ] **Step 4: run-worker.ts 传 mcpServers**

```typescript
const agentRow = db.select().from(agents).where(eq(agents.id, runRow.agentId)).get();
const mcpServers = agentRow?.mcpServers ?? null;

const result = await backend.execute(
  { prompt, cwd, issueId: runRow.issueId, agentId: runRow.agentId, runId: runRow.id, mcpServers },
  onEvent, signal,
);
```

- [ ] **Step 5: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/runtime/types.ts app/packages/server/src/runtime/claude-code.ts app/packages/server/src/runtime/opencode.ts app/packages/server/src/runtime/cursor.ts app/packages/server/src/orchestration/run-worker.ts
git commit -m "feat(s05): MCP 注入（ExecutionInput + --mcp-config 临时文件 try/finally）"
```

---

### Task 2.4: skill + MCP API 路由

**Files:** Create `routes/skills.ts`; Modify `app.ts`

**必读：** spec §4（API 端点 + R1 悬空过滤）

- [ ] **Step 1: routes/skills.ts**

```typescript
import type { FastifyInstance } from 'fastify';
import { eq } from 'drizzle-orm';
import { db } from '../db/client.js';
import { agents, agentSkills } from '../db/schema.js';
import { getSkillIndex, scanSkills } from '../skill/scanner.js';

export async function skillRoutes(app: FastifyInstance): Promise<void> {
  // GET /api/skills —— 内存索引 + usedBy 反查
  app.get('/api/skills', async () => {
    const index = getSkillIndex();
    const allAssigns = db.select().from(agentSkills).all();
    const result = [];
    for (const [name, skill] of index) {
      const assignedAgentIds = allAssigns.filter((a) => a.skillId === name).map((a) => a.agentId);
      const usedBy = assignedAgentIds.length > 0
        ? db.select().from(agents).all()
            .filter((a) => assignedAgentIds.includes(a.id))
            .map((a) => ({ id: a.id, name: a.name, runtime: a.runtime }))
        : [];
      result.push({ name: skill.name, description: skill.description, source: skill.source, usedBy });
    }
    return result;
  });

  // POST /api/skills/refresh —— 重扫
  app.post('/api/skills/refresh', async () => {
    scanSkills();
    return { ok: true };
  });

  // GET /api/agents/:id/skills —— 返回已分配 skillId（R1：过滤悬空）
  app.get('/api/agents/:id/skills', async (req) => {
    const { id } = req.params as { id: string };
    const index = getSkillIndex();
    const rows = db.select().from(agentSkills).where(eq(agentSkills.agentId, id)).all();
    return rows.filter((r) => index.has(r.skillId)).map((r) => r.skillId);
  });

  // PUT /api/agents/:id/skills —— 整体替换分配
  app.put('/api/agents/:id/skills', async (req, reply) => {
    const { id } = req.params as { id: string };
    const { skillIds } = req.body as { skillIds: string[] };
    db.delete(agentSkills).where(eq(agentSkills.agentId, id)).run();
    if (skillIds.length > 0) {
      db.insert(agentSkills).values(skillIds.map((sid) => ({ agentId: id, skillId: sid }))).run();
    }
    return { ok: true };
  });

  // GET /api/agents/:id/mcp
  app.get('/api/agents/:id/mcp', async (req) => {
    const { id } = req.params as { id: string };
    const agent = db.select().from(agents).where(eq(agents.id, id)).get();
    return { mcpServers: agent?.mcpServers ?? null };
  });

  // PUT /api/agents/:id/mcp
  app.put('/api/agents/:id/mcp', async (req) => {
    const { id } = req.params as { id: string };
    const { mcpServers } = req.body as { mcpServers: string | null };
    db.update(agents).set({ mcpServers }).where(eq(agents.id, id)).run();
    return { ok: true };
  });
}
```

- [ ] **Step 2: app.ts 注册 skillRoutes**

```typescript
import { skillRoutes } from './routes/skills.js';
// ...
await app.register(skillRoutes);
```

- [ ] **Step 3: typecheck + 自测 + commit**

```bash
pnpm --filter @ma/server typecheck
# 起 server 验证：GET /api/skills（5 skill）、GET /api/agents/agt-lead/skills（multica-squads）
git add app/packages/server/src/routes/skills.ts app/packages/server/src/app.ts
git commit -m "feat(s05): skill + MCP API 路由（GET/refresh/分配/mcp）"
```

- [ ] **Step 4: handoff + commit**

写 `app/.progress/s05-impl-2.md`。给 impl-3 注意点：API 路径、claude stdin spike 结果、MCP argv。

```bash
git add app/.progress/s05-impl-2.md
git commit -m "docs(s05): impl-2 handoff"
```

---

# 执行者片段 C（impl-3）：前端 + 端到端验收

> **参考边界：** 前端三块 + 验收。
> **会话边界：** 读 spec §9/§10 + s05-impl-2 handoff。同一分支。产出 `s05-impl-3.md`。

### Task 3.1: Skills 页 + agent 详情 Skills/MCP Tab + 侧栏

**Files:** Create `app/packages/web/app/skills/page.tsx`, `components/SkillsPage.tsx`; Modify agent 详情 + `Sidebar.tsx`

**必读：** spec §9 + RTM UI-SKL / UI-AGT-009~012 + 原型 `renderSkills`（app.js:619）

- [ ] **Step 1: Skills 页（照原型 renderSkills）**

`app/skills/page.tsx` + `SkillsPage.tsx`：页头（count + 重新扫描）+ 搜索 + 表格 4 列（名称/被谁使用/来源/——）。GET /api/skills + POST /api/skills/refresh。

- [ ] **Step 2: agent 详情 Skills Tab**

列出全部 skill（GET /api/skills）+ checkbox 勾选（GET /api/agents/:id/skills 初值）+ PUT 更新分配。

- [ ] **Step 3: agent 详情 MCP Tab**

JSON 编辑器 textarea + 保存（PUT /api/agents/:id/mcp）+ 清空。

- [ ] **Step 4: 侧栏 Skills 入口激活**

S03 的 Sidebar 12 项里 Skills 是占位不可跳转。激活 → `/skills`。

- [ ] **Step 5: typecheck + commit**

```bash
pnpm --filter @ma/web typecheck
git add app/packages/web
git commit -m "feat(s05): Skills 页 + agent 详情 Skills/MCP Tab + 侧栏激活"
```

---

### Task 3.2: 端到端验收

**必读：** spec §10（全部验收标准）

- [ ] **Step 1: 工程门槛**

```bash
cd app
pnpm -r typecheck
MA_WORKSPACE_CWD="D:\code\multi-agent" pnpm dev
```

- [ ] **Step 2: claude stdin**
- [ ] **Step 3: skill 扫描 + 分配 + 注入**
- [ ] **Step 4: MCP 配置 + 注入**
- [ ] **Step 5: 回归 S01-S04**
- [ ] **Step 6: FRI-11 全路径**

- [ ] **Step 7: handoff + commit**

写 `app/.progress/s05-impl-3.md`。push 分支，停，等计划者验收。

---

## 自检

**spec 覆盖：**
- §1 数据流 → Task 1.x + 2.x ✓
- §3 数据模型 → Task 1.1 ✓
- §4 API → Task 2.4 ✓
- §5 skill 扫描 → Task 1.3 ✓
- §6 skill prompt 注入 → Task 2.2 ✓
- §7 MCP 注入 → Task 2.3 ✓
- §8 claude stdin → Task 2.1 ✓
- §9 前端 → Task 3.1 ✓
- §10 验收 → Task 3.2 ✓
- R1-R6 → 全覆盖 ✓

**占位扫描：** 无 TBD/TODO，claude stdin argv 标 spike ✓

**类型一致：** scanSkills/getSkillIndex/buildPrompt(agentId)/ExecutionInput.mcpServers/skillRoutes 跨 Task 一致 ✓
