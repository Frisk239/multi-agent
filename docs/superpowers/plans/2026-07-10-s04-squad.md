# S04 Squad 小队（leader 委派闭环）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **本项目工程模式：** AGENTS.md 规定的"垂直切片 × 计划者-执行者"。计划者把 Task 按自然依赖边界切成执行者片段，每个片段由用户新开的会话执行，靠 `app/.progress/` handoff 串行交接。执行者数量不固定。
> **spec 真源：** [`docs/superpowers/specs/2026-07-10-s04-squad-design.md`](../specs/2026-07-10-s04-squad-design.md)（两轮自审 R1-R7）
> **S03 真实代码（S04 在此之上改）：** 所有 file:line 已核对到 S03 impl-2 落地版本

**Goal:** 打通 squad 委派闭环：指派 squad → leader 被 brief → leader 执行产出 @mention → comment-trigger 自动派 worker → worker 执行。

**Architecture:** 不新建任务抽象，全部复用 S03 的 agent_run + RunWorker。S04 三块新增：squad→leader 路由、briefing 注入、comment-trigger。并发模型从全局 busy 改 per-agent 槽。

**Tech Stack:** 同 S01-S03（TypeScript / Fastify / Drizzle / better-sqlite3 / Next.js / Zod）

## Global Constraints

（来自 spec，所有 Task 隐式遵守）

- **分支：** `feat/s04-squad`，从 S03 合并后的 main 切出（S03 必须先合 main）
- **不新建 squad task 抽象**：squad 指派 = 解析 leader + 复用 agent_run
- **状态枚举不变**：issue.status 7 态、agent_run.status 5 态（S04 不加状态）
- **agent_run 加 `is_leader` + `squad_id` 两列**（照 multica 090/127 migration）
- **agent 表加 `concurrency` 列**（per-agent 槽上限）
- **squad 表加 `operating_protocol` + `mission_directive`**，新建 `squad_member` 表
- **并发模型**：per-(issue,agent) 去重 + per-agent 槽（concurrency）
- **乒乓熔断**：issue run 总数上限 15
- **comment-trigger**：只处理 type==='comment'，@agent 自指放行，@squad leader 自指跳过
- **briefing 前置**（我们无 Instructions 层）
- **Git：** feature 分支，Conventional Commits，绝不 push main

## 文件结构（S04 改动决策）

```
app/packages/
├── shared/src/schema.ts          [改] AgentRun 加 isLeader/squadId；SquadDetail 契约（protocol/directive/members）
├── server/src/
│   ├── db/
│   │   ├── schema.ts             [改] squad 加2列；agent 加1列；agent_run 加2列；新增 squadMembers 表
│   │   ├── seed.ts               [改] squad insert 补 protocol/directive；agent 补 concurrency；新增 squad_member insert
│   │   └── reshape.ts            [改] toAgentRun 映射 isLeader/squadId；新增 toSquadDetail
│   ├── orchestration/
│   │   ├── run-worker.ts         [改 ★核心重写] 全局 busy → per-agent 槽 + 并发执行
│   │   ├── run-service.ts        [改] enqueue 去重改 per-(issue,agent) + 熔断；新增 enqueueLeaderRun
│   │   └── comment-trigger.ts    [新] triggerFromComment + parseMentions + 防自指
│   ├── runtime/prompt.ts         [改] buildPrompt 签名加 run 参数；leader run 前置 briefing
│   ├── db/squad-loader.ts        [新] loadSquadDetail（briefing 组装用）
│   └── routes/
│       ├── issues.ts             [改] PUT assignee=squad 分支 → enqueueLeaderRun
│       └── comments.ts           [改] POST comment 后调 triggerFromComment
└── web/                          [几乎不改] 闭环靠现有时间线 + RunStatusBar 呈现
```

---

# 执行者片段 A（impl-1）：DB + shared 契约 + seed

> **参考边界：** 数据层底座。S04 所有后续工作依赖此。
> **会话边界：** 开新会话，读 AGENTS.md + spec §3/§4 + s04-planner handoff + 本 Task 组。从 S03 合并后的 main 开 `feat/s04-squad`。完成后写 `app/.progress/s04-impl-1.md`。

### Task 1.1: 分支 + Drizzle schema 扩展

**Files:** Modify `app/packages/server/src/db/schema.ts`

**Interfaces:**
- Produces: `squads` 加 operatingProtocol/missionDirective 列；`agents` 加 concurrency 列；`agentRuns` 加 isLeader/squadId 列；新增 `squadMembers` 表

**必读：** spec §3.1-3.4

- [ ] **Step 1: 开分支**

```bash
cd D:/code/multi-agent
git checkout main && git pull origin main
git checkout -b feat/s04-squad
```

- [ ] **Step 2: squad 表加 2 列（schema.ts，在现有 squads 定义内加）**

```typescript
export const squads = sqliteTable('squad', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  leaderId: text('leader_id'),
  operatingProtocol: text('operating_protocol').notNull().default(''),
  missionDirective: text('mission_directive').notNull().default(''),
  createdAt: integer('created_at').notNull(),
});
```

- [ ] **Step 3: agent 表加 concurrency 列**

```typescript
export const agents = sqliteTable('agent', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  category: text('category'),
  runtime: text('runtime', { enum: ['claude-code', 'opencode', 'cursor'] })
    .notNull()
    .default('claude-code'),
  concurrency: integer('concurrency').notNull().default(1),
  createdAt: integer('created_at').notNull(),
});
```

- [ ] **Step 4: agent_run 表加 is_leader + squad_id 列（照 multica 090/127）**

在 agentRuns 定义的 columns 内加（status 之后）：

```typescript
    isLeader: integer('is_leader').notNull().default(0),
    squadId: text('squad_id'),
```

- [ ] **Step 5: 新增 squad_member 表（在 schema.ts 末尾，runMessages 之后）**

```typescript
// —— squad_member（成员关系，简化版，不建 member_type/role，成员恒 agent）——
export const squadMembers = sqliteTable(
  'squad_member',
  {
    squadId: text('squad_id')
      .notNull()
      .references(() => squads.id, { onDelete: 'cascade' }),
    agentId: text('agent_id')
      .notNull()
      .references(() => agents.id, { onDelete: 'cascade' }),
  },
  (t) => ({
    pk: primaryKey({ columns: [t.squadId, t.agentId] }),
    squadIdx: index('idx_squad_member_squad').on(t.squadId),
  }),
);
```

- [ ] **Step 6: db:generate + migrate**

```bash
cd app
pnpm --filter @ma/server db:generate
# 确认 drizzle/ 出现 0003_*.sql（含 ALTER ADD COLUMN + CREATE TABLE squad_member）
rm -f packages/server/dev.db packages/server/dev.db-shm packages/server/dev.db-wal
pnpm --filter @ma/server db:migrate
```

- [ ] **Step 7: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/db/schema.ts app/packages/server/drizzle
git commit -m "feat(s04): squad/agent/agent_run 扩列 + squad_member 表"
```

---

### Task 1.2: shared 契约扩展

**Files:** Modify `app/packages/shared/src/schema.ts`

**必读：** spec §4（R2 修订）

- [ ] **Step 1: AgentRun 加 isLeader + squadId**

找到现有 AgentRun 定义，在 `createdAt` 之前加：

```typescript
export const AgentRun = z.object({
  id: BusinessId,
  issueId: BusinessId,
  agentId: BusinessId,
  runtime: RuntimeId,
  status: AgentRunStatus,
  error: z.string().nullable(),
  startedAt: z.string().datetime().nullable(),
  finishedAt: z.string().datetime().nullable(),
  isLeader: z.boolean().default(false),
  squadId: BusinessId.nullable(),
  createdAt: z.string().datetime(),
});
```

- [ ] **Step 2: 新增 SquadMember + SquadDetail 契约（briefing/前端用）**

在 SquadSummary 之后加：

```typescript
export const SquadMember = z.object({
  agentId: BusinessId,
  name: z.string(),
});
export type SquadMember = z.infer<typeof SquadMember>;

export const SquadDetail = z.object({
  id: BusinessId,
  name: z.string(),
  leaderId: BusinessId,
  operatingProtocol: z.string(),
  missionDirective: z.string(),
  members: z.array(SquadMember),
});
export type SquadDetail = z.infer<typeof SquadDetail>;
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter @ma/shared typecheck
git add app/packages/shared/src/schema.ts
git commit -m "feat(s04): shared AgentRun 加 isLeader/squadId + SquadDetail 契约"
```

---

### Task 1.3: seed 更新 + reshape + squad-loader

**Files:** Modify `seed.ts`, `reshape.ts`; Create `squad-loader.ts`

**必读：** spec §3.5（R5 修订：明确改 seed.ts insert 语句）

- [ ] **Step 1: seed.ts — squad insert 补 protocol/directive**

```typescript
db.insert(squads)
  .values([
    {
      id: 'sqd-product',
      name: '产品小队',
      leaderId: 'agt-lead',
      operatingProtocol: '1. 队长接收 Issue briefing\n2. 按专精 @mention 委派\n3. 成员回帖交付物路径\n4. 队长汇总后请求 MVP 签核',
      missionDirective: '基于 chanpin 真源产出 PRD + RTM + 可交互原型，Must 路径可点通。',
      createdAt: NOW,
    },
    {
      id: 'sqd-philosophy',
      name: '哲学与人文研究小队',
      leaderId: 'agt-lead',
      operatingProtocol: '文献综述 → 论点提炼 → 答辩材料。',
      missionDirective: '人文类课题调研与写作支持。',
      createdAt: NOW,
    },
    {
      id: 'sqd-eco',
      name: '生态研究团队',
      leaderId: 'agt-prd',
      operatingProtocol: '数据采集 → 分析 → 可视化报告。',
      missionDirective: '生态监测与数据分析 mock 小队。',
      createdAt: NOW,
    },
  ])
  .run();
```

- [ ] **Step 2: seed.ts — agent insert 补 concurrency**

```typescript
db.insert(agents)
  .values([
    { id: 'agt-lead', name: '产品·策划队长', category: '产品', runtime: 'claude-code', concurrency: 6, createdAt: NOW },
    { id: 'agt-research', name: '产品·调研与洞察官', category: '产品', runtime: 'opencode', concurrency: 4, createdAt: NOW },
    { id: 'agt-prd', name: '产品·需求与PRD官', category: '产品', runtime: 'cursor', concurrency: 4, createdAt: NOW },
    { id: 'agt-proto', name: '产品·设计·原型官', category: '产品', runtime: 'claude-code', concurrency: 6, createdAt: NOW },
  ])
  .run();
```

- [ ] **Step 3: seed.ts — 新增 squad_member insert**

```typescript
db.insert(squadMembers)
  .values([
    { squadId: 'sqd-product', agentId: 'agt-research' },
    { squadId: 'sqd-product', agentId: 'agt-prd' },
    { squadId: 'sqd-product', agentId: 'agt-proto' },
    { squadId: 'sqd-philosophy', agentId: 'agt-research' },
    { squadId: 'sqd-eco', agentId: 'agt-research' },
    { squadId: 'sqd-eco', agentId: 'agt-proto' },
  ])
  .run();
```

- [ ] **Step 4: reshape.ts — toAgentRun 映射 isLeader/squadId**

```typescript
export function toAgentRun(row: RunRow): AgentRun {
  return {
    id: row.id,
    issueId: row.issueId,
    agentId: row.agentId,
    runtime: row.runtime,
    status: row.status,
    error: row.error,
    startedAt: iso(row.startedAt),
    finishedAt: iso(row.finishedAt),
    isLeader: row.isLeader === 1,
    squadId: row.squadId,
    createdAt: new Date(row.createdAt).toISOString(),
  };
}
```

- [ ] **Step 5: 新建 squad-loader.ts（briefing 组装用）**

```typescript
// app/packages/server/src/db/squad-loader.ts
import { eq } from 'drizzle-orm';
import { db } from './client.js';
import { squads, squadMembers, agents } from './schema.js';
import type { SquadDetail } from '@ma/shared';

// 加载 squad 详情（含成员），briefing 组装 + trigger 用
export function loadSquadDetail(squadId: string): SquadDetail | null {
  const squad = db.select().from(squads).where(eq(squads.id, squadId)).get();
  if (!squad) return null;
  const memberRows = db
    .select({ agentId: squadMembers.agentId, name: agents.name })
    .from(squadMembers)
    .innerJoin(agents, eq(squadMembers.agentId, agents.id))
    .where(eq(squadMembers.squadId, squadId))
    .all();
  return {
    id: squad.id,
    name: squad.name,
    leaderId: squad.leaderId,
    operatingProtocol: squad.operatingProtocol,
    missionDirective: squad.missionDirective,
    members: memberRows.map((m) => ({ agentId: m.agentId, name: m.name })),
  };
}

// 查 squad leaderId（trigger 路由用，轻量查询）
export function getSquadLeaderId(squadId: string): string | null {
  const squad = db.select().from(squads).where(eq(squads.id, squadId)).get();
  return squad?.leaderId ?? null;
}
```

- [ ] **Step 6: seed + typecheck + 行数验证**

```bash
pnpm --filter @ma/server db:seed
pnpm -r typecheck
# 验证（sqlite3 或 node 脚本）：
# squad 3 行带 protocol/directive；agent 4 行带 concurrency；squad_member 6 行；issue 8 + comment 6 不变
```

- [ ] **Step 7: handoff + commit**

写 `app/.progress/s04-impl-1.md`，给 impl-2 注意点：表名/列名、toAgentRun 已映射 isLeader、loadSquadDetail 已就绪、enqueueLeaderRun 要在 run-service 加。

```bash
git add app/packages/server/src/db app/.progress/s04-impl-1.md
git commit -m "feat(s04): seed 补 protocol/concurrency/member + reshape + squad-loader"
```

---

# 执行者片段 B（impl-2）：briefing + 路由 + trigger + 并发改造

> **参考边界：** S04 核心逻辑。最厚最险的片段——含 RunWorker 并发改造（★核心重写）+ comment-trigger 闭环。
> **会话边界：** 读 spec §5/§6/§7 + s04-impl-1 handoff + 本 Task 组。同一分支。产出 `s04-impl-2.md`。

### Task 2.1: buildPrompt 扩展（briefing 前置）

**Files:** Modify `app/packages/server/src/runtime/prompt.ts`

**必读：** spec §5（briefing 前置，S9 决策）

- [ ] **Step 1: buildPrompt 签名加 run 参数 + leader 分支**

当前签名 `buildPrompt(issueId: string)`。改为：

```typescript
import { loadSquadDetail } from '../db/squad-loader.js';

interface PromptRunContext {
  isLeader: boolean;
  squadId: string | null;
}

export function buildPrompt(issueId: string, run?: PromptRunContext): string | null {
  const issue = db.select().from(issues).where(eq(issues.id, issueId)).get();
  if (!issue) return null;
  const rows = db
    .select()
    .from(comments)
    .where(eq(comments.issueId, issueId))
    .orderBy(desc(comments.createdAt))
    .limit(K)
    .all()
    .reverse();
  const history = rows.map((c) => `[${c.authorType}:${c.authorId}] ${c.body}`).join('\n\n');
  const body = [
    `Issue ${issue.identifier}: ${issue.title}`,
    issue.description ? `Description:\n${issue.description}` : '',
    history ? `Recent comments:\n${history}` : '',
    'Please work on this issue in the current workspace.',
  ]
    .filter(Boolean)
    .join('\n\n');

  // S04 briefing 前置（spec §5 S9：我们无 Instructions 层，briefing 是最高优先级角色指令）
  if (run?.isLeader && run?.squadId) {
    const squad = loadSquadDetail(run.squadId);
    if (squad) {
      // roster 跳过 leader 本人（spec §5，照 multica squad_briefing.go:156）
      const rosterMembers = squad.members.filter((m) => m.agentId !== squad.leaderId);
      const roster = rosterMembers
        .map((m) => `- ${m.name} — [@${m.name}](mention://agent/${m.agentId})`)
        .join('\n');
      const briefing = [
        `# Squad Operating Protocol\n${squad.operatingProtocol}`,
        `# Squad Roster\n${roster}`,
        `# Mission Directive\n${squad.missionDirective}`,
      ].join('\n\n');
      return briefing + '\n\n---\n\n' + body;
    }
  }
  return body;
}
```

- [ ] **Step 2: run-worker.ts 调用点改**

run-worker.ts:62 当前 `const prompt = buildPrompt(runRow.issueId);`。改为传 run context：

```typescript
const prompt = buildPrompt(runRow.issueId, {
  isLeader: runRow.isLeader === 1,
  squadId: runRow.squadId,
});
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/runtime/prompt.ts app/packages/server/src/orchestration/run-worker.ts
git commit -m "feat(s04): buildPrompt leader run 前置三段 briefing"
```

---

### Task 2.2: enqueue 去重改 per-(issue,agent) + 熔断 + enqueueLeaderRun

**Files:** Modify `app/packages/server/src/orchestration/run-service.ts`

**必读：** spec §6.1（去重）、§7.4（熔断 R1）、§5.2（enqueueLeaderRun）

- [ ] **Step 1: 改 enqueueAgentRun 去重 + 加熔断**

当前 `enqueueAgentRun`（run-service.ts:49-80）的 active 检查是 per-issue。改为：

```typescript
const MAX_RUNS_PER_ISSUE = 15; // 乒乓熔断（spec §7.4 R1）

export function enqueueAgentRun(issueId: string, agentId: string): AgentRun | null {
  // per-(issue,agent) 去重（spec §6.1）：同一 agent 同一 issue 不重复排
  const activeForAgent = db
    .select()
    .from(agentRuns)
    .where(
      and(
        eq(agentRuns.issueId, issueId),
        eq(agentRuns.agentId, agentId),
        inArray(agentRuns.status, [...ACTIVE]),
      ),
    )
    .get();
  if (activeForAgent) return null;

  // 乒乓熔断（spec §7.4 R1）：issue 总 run 数超限
  const totalRow = db
    .select({ cnt: sql<number>`COUNT(*)` })
    .from(agentRuns)
    .where(eq(agentRuns.issueId, issueId))
    .get();
  if ((totalRow?.cnt ?? 0) >= MAX_RUNS_PER_ISSUE) {
    // 记 system comment 通知用户
    const cid = crypto.randomUUID();
    db.insert(comments)
      .values({
        id: cid,
        issueId,
        type: 'comment',
        authorType: 'member',
        authorId: 'system',
        body: `⚠️ 已达 issue run 上限（${MAX_RUNS_PER_ISSUE}），停止派发。`,
        createdAt: Date.now(),
      })
      .run();
    const cRow = db.select().from(comments).where(eq(comments.id, cid)).get();
    if (cRow) eventBus.publish({ type: 'comment:created', comment: toComment(cRow) });
    return null;
  }

  const agent = db.select().from(agents).where(eq(agents.id, agentId)).get();
  if (!agent) return null;
  const id = crypto.randomUUID();
  const createdAt = Date.now();
  db.insert(agentRuns)
    .values({
      id, issueId, agentId,
      runtime: agent.runtime,
      status: 'queued',
      error: null, startedAt: null, finishedAt: null,
      isLeader: 0, squadId: null,
      createdAt,
    })
    .run();
  const row = db.select().from(agentRuns).where(eq(agentRuns.id, id)).get()!;
  const run = toAgentRun(row);
  eventBus.publish({ type: 'run:queued', run });
  wakeRunWorker();
  return run;
}
```

> 注意 import：run-service.ts 顶部要加 `import { sql } from 'drizzle-orm';` 和 `import { comments } from '../db/schema.js';` 和 `import { toComment } from '../db/reshape.js';`（若尚未导入）。

- [ ] **Step 2: 新增 enqueueLeaderRun（is_leader=1 + squad_id）**

在 enqueueAgentRun 之后加：

```typescript
// enqueueLeaderRun —— squad 指派时，解析 leader 排 leader run（spec §5.2）
// 复用 enqueueAgentRun 的去重 + 熔断逻辑，但多设 is_leader=1 + squad_id
export function enqueueLeaderRun(
  issueId: string,
  leaderId: string,
  squadId: string,
): AgentRun | null {
  const activeForAgent = db
    .select()
    .from(agentRuns)
    .where(
      and(
        eq(agentRuns.issueId, issueId),
        eq(agentRuns.agentId, leaderId),
        inArray(agentRuns.status, [...ACTIVE]),
      ),
    )
    .get();
  if (activeForAgent) return null;

  // 复用熔断检查（同 enqueueAgentRun 逻辑，可抽成共用函数避免重复——实现者酌情）
  const totalRow = db
    .select({ cnt: sql<number>`COUNT(*)` })
    .from(agentRuns)
    .where(eq(agentRuns.issueId, issueId))
    .get();
  if ((totalRow?.cnt ?? 0) >= MAX_RUNS_PER_ISSUE) return null;

  const agent = db.select().from(agents).where(eq(agents.id, leaderId)).get();
  if (!agent) return null;
  const id = crypto.randomUUID();
  const createdAt = Date.now();
  db.insert(agentRuns)
    .values({
      id, issueId, agentId: leaderId,
      runtime: agent.runtime,
      status: 'queued',
      error: null, startedAt: null, finishedAt: null,
      isLeader: 1, squadId,
      createdAt,
    })
    .run();
  const row = db.select().from(agentRuns).where(eq(agentRuns.id, id)).get()!;
  const run = toAgentRun(row);
  eventBus.publish({ type: 'run:queued', run });
  wakeRunWorker();
  return run;
}
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/orchestration/run-service.ts
git commit -m "feat(s04): enqueue per-(issue,agent) 去重 + 乒乓熔断 + enqueueLeaderRun"
```

---

### Task 2.3: issues PUT 扩展 squad 路由

**Files:** Modify `app/packages/server/src/routes/issues.ts`

**必读：** spec §5.1（assignee=squad → enqueueLeaderRun + R7 confirm 复用说明）

- [ ] **Step 1: import enqueueLeaderRun**

issues.ts:8 当前 import。加 enqueueLeaderRun：

```typescript
import { cancelActiveRunsForIssue, enqueueAgentRun, enqueueLeaderRun } from '../orchestration/run-service.js';
import { loadSquadDetail } from '../db/squad-loader.js';
```

- [ ] **Step 2: PUT assignee 副作用加 squad 分支**

issues.ts:157-168 当前 assignee 副作用。扩展为：

```typescript
    if (input.assignee !== undefined) {
      const prevKey = assigneeKey(prev.assigneeType, prev.assigneeId);
      const nextType = input.assignee?.type ?? null;
      const nextId = input.assignee?.id ?? null;
      const nextKey = assigneeKey(nextType, nextId);
      if (prevKey !== nextKey) {
        cancelActiveRunsForIssue(id);
        if (nextType === 'agent' && nextId) {
          enqueueAgentRun(id, nextId);
        } else if (nextType === 'squad' && nextId) {
          // S04：squad 指派 → 解析 leader → enqueueLeaderRun（spec §5.1）
          const squad = loadSquadDetail(nextId);
          if (squad?.leaderId) {
            enqueueLeaderRun(id, squad.leaderId, squad.id);
          }
        }
      }
    }
```

- [ ] **Step 3: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/routes/issues.ts
git commit -m "feat(s04): PUT assignee=squad → 解析 leader enqueueLeaderRun"
```

---

### Task 2.4: comment-trigger 新建 + 挂接

**Files:** Create `comment-trigger.ts`; Modify `comments.ts`, `run-worker.ts`

**必读：** spec §7（全部：trigger 逻辑 + 防自指 S8 + 熔断 §7.4 + type 判断 R4）

- [ ] **Step 1: 新建 orchestration/comment-trigger.ts**

```typescript
// app/packages/server/src/orchestration/comment-trigger.ts
import type { Comment } from '@ma/shared';
import { enqueueAgentRun, enqueueLeaderRun } from './run-service.js';
import { getSquadLeaderId } from '../db/squad-loader.js';

// 解析 comment body 的 mention link（spec §7.1）
// 格式：[@Name](mention://agent/<id>) 或 [@Name](mention://squad/<id>)
function parseMentions(body: string): Array<{ kind: 'agent' | 'squad'; id: string }> {
  const re = /mention:\/\/(agent|squad)\/([\w-]+)/g;
  const results: Array<{ kind: 'agent' | 'squad'; id: string }> = [];
  let match;
  while ((match = re.exec(body)) !== null) {
    results.push({ kind: match[1] as 'agent' | 'squad', id: match[2] });
  }
  // 去重（同一条 comment @同一个 target 多次只排一次）
  const seen = new Set<string>();
  return results.filter((r) => {
    const key = `${r.kind}:${r.id}`;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}

// triggerFromComment —— comment 创建后触发任务派发（spec §7）
// 挂接点：人工 comment（comments.ts POST）+ agent 终态 comment（run-worker.ts completed 分支）
export function triggerFromComment(comment: Comment): void {
  // R4：只处理普通 comment（status_change 的 body 是 JSON，不含 mention）
  if (comment.type !== 'comment') return;

  const mentions = parseMentions(comment.body);

  for (const m of mentions) {
    if (m.kind === 'agent') {
      // S8：@agent 自指放行（照 multica comment.go:2177，cross-issue 自触发是 feature）
      // 防循环靠 per-(issue,agent) 去重 + 熔断，不靠 author 判定
      enqueueAgentRun(comment.issueId, m.id);
    } else if (m.kind === 'squad') {
      const leaderId = getSquadLeaderId(m.id);
      if (!leaderId) continue;
      // S8：@squad leader 自指跳过（防 leader @自己小队 → 触发自己 → 循环）
      // 简化判定（leader/worker 不重叠）：authorId === leaderId 则跳过
      if (comment.authorType === 'agent' && comment.authorId === leaderId) continue;
      enqueueLeaderRun(comment.issueId, leaderId, m.id);
    }
  }
}
```

- [ ] **Step 2: comments.ts POST 后挂 trigger**

comments.ts:53 `eventBus.publish({ type: 'comment:created', comment });` 之后加：

```typescript
    eventBus.publish({ type: 'comment:created', comment });
    // S04：comment-trigger 解析 mention 派任务（spec §7.3 入口1）
    triggerFromComment(comment);
```

comments.ts 顶部加 import：

```typescript
import { triggerFromComment } from '../orchestration/comment-trigger.js';
```

- [ ] **Step 3: run-worker.ts completed 分支挂 trigger**

run-worker.ts:166 `eventBus.publish({ type: 'comment:created', comment: toComment(cRow) });` 之后加：

```typescript
      eventBus.publish({ type: 'comment:created', comment: toComment(cRow) });
      // S04：agent 终态 comment 的 mention 触发 worker 派发（spec §7.3 入口2）
      triggerFromComment(toComment(cRow));
```

> 注意：toComment(cRow) 调两次会创建两个对象但内容相同，无副作用。或提取成变量复用。

run-worker.ts 顶部加 import：

```typescript
import { triggerFromComment } from './comment-trigger.js';
```

- [ ] **Step 4: typecheck + commit**

```bash
pnpm --filter @ma/server typecheck
git add app/packages/server/src/orchestration/comment-trigger.ts app/packages/server/src/routes/comments.ts app/packages/server/src/orchestration/run-worker.ts
git commit -m "feat(s04): comment-trigger 解析 mention 派任务（防自指 + 熔断）"
```

---

### Task 2.5: RunWorker 并发改造（★核心重写）

**Files:** Modify `app/packages/server/src/orchestration/run-worker.ts`

**必读：** spec §6.2（★核心重写，R3 修订）+ 现有 run-worker.ts 全文

> **这是 S04 最难的改动。** S03 的全局 busy 单 run 串行模式要改成 per-agent 槽 + 并发执行。建议执行者重写整个 tick 函数。

- [ ] **Step 1: 重写 run-worker.ts 的并发模型**

核心改动：
1. 删除全局 `busy` 标志
2. tick 改为遍历所有 queued，对每个检查其 agent 的 per-agent 槽位，可用的 claim 并 fire-and-forget 执行
3. `executeRun` 从 tick 内部提取为独立 async 函数（多个可并发）

```typescript
import { eq, and, asc, sql } from 'drizzle-orm';
import { db } from '../db/client.js';
import { agentRuns, runMessages, comments, agents } from '../db/schema.js';
import { toAgentRun, toRunMessage, toComment } from '../db/reshape.js';
import { eventBus } from './event-bus.js';
import { registerRunAbort, clearRunAbort } from './run-control.js';
import { getBackend } from '../runtime/registry.js';
import { buildPrompt } from '../runtime/prompt.js';
import { triggerFromComment } from './comment-trigger.js';
import type { AgentEvent } from '../runtime/types.js';

let timer: ReturnType<typeof setInterval> | null = null;

export function startRunWorker(): void {
  if (timer) return;
  timer = setInterval(() => { void tick(); }, 500);
}

export function wakeRunWorker(): void {
  void tick();
}

// S04 并发模型（spec §6.2 ★核心重写）：
// 遍历 queued，对每个检查其 agent 的 per-agent 槽位（active running < agent.concurrency），
// 可用的 claim 并 fire-and-forget 执行（不 await，多个 run 并发）。
async function tick(): Promise<void> {
  const queuedRows = db
    .select()
    .from(agentRuns)
    .where(eq(agentRuns.status, 'queued'))
    .orderBy(asc(agentRuns.createdAt))
    .all();

  for (const queued of queuedRows) {
    // per-agent 槽位检查
    const agent = db.select().from(agents).where(eq(agents.id, queued.agentId)).get();
    if (!agent) continue;
    const activeCount = db
      .select({ cnt: sql<number>`COUNT(*)` })
      .from(agentRuns)
      .where(
        and(eq(agentRuns.agentId, queued.agentId), eq(agentRuns.status, 'running')),
      )
      .get();
    if ((activeCount?.cnt ?? 0) >= agent.concurrency) continue; // 该 agent 槽满

    // claim（条件 UPDATE queued→running）
    const now = Date.now();
    db.update(agentRuns)
      .set({ status: 'running', startedAt: now })
      .where(and(eq(agentRuns.id, queued.id), eq(agentRuns.status, 'queued')))
      .run();
    const runRow = db.select().from(agentRuns).where(eq(agentRuns.id, queued.id)).get();
    if (!runRow || runRow.status !== 'running') continue; // 没抢到（被别的 tick 抢）

    const run = toAgentRun(runRow);
    eventBus.publish({ type: 'run:running', run });

    // fire-and-forget 并发执行（不 await）
    void executeRun(runRow);
  }
}

// executeRun —— 单个 run 的完整执行（从 tick 提取，支持并发）
async function executeRun(runRow: typeof agentRuns.$inferSelect): Promise<void> {
  const cwd = process.env.MA_WORKSPACE_CWD;
  if (!cwd) { await failRun(runRow.id, '未配置 MA_WORKSPACE_CWD'); return; }

  const prompt = buildPrompt(runRow.issueId, {
    isLeader: runRow.isLeader === 1,
    squadId: runRow.squadId,
  });
  if (!prompt) { await failRun(runRow.id, 'issue 不存在'); return; }

  const signal = registerRunAbort(runRow.id);
  let seq = 0;
  const nextSeq = () => ++seq;

  const onEvent = (e: AgentEvent) => {
    // [同 S03 onEvent 逻辑，原样保留]
    if (e.type === 'message_delta' || e.type === 'log') {
      eventBus.publish({ type: 'run:progress', runId: runRow.id, issueId: runRow.issueId, text: e.text });
      return;
    }
    let kind: 'assistant' | 'user' | 'tool_start' | 'tool_end' | 'system' = 'system';
    let body = '';
    if (e.type === 'message') { kind = e.role === 'user' ? 'user' : 'assistant'; body = e.text; }
    else if (e.type === 'tool_start') { kind = 'tool_start'; body = JSON.stringify({ name: e.name, args: e.args ?? null }); }
    else if (e.type === 'tool_end') { kind = 'tool_end'; body = JSON.stringify({ name: e.name, result: e.result ?? '' }); }
    const id = crypto.randomUUID();
    const createdAt = Date.now();
    const s = nextSeq();
    db.insert(runMessages).values({ id, runId: runRow.id, seq: s, kind, body, createdAt }).run();
    const message = toRunMessage({ id, runId: runRow.id, seq: s, kind, body, createdAt });
    eventBus.publish({ type: 'run:message', message, issueId: runRow.issueId });
  };

  try {
    const backend = getBackend(runRow.runtime);
    const result = await backend.execute(
      { prompt, cwd, issueId: runRow.issueId, agentId: runRow.agentId, runId: runRow.id },
      onEvent, signal,
    );
    clearRunAbort(runRow.id);
    const finishedAt = Date.now();

    if (result.exitReason === 'cancelled' || signal.aborted) {
      db.update(agentRuns).set({ status: 'cancelled', finishedAt, error: result.error ?? null }).where(eq(agentRuns.id, runRow.id)).run();
      const r = toAgentRun(db.select().from(agentRuns).where(eq(agentRuns.id, runRow.id)).get()!);
      eventBus.publish({ type: 'run:cancelled', run: r });
      return;
    }
    if (result.exitReason === 'failed') {
      await failRun(runRow.id, result.error ?? '执行失败');
      return;
    }
    // completed
    db.update(agentRuns).set({ status: 'completed', finishedAt, error: null }).where(eq(agentRuns.id, runRow.id)).run();
    const finalText = result.finalText || '(无输出)';
    const cid = crypto.randomUUID();
    db.insert(comments).values({
      id: cid, issueId: runRow.issueId, type: 'comment',
      authorType: 'agent', authorId: runRow.agentId, body: finalText, createdAt: finishedAt,
    }).run();
    const comment = toComment(db.select().from(comments).where(eq(comments.id, cid)).get()!);
    eventBus.publish({ type: 'comment:created', comment });
    triggerFromComment(comment); // S04：agent 终态 comment 触发 mention 派发
    const r = toAgentRun(db.select().from(agentRuns).where(eq(agentRuns.id, runRow.id)).get()!);
    eventBus.publish({ type: 'run:completed', run: r });
  } catch (err) {
    clearRunAbort(runRow.id);
    await failRun(runRow.id, String(err));
  }
}

async function failRun(runId: string, error: string): Promise<void> {
  // [同 S03 failRun，原样保留]
  const finishedAt = Date.now();
  const row = db.select().from(agentRuns).where(eq(agentRuns.id, runId)).get();
  if (row) {
    db.update(agentRuns).set({ status: 'failed', finishedAt, error }).where(eq(agentRuns.id, runId)).run();
    const r = toAgentRun(db.select().from(agentRuns).where(eq(agentRuns.id, runId)).get()!);
    eventBus.publish({ type: 'run:failed', run: r });
  }
}
```

- [ ] **Step 2: typecheck + 自测**

```bash
pnpm --filter @ma/server typecheck
# 起 server（设 MA_WORKSPACE_CWD），验证：
# 1. 指派普通 agent → 单 run 正常跑（S03 回归）
# 2. 指派 squad → leader run 跑（is_leader=1）
# 3. leader 完成 → comment-trigger 派 worker → worker 并发跑
```

- [ ] **Step 3: handoff + commit**

```bash
git add app/packages/server/src/orchestration/run-worker.ts
git commit -m "feat(s04): RunWorker 并发改造（全局 busy → per-agent 槽）★核心重写"
# 写 s04-impl-2.md 后
git add app/.progress/s04-impl-2.md
git commit -m "docs(s04): impl-2 handoff"
```

---

# 执行者片段 C（impl-3）：前端 + 验收

> **参考边界：** 前端几乎零新增（spec §8），重点是端到端验收闭环。
> **会话边界：** 读 spec §8/§9 + s04-impl-2 handoff。同一分支。产出 `s04-impl-3.md`。

### Task 3.1: 前端确认（可能零改动）

**必读：** spec §8

- [ ] **Step 1: 确认现有前端呈现闭环**

S04 前端复用 S02/S03 现有组件。验证：
- 详情页 AssigneeSelect 能选 squad（S02 roster 已含 squad）+ confirm（S03 N10）
- 指派 squad 后 RunStatusBar 显示 leader run 状态
- leader 终态 comment 在时间线显示（含 @mention pill）
- worker run 状态 + 终态 comment 在时间线显示

若现有组件已正确呈现，**零代码改动**。若有缺陷（如 squad assignee 的 label 不显示），小修。

- [ ] **Step 2: typecheck + commit（若有改动）**

```bash
pnpm --filter @ma/web typecheck
# 若有改动：
git add app/packages/web
git commit -m "fix(s04): 前端 squad 闭环呈现微调"
```

---

### Task 3.2: 端到端验收

**必读：** spec §9（全部验收标准）

- [ ] **Step 1: 工程门槛**

```bash
cd app
pnpm -r typecheck   # 全绿
MA_WORKSPACE_CWD="D:\code\multi-agent" pnpm dev
```

- [ ] **Step 2: squad→leader 路由验收**

```
# 指派 FRI-11（当前 squad:产品小队）触发 leader run
# 或用未指派的 FRI-04/FRI-08 指派给产品小队
# 验证：agent_run 表有 is_leader=1, squad_id='sqd-product' 的 run
# 验证：leader run 的 prompt 含三段 briefing（临时脚本调 buildPrompt 或看日志）
# 验证：roster 段含 agt-research/prd/proto 三个 mention，不含 agt-lead
```

- [ ] **Step 3: comment-trigger 闭环验收（核心）**

```
# leader(claude-code) 执行完成 → finalText 含 @mention
# → comment-trigger 解析 → worker run 被 enqueue
# → worker(opencode/cursor) 并发执行
# → 时间线呈现：leader briefing comment → worker 汇报 comment
```

- [ ] **Step 4: 防自指 + 熔断**

```
# @squad 且 author=leader → 跳过（不循环）
# @agent 自指 → 放行但去重
# issue run 总数达 15 → 熔断 system comment
```

- [ ] **Step 5: 回归**

```
# S01 看板 + S02 评论 + S03 单 agent 指派执行 不破坏
```

- [ ] **Step 6: 答辩路径**

```
# FRI-11 点亮 squad 段：看板 FRI-11（产品小队）→ 详情时间线见 leader briefing → @mention 委派 → worker 执行
```

- [ ] **Step 7: handoff + commit**

写 `app/.progress/s04-impl-3.md`，含每条验收 ✅/❌ + 证据。

```bash
git add app/.progress/s04-impl-3.md
git commit -m "docs(s04): impl-3 handoff（验收通过）"
```

---

## 计划者收尾（所有执行者完成后）

1. 读 s04-impl-3.md，确认验收全 ✅
2. 开 PR → 新会话审 diff
3. 合并 main
4. 写 `app/.progress/s04-planner-2.md`（切片总结 + FRI-11 squad 段点亮 + 给 S05 注意点）

---

## 自检

**spec 覆盖：**
- §1 数据流 → Task 2.1-2.5 全链路 ✓
- §3 数据模型 → Task 1.1 ✓
- §4 shared 契约 → Task 1.2 ✓
- §5 briefing 注入 → Task 2.1 ✓
- §5.1 squad→leader 路由 → Task 2.3 ✓
- §6 并发模型 → Task 2.2 + 2.5 ✓
- §7 comment-trigger → Task 2.4 ✓
- §7.4 熔断 → Task 2.2 ✓
- §8 前端 → Task 3.1 ✓
- §9 验收 → Task 3.2 ✓
- R1-R7 修订 → 全覆盖 ✓

**占位扫描：** 无 TBD/TODO，所有 step 有完整代码 ✓

**类型一致：** enqueueLeaderRun / triggerFromComment / loadSquadDetail / getSquadLeaderId / buildPrompt(run) 跨 Task 名称一致 ✓
