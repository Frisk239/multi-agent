# 补4 — Settings / 环境诊断（G0 只读）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans；本项目惯例 **计划者只验收 + 执行者 impl-1 → impl-2 串行**。  
> **spec（已批准）：** [`docs/superpowers/specs/2026-07-17-bu04-settings-design.md`](../specs/2026-07-17-bu04-settings-design.md)

**Goal:** `GET /api/settings/status` 聚合只读诊断 + `/settings` 页 + 侧栏/Ctrl+K，解释「为什么跑不起来」；不写 env、不回传密钥。

**Architecture:** 新建 `settings` 路由，复用 `allBackends().detect()`、`memoryManager.getStatus()`、`fs` 检查 `MA_WORKSPACE_CWD`；布尔探测 LLM/embed env。前端 React Query 拉状态，诊断列表按 error→warn→ok 排序。无 DB migration。

**Tech Stack:** 现有 Fastify monorepo、Zod shared、Next.js、现有 Icon `settings`。

## Global Constraints

- **分支：** `feat/bu04-settings` from **origin/main**  
- **建议：** main 已含补3 再开实现（无硬依赖；无 0009 冲突即可基于当前 main）  
- **G0：** 只读；**禁止**响应中出现任何 API key / token 字符串  
- **overall：** 仅 **`cwd` error → `blocked`**；其它 error/warn → 最多 `degraded`；全 ok → `ok`  
- **不 push main**；不 commit `wiki/`、`*.db`  
- **回归：** typecheck；issues/wiki/memory/inbox/runtimes 200  
- Conventional Commits：`feat(bu04):` / `docs(bu04):`  
- 计划者只验收  

---

## File Structure

```
app/packages/shared/src/schema.ts          Settings* types
app/packages/server/src/routes/settings.ts [新] GET /api/settings/status
app/packages/server/src/app.ts             register
app/packages/web/lib/api.ts                useSettingsStatus
app/packages/web/components/SettingsPage.tsx [新]
app/packages/web/app/settings/page.tsx     [新]
app/packages/web/components/Sidebar.tsx    nav 设置
app/packages/web/components/CommandPalette.tsx
app/packages/web/app/globals.css           诊断行样式（可选）
```

---

### Task 1: Shared 契约

**Files:**
- Modify: `app/packages/shared/src/schema.ts`（并确认 `index.ts` re-export 若需要）

**Produces:**

```ts
export const SettingsCheckStatus = z.enum(['ok', 'warn', 'error']);
export type SettingsCheckStatus = z.infer<typeof SettingsCheckStatus>;

export const SettingsOverall = z.enum(['ok', 'degraded', 'blocked']);
export type SettingsOverall = z.infer<typeof SettingsOverall>;

export const SettingsCheck = z.object({
  id: z.string(),
  label: z.string(),
  status: SettingsCheckStatus,
  detail: z.string().nullable(),
  hint: z.string().nullable().optional(),
  href: z.string().nullable().optional(),
});
export type SettingsCheck = z.infer<typeof SettingsCheck>;

export const SettingsStatusResponse = z.object({
  overall: SettingsOverall,
  summary: z.object({
    errors: z.number().int().nonnegative(),
    warnings: z.number().int().nonnegative(),
  }),
  checks: z.array(SettingsCheck),
  secrets: z.object({
    wikiLlmConfigured: z.boolean(),
    embeddingConfigured: z.boolean(),
  }),
  server: z.object({
    port: z.number().int().optional(),
  }),
});
export type SettingsStatusResponse = z.infer<typeof SettingsStatusResponse>;
```

- [ ] **Step 1: 写入 schema.ts**  
- [ ] **Step 2:** `pnpm --filter @ma/shared typecheck`  
- [ ] **Step 3: Commit** `feat(bu04): shared SettingsStatusResponse contract`

---

### Task 2: `GET /api/settings/status`

**Files:**
- Create: `app/packages/server/src/routes/settings.ts`
- Modify: `app/packages/server/src/app.ts`

**Implements `buildSettingsStatus(): Promise<SettingsStatusResponse>`（可同文件）：**

```ts
import { existsSync } from 'node:fs';
import { allBackends } from '../runtime/registry.js';
import { memoryManager } from '../memory/manager.js';
import type { SettingsCheck, SettingsStatusResponse } from '@ma/shared';

function envNonEmpty(name: string): boolean {
  const v = process.env[name];
  return Boolean(v && v.trim());
}

export async function buildSettingsStatus(): Promise<SettingsStatusResponse> {
  const checks: SettingsCheck[] = [];

  // --- cwd ---
  const cwd = process.env.MA_WORKSPACE_CWD?.trim() || null;
  if (!cwd) {
    checks.push({
      id: 'cwd',
      label: '工作区目录',
      status: 'error',
      detail: '未配置 MA_WORKSPACE_CWD',
      hint: '启动 server 前设置环境变量 MA_WORKSPACE_CWD 为项目根目录',
      href: null,
    });
  } else if (!existsSync(cwd)) {
    checks.push({
      id: 'cwd',
      label: '工作区目录',
      status: 'error',
      detail: `路径不存在: ${cwd}`,
      hint: '检查 MA_WORKSPACE_CWD 是否指向有效目录',
      href: null,
    });
  } else {
    checks.push({
      id: 'cwd',
      label: '工作区目录',
      status: 'ok',
      detail: cwd,
      href: null,
    });
  }

  // --- runtimes ---
  for (const b of allBackends()) {
    const d = await b.detect();
    if (!d.installed) {
      checks.push({
        id: `runtime:${b.id}`,
        label: b.label,
        status: 'error',
        detail: '未安装或不在 PATH',
        hint: '安装对应 CLI 或检查 PATH',
        href: '/runtimes',
      });
    } else if (!d.version) {
      checks.push({
        id: `runtime:${b.id}`,
        label: b.label,
        status: 'warn',
        detail: d.path ? `已安装（无版本）: ${d.path}` : '已安装（无版本）',
        href: '/runtimes',
      });
    } else {
      checks.push({
        id: `runtime:${b.id}`,
        label: b.label,
        status: 'ok',
        detail: `${d.version}${d.path ? ` · ${d.path}` : ''}`,
        href: '/runtimes',
      });
    }
  }

  // --- wiki llm ---
  const wikiOk = envNonEmpty('WIKI_LLM_API_KEY');
  checks.push({
    id: 'wiki_llm',
    label: 'Wiki LLM',
    status: wikiOk ? 'ok' : 'error',
    detail: wikiOk
      ? `已配置（provider=${process.env.WIKI_LLM_PROVIDER ?? 'openai'}）`
      : '未配置 WIKI_LLM_API_KEY',
    hint: wikiOk ? null : 'ingest/query/lint 需要 WIKI_LLM_API_KEY',
    href: '/wiki',
  });

  // --- embedding ---
  const embedOk =
    envNonEmpty('EMBEDDING_API_KEY') || envNonEmpty('OPENAI_API_KEY');
  const memMode = (process.env.MEMORY_PROVIDER ?? 'sqlite-text').toLowerCase();
  const embedStatus = embedOk
    ? 'ok'
    : memMode === 'pgvector'
      ? 'error'
      : 'warn';
  checks.push({
    id: 'embedding',
    label: 'Embedding',
    status: embedStatus,
    detail: embedOk
      ? '已配置 EMBEDDING_API_KEY 或 OPENAI_API_KEY'
      : '未配置 embedding 密钥',
    hint:
      embedStatus === 'ok'
        ? null
        : memMode === 'pgvector'
          ? 'MEMORY_PROVIDER=pgvector 时需要 embedding 密钥'
          : 'sqlite-text 可无 embedding；切换 pgvector 前请配置',
    href: '/memory',
  });

  // --- memory ---
  const mem = memoryManager.getStatus();
  checks.push({
    id: 'memory',
    label: '记忆层',
    status: mem.available ? 'ok' : 'error',
    detail: mem.available
      ? `provider=${mem.provider ?? 'unknown'}`
      : `不可用（provider=${mem.provider ?? 'null'}）`,
    href: '/memory',
  });

  // --- server ---
  const port = Number(process.env.PORT ?? 3001);
  checks.push({
    id: 'server',
    label: '服务',
    status: 'ok',
    detail: `监听端口 ${port}`,
    href: null,
  });

  const errors = checks.filter((c) => c.status === 'error').length;
  const warnings = checks.filter((c) => c.status === 'warn').length;
  const cwdBlocked = checks.some((c) => c.id === 'cwd' && c.status === 'error');
  const overall = cwdBlocked
    ? 'blocked'
    : errors > 0 || warnings > 0
      ? 'degraded'
      : 'ok';

  return {
    overall,
    summary: { errors, warnings },
    checks,
    secrets: {
      wikiLlmConfigured: wikiOk,
      embeddingConfigured: embedOk,
    },
    server: { port },
  };
}

export async function settingsRoutes(app: FastifyInstance): Promise<void> {
  app.get('/api/settings/status', async () => buildSettingsStatus());
}
```

`app.ts`：`await app.register(settingsRoutes);`

**安全自检：** 实现后对响应 `JSON.stringify` 断言不包含 `sk-` 或 env 中真实 key 子串（smoke 时人为设假 key 也不应出现在 body——只布尔）。

- [ ] **Step 1: 实现 settings.ts + register**  
- [ ] **Step 2: smoke**

```bash
# 无 MA_WORKSPACE_CWD
curl -s localhost:PORT/api/settings/status | jq '.overall, .checks[] | select(.id=="cwd")'
# expect overall=blocked, cwd status=error

# 有 CWD
MA_WORKSPACE_CWD=/path curl ...
# cwd ok
```

- [ ] **Step 3: typecheck + commit** `feat(bu04): GET /api/settings/status diagnostics`

---

### Task 3: Web Settings 页 + 导航

**Files:**
- Modify: `app/packages/web/lib/api.ts`
- Create: `app/packages/web/components/SettingsPage.tsx`
- Create: `app/packages/web/app/settings/page.tsx`
- Modify: `app/packages/web/components/Sidebar.tsx`
- Modify: `app/packages/web/components/CommandPalette.tsx`
- Modify: `app/packages/web/app/globals.css`（可选 `.settings-check*`）

**Hook:**

```ts
export function useSettingsStatus() {
  return useQuery<SettingsStatusResponse>({
    queryKey: ['settings-status'],
    queryFn: async () => {
      const res = await fetch(`${API}/settings/status`);
      if (!res.ok) throw new Error('加载环境诊断失败');
      return res.json();
    },
    staleTime: 10_000,
  });
}
```

**SettingsPage：**

- loading / error EmptyState  
- header：环境诊断 + overall badge（blocked 红 / degraded 黄 / ok 绿）  
- summary 文案：`${errors} 项错误 · ${warnings} 项警告`  
- checks 排序：error first, then warn, then ok（稳定：同 status 按原序）  
- 行：status 点、label、detail、hint、Link(href)  
- footer：本页只读说明  

**Sidebar：** config 区加  

```ts
{ id: 'settings', label: '设置', icon: 'settings', section: 'config', href: '/settings' },
```

**CommandPalette：** nav 项「设置」→ `/settings`

- [ ] **Step 1: api + page + nav**  
- [ ] **Step 2: typecheck**  
- [ ] **Step 3: Commit** `feat(bu04): settings diagnostics page and nav`

---

### Task 4: 回归 + handoff

**Files:**
- `app/.progress/bu04-impl-1.md` / `bu04-impl-2.md`
- 更新 phase4b 进度表

**验收勾选：**

- [ ] GET status shape + 无密钥  
- [ ] 无 CWD → blocked  
- [ ] `/settings` + 侧栏 + cmdk  
- [ ] runtime 行链 `/runtimes`  
- [ ] typecheck；issues/wiki/memory/inbox 200  

```bash
cd app && pnpm -r typecheck
git push -u origin feat/bu04-settings
```

---

## 执行者拆分

| 棒 | Tasks |
|---|---|
| **impl-1** | Task 1–2（shared + API + smoke） |
| **impl-2** | Task 3–4（UI + 导航 + 回归） |

串行。

---

## Self-Review

| Spec | Task |
|---|---|
| G0 只读 | 全程无写接口 |
| cwd blocked | Task 2 overall 规则 |
| runtime/wiki/embed/memory/server | Task 2 checks |
| 不回传密钥 | secrets 仅 boolean |
| /settings + nav | Task 3 |
| 无 agent 摘要 Must | 未实现（符合） |

---

## 明确不做

G1/G2 写 env；Autopilot；agent 就绪 bulk 摘要；e2e。

---

## 执行交接

**Plan complete and saved to `docs/superpowers/plans/2026-07-17-bu04-settings.md`.**

派执行者前建议 **补3 已合 main**。Kickoff：`app/.progress/bu04-planner-0.md`。
