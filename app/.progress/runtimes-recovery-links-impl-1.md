# Closeout: runtimes-recovery-links

## 证据

- typecheck 绿
- Playwright：
  - `runtimes-to-agents-missing` / `runtimes-to-failed-runs`
  - cwd 横幅 → agents cwd
  - 3 条 `runtime-agents-link`（首链 `?runtime=claude-code`）

## 交付

- 运行时探测页与 agents/runs 失败闭环互链

## 再下一刀建议

- 人指定主题；cwd 持久化策略若要做需 ADR
