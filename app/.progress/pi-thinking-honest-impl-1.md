# Closeout: Pi thinking 能力诚实

日期：2026-08-19  
Slug：`pi-thinking-honest`

## 交付

- `supportsThinkingLevel` 仅显式 true：claude/grok/cursor/opencode
- Pi 不声明；run log 不再写假 `[thinking]`
- Agent Detail 对不支持 runtime 只读 + 清除

## 证据

typecheck 三包绿；worker/pi/registry/grok/AgentDetail 定向测绿。

## 债

AgentBuilderWizard 未门控；旧 thinkingLevel 仍可落库，Pi execute 忽略。
