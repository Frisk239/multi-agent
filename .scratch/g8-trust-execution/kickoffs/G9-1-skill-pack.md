# 可选执行者 Kickoff · G9-1 Builtin 方法论 skill pack

> 非 G8 主队列；产品要「可感知差异、零 runtime 风险」时启用。

---

## 启动提示词（复制从下一行开始）

```
你是本仓实现执行者。工作区：multi-agent 仓库根。

## 本刀：G9-1 · Builtin 方法论 skill pack（可选）
学自 references/repos/gstack 的「角色链 Markdown skill」思想，**不**接入 gstack browse daemon。

### Must
1. 在本仓既有 skill builtin 目录（查找 `ma-mentioning` / `ma-squads` 等同级）新增 3–5 个工程方法论 skill，例如：
   - 计划 / 实现清单
   - 代码审查清单
   - 测试与验收
   - 关刀/交付说明
2. 纯 Markdown（+ 本仓 skill frontmatter 惯例）；可被 Skills 页扫描到。
3. 文案中文为主，贴「本地多智能体编排」场景。
4. 不改 runtime adapter；不新增 MCP。

### Out
- gstack 浏览器、ngrok、iOS QA
- 强制所有 agent 默认挂载（可文档说明如何挂载）

### 验收
- Skills 列表可见；打开正文可读；scanner 测试不红。

### 回报
skill 路径列表、挂载说明 3 行。
```
