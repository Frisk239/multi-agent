# Push debt · 2026-07-17 会话末

本地 `main` **ahead 3** of `origin/main`（`bd6ec14`）：

1. `5574b1c` feat: jump to active runs from the command palette  
2. `455f863` feat: show in-flight run chip in the sidebar status row  
3. `62c5fc8` feat: preview automation title and body templates while creating  

更早已推到远程：`a3538f2` runs-active-nav · `bd6ec14` automation-next-run。

网络恢复后执行：

```bash
git push origin main
```

勿 commit `app/packages/server/wiki/`。
