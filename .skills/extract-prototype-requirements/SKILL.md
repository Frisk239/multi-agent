---
name: extract-prototype-requirements
description: 从可交互原型反提需求清单
---

# Extract Prototype Requirements

当被要求从原型（`chanpin/prototype/`）反提需求时：

1. 通读原型 seed 数据与 UI 组件，识别所有可交互元素
2. 将每个元素映射成一条需求（动词 + 对象 + 约束）
3. 标注需求来源（哪个原型页面/组件）
4. 输出结构化清单，供 PRD 编写者消费

原型是需求真源，不得臆造原型里没有的功能。
